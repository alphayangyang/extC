# VISION.md —— extC 的设想（一份合并版）

> 来源：`DESIGN.md`（P 与前提）、`DECISIONS.md`（定案）、以及主人在对话里历次定的调。
> **凡标「原话」的是主人说过的；标「推论」的是从他的话推出来的。**
> 本文只做汇总，**不引入新决定**。

---

## 1. 定位

**原话**（2026-09-18）：

> 「我的目标也不只是五子棋，如果只是为了让我方便写五子棋的话那我直接用 C 写也不是不行。
> 主要是我就是觉得 **C C++ 都太垃圾了**（C 太原始 C++ 太混乱），
> **Go 我觉得很好但是不够安全，Rust 太陡峭了**。」

⇒ **推论**：extC = **Rust 的安全保证 + 不写生命周期 + Go 那么少的语法量**。

**四句抱怨 → 四条具体决定**：

| 抱怨 | extC 的答案 |
|---|---|
| **C 太原始** | `slice<u8>` 带长度；prelude 里的容器；泛型单态化；**arena + 逃逸检查**（永不写 `free`） |
| **C++ 太混乱** | **一门语言一件事**；无隐式转换（只许无损失拓宽）；`!` 是唯一签字符号；统一规则、无特例 |
| **Go 不够安全** | **不可为空引用**；trap 带 extC 位置；`option`/`result` + `?`；无 interface/反射；**无 GC** |
| **Rust 太陡峭** | **深度是词法的** —— 比两个整数，**用户不写生命周期**；唯一逃生舱是 `extern!`，**可 grep 数出来** |

---

## 2. 三条前提

| # | 前提 | 状态 |
|---|---|---|
| **G1** | **内存自动回收** —— 用户永不写 `malloc` / `free` / `close` | ✅ 从未破过（生成的 C 里 `malloc` 出现 0 次） |
| **G2** | 第一个真实用户是五子棋 | ✅ 棋盘/五连/`result` 都能写 |
| **G3** | 能自举 | 🔸 **原话修正**：「我最终不一定要完全自举，我觉得 C 写语法解析底层效率很高」 ⇒ 降级为**可选的验证手段** |

**原话**（工作方式）：

> 「**能在 extC 里面写的东西就在 extC 里面写**」（除非很难，或者很影响效率）
> 「不要使用 python —— 毕竟是 C 程设作业」
> 「note 以后能不能全部用英语」（编译器输出）／「每次写完之后，直接输出一段示例代码告诉我现在新支持了什么写法」／「依旧上 git 维护」／「语法/设计细节可以找我确认」

---

## 3. 一条原则

> **P：凡是编译期能证明的，运行时不留痕迹。**
> **P′（同一句话的另一面）：不能证明的，语法上必须看得见。**

**P 禁的是「隐藏的机制」**（GC / 异常 / 运行时元数据 / 隐藏分配 / 运行时类型标签 / 隐藏控制流），
**不是数据拷贝**（值语义的拷贝写在类型上、看得见）。

**推论**：内存归 arena、错误归 `result`、多态归**标签联合 + `match`**、分派归**单态化**、越界归范围类型。

**`!` = 「这里我签字，接受运行时的后果」**（`a[i]!` / `@recursive!` / `extern!`）——
而且是**稀缺**的：`grep -c '!'` 就是一次逃生舱审计。

---

## 4. 语法与命名口味

**原话**：

> 「我是这种命名派：`var thisIsAGoodName: i32`」

| 项 | 定调 |
|---|---|
| 声明 | `let` / `var`，**类型写在名字后面**（`name: Type`） |
| 命名 | 变量 camelCase；**类型名首字母小写**（强制）；泛型参数首字母大写（`T`/`K`/`V`） |
| 类型名 | **不带位宽不许用**：`i32` 而不是 `int`；`size_t` 一律写 `i64` |
| 标点 | **换行即语句结束**，分号可选；无 `as`、无指针算术 |
| **显式优于推导** | **原话**：「显式是对的，不要那么多自动推导」⇒ `a == { ... }` 右边裸字面量**不推**，必须写 `point { ... }` |
| 名字品味 | **原话**：`VarArray` / `WString` / `is_empty` / `to_bytes` 这类名字**丑** |（后 `VarArray` 由主人自己改回 `varArray`）|

---

## 5. 内存模型

> **一只 arena = 一个词法作用域。** arena 构成嵌套树，越内层深度越大。
> **唯一分配规则**：所有资源属于某个 arena；arena 结束时它的资源一起死。
> **唯一引用规则**：`depth(r) ≥ depth(v)`（引用不能活得比被指对象长）。

**定死**（2026-09-18）：**arena 永远是词法的**，不是动态的 —— 否则递归里每个节点的临时内存都活着 ⇒ O(节点数) 爆。

| v0 里像独立机制的东西 | 现在是 |
|---|---|
| 局部变量 / 栈 | **arena = 函数帧**（同一个东西） |
| `box<T>` / `varArray<T>` / string / file | 只是「在哪只 arena 里」 |
| `region` | **显式命名的 arena**（全语言为 arena 新增的唯一语法） |
| **全局变量** | **深度 0 的 arena** ⇒ `static` 关键字因此消失 |

**深度**：参数 = 0，函数体 = 1，每进一层块 +1。**比两个整数**，不写生命周期 ✓

**实现**（已完成）：每帧一只 arena 对象（块链表）+ `alloc<T>(n)` 原语；`varArray` 记住「出生在哪只 arena」（`home` 句柄）。

---

## 6. 引用的完整语义

| 项 | 定案 |
|---|---|
| **`ref T` / `mut ref T`** | **只读是默认**（安全是默认），要写就显式写 `mut` |
| **可写性从哪来** | ① 绑定 `var`/`let` ② 参数 `mut ref`/`ref` ③ 字段类型上写 `mut`。**取引用时权限从源头继承** ⇒ 日常代码不用写 `mut` |
| **值位置自动解引用** | `p` 当值用 ⇒ 是它指向的值；`p = v` ⇒ 写进它指向的地方 |
| **`ref x` 自己不解引用** | 显式要的引用不该被自己解掉 ⇒ `let r = ref n` 拿到引用，`let y = r` 拿到值 |
| **换指向** | **允许**，判据是**右边**：值是写进去、引用是换指向（变量和字段同理） |
| **视图可写性** | `slice<T>`（只读）/ `mut slice<T>`（可写）——**一个结构体、一套方法集**，`mut` 是限定词不是第二个类型 |
| **`let` 是命名，`var` 是存储** | **原话**：「`let` 是可以被修改的…本质是在里面重新覆盖 `a` 的含义，而不是写入」⇒ `let` 可**遮蔽**（含同一层） |
| **逃逸检查** | 词法深度；`alloc` 出来的内存深度 1（活到函数返回）⇒ **返回它就是编译错误** ✓ |

---

## 7. 错误处理

| 项 | 定案 |
|---|---|
| **无异常** | 异常是运行时展开的控制流 ✗；`result<T,E>` 是**值** ✓ |
| `option<T>` / `result<T,E>` / `unit` | **写在 `stdlib/prelude.extc` 里**（extC 源码）✓ |
| **`?`** | 可见的、静态解析的、**无栈展开**的转发；四个位置：`f()?` / `let x = e?` / `x = e?` / `return e?` |
| `trap` | 越界/除零等 = **带 extC 位置的 trap**，不是 UB ✓ |

---

## 8. 多态与分派

| 项 | 定案 |
|---|---|
| **泛型** | 单态化（每个实例生成一份 C）——**没有运行时分派** |
| **没有 trait / interface / vtable / 反射** | 「做成更好的 pthreads」不是答案；**标签联合 + `match` + 穷尽检查 = 写出来的分派** ✓ |
| **协议而不是 trait** | `==` 由类型自己写 `fn ==`；视图靠 `data` + `len`；`?` 靠 `ok`/`has`/`err`/`value` —— **语言认识协议，库提供结构** ✓ |
| **带载荷枚举 + `match`** | ⬜ 计划中（替代函数指针的主力） |
| **函数值（lambda）** | **原话**：「lambda 为什么不能存起来…可以规定只能用 `let` 修饰符，且不能够修改」⇒ **传、存、捕获都允许**；判据是**函数身份编译期唯一** ⇒ 直接调用、零堆、零间接 ✓<br>✗ **禁区：运行期才知道调谁**（间接调用违反 P） |
| **运行期换实现 / 插件表 / mock** | ✗ **永久不做**；代替品 = 枚举 + `match`（封闭集合）、或**多入口 + 字节协议**（模块边界） |

---

## 9. 边界（跟外部的接口）

| 项 | 定案 |
|---|---|
| **`extern!` 是唯一逃生舱** | 「不该是卖点，也不该永久禁止」；**标准库可调 libc（编译器生成），用户代码不能** |
| **安全的四条防线** | ① 谁能写（`reserved` = **加载来源**，伪造不了）② 白名单（短到能背）③ 六条可查规则 ④ 混合期**对拍** |
| **ISO C 是基线** | **原话**：「我做肯定是先做 ANSI C（或者 ISO C）」⇒ 实测**只需约 10 个函数**（`mem*` 6 + 文件 IO 4）；`str*` 家族价值为零 |
| **沙箱** | 进程沙箱最便宜（**主人的五子棋引擎已经是这个形状**）；`fork`/`seccomp` 是 POSIX ⇒ 最不可移植的一块 |
| **模块化 / 多入口** | 定案 6：**`@main` 标在任意函数上**；备注「多入口 / 命名入口等模块系统」⇒ **一个源码树编出多个可执行文件，用字节协议说话** = extC 的「模块化事件驱动」✓ |

---

## 10. 明确**不做**的（清单）

无 GC、无引用计数、无异常、无手动 `free`/`malloc`/`close`、无 `null`、无裸指针算术、
无 `size_t`、无 `int`/`long`/`char`/`double`（一律带位宽）、无 `static`、无 `impl` 块、
无 trait/interface/vtable/RTTI、无反射、无 `as` 链、无变参函数、无 C 互操作给用户、
无运行期函数身份、无全局可变状态（全局已经就是全局）、无析构函数/`Drop`。

---

## 11. 代价与损失（诚实清单）

| 损失 | 程度 |
|---|---|
| **运行期换实现**（插件热插拔、框架、动态回调注册） | ✗ 永久 |
| **同一类型多种排序序** | ✅ 已解决（lambda，`sortBy`） |
| **访问者模式**（一次遍历多种操作） | ✅ 已解决（lambda；或枚举 + `match`） |
| **包带回调的 C 库**（`qsort`/`pthread`/GUI） | 🙂 `extern!` 边界放开函数指针即可 |
| **`mock` / 测试注入** | 🙂 编译期换实现 |
| **跨线程的引用** | 设计上「引用不过线程」 |
| **arena 增长留垃圾**（旧 buffer 攒到帧结束） | ⚠️ 资源问题；可加 `reserve()` |
| **容器拷贝 = 共享存储**（两个 `len` 会漂） | ⚠️ 逻辑坑，要写文档 |
| **没有生态** | ⚠️ **结构性**：不能随便包 C 库、不能给别人当框架 |

---

## 12. 靶子与验收

**方法论**（`PLAN.md`）：**把真实程序当靶子，每完成一块就把降级版往前推一格** ——
「语言够不够用」永远由**真实程序**回答，不由设计文档回答。

| 靶子 | 测什么 | 状态 |
|---|---|---|
| **1 · 五子棋引擎** | 数组、切片、递归、`result`、stdio 协议、输入 | ✅ 基本能（差输入） |
| **2 · 带生命周期的小程序** | **arena、逃逸检查、视图**（源码 → token 视图 → arena AST → 求值） | ⚠️ 能写，但会绕过核心机制 |
| **3 · 自举** | 全部 | 🔸 **可选**（主人降级了） |

**验收标准**：`make` 无警告；`./tests/run.sh` 全绿（现在 99）；**每个特性配一个能跑的
`examples/`**；做完一块输出一段示例代码（主人定的维护规矩）；`grep -inE 'struct slice|slice_[a-z]' src/*.c` 必须一无所获。

---

## 13. 现在在哪（2026-09-18 末）

| 已完成 | 未完成 |
|---|---|
| 类型层、`ref`/`mut ref`、泛型单态化、prelude 机制 | **悬垂反例**（`refDepth(函数调用)=0` 是错的上界）⬅ 唯一的真 bug |
| 固定数组、切片视图 + 可写性、`option`/`result`/`?` | **④ 参数洗白**（借来的值能存进全局） |
| `::` 关联函数、位运算、全局变量/常量 | `match` + 带载荷枚举、`@main`、模块 |
| **逃逸检查（词法深度）** | `arena` 内建类型 + `varArray<T>` |
| arena 运行时（每帧一只 + `alloc` 原语） | lambda、输入、`for`、算术 UB 两处 |

**六步计划见 [`BOOTSTRAP.md`](BOOTSTRAP.md) §8。**
