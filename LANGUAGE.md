# LANGUAGE.md —— extC 语言设计（正式版）

> **这份文档是「extC 是什么」的唯一权威版本。**
>
> 它不是手册（`MANUAL.md` 只描述**已实现**的东西），不是决策记录（`DECISIONS.md`），
> 也不是开发日志（`DEVLOG.md`）。它回答的是**这门语言想成为什么、以及为什么**。
>
> 每个论断都标了状态：**✅ 已实现** · **🔸 部分** · **⬜ 计划** · **⛔ 永久不做**
>
> 日期：2026-09-18 ｜ 基线：commit 5806385 ｜ 测试：116 全绿
>
> ⚠️ **2026-09-23 同步说明**：上面那行的**基线是 09-18 的**（116 测试），而实测是 **251 通过 / `check.sh` 14 节全绿** ✗
> 正文的 ✅/🔸/⬜ 标记已在 2026-09-23 **逐条实测复核**过一遍，**改掉了 7 处「早就做完却还标着待做」**
> （`match` 第三刀 · arena 内建 · `varArray` · 有损转换 · IO 长度类型 · 块=释放点 …）✗
> ⇒ **这份文档的「权威」指的是设计口径，不是实现状态**；状态一律以 `PLAN.md` §0.4 + 实测为准 ✓

---

## 0. 定位

### 0.1 一句话

> **用编译期检查替代运行时机制。而凡是编译期能证明的，运行时不留痕迹。**

### 0.2 主人自己的话（这是全部设计的源头）

> 「我就是觉得 C C++ 都太垃圾了（C 太原始 C++ 太混乱），Go 我觉得很好但是不够安全，Rust 太陡峭了」

拆开来就是这门语言的**三个坐标**：

| 坐标 | 取自 | 具体是什么 |
|---|---|---|
| **安全** | Rust | 没有悬垂引用、没有 UB、没有手动释放。**但不需要写生命周期标注** |
| **简单** | Go | 语法量少到能一页看完；没有重载、没有继承、没有模板元编程 |
| **底层** | C | 编译到 C，能跟 C 库和操作系统说话，性能可预期，没有隐藏的运行时 |

### 0.3 它不是

| 不是 | 因为 |
|---|---|
| C 的方言 / 超集 | 它**删掉**了 C 的一大半（指针算术、`static` 的三层含义、`as` 强转、`errno`、`scanf`、`qsort`）|
| Rust 的简化版 | 它**不写生命周期** —— 那不是"简化语法"，是换了一套模型（词法深度 + arena）|
| 带 GC 的现代语言 | 没有任何 GC、引用计数、运行时元数据 |
| 又一门"安全 C" | 那些通常只加了检查；extC 是**先定原则，再让语法从原则里长出来** |

### 0.4 两个前提（不是原则，是选择）

| # | 前提 | 状态 |
|---|---|---|
| **G1** | **内存自动回收** —— 用户永远不写 `malloc` / `free` / `close` | ✅ 语言里连这些名字都没有（生成的 C 里 `malloc` 出现 **0** 次）|
| **G2** | 第一个真实用户是做五子棋（**不是**自举） | ✅ 棋盘 / 五连判定 / `result` / `match` 都能写 |
| **G3** | 能自举 | 🔸 **降级**为可选的验证手段（原话：「我不一定完全自举，我觉得 C 写语法解析底层效率很高」）|

### 0.5 ⭐ extC **承诺什么、不承诺什么**

> 主人 2026-09-18 的疑问：「这似乎并不能满足我的原则啊，还是不可避免会引入运行时错误」
>
> **答案：目标从来不是"没有运行时错误"** —— 那是任何语言都做不到的（可判定性）。
> 目标是**把失败分类**：该在编译期消灭的消灭掉，消灭不掉的**语法上看得见、并且失败得响亮**。

| 失败类别 | 例子 | extC 怎么处理 | 状态 |
|---|---|---|---|
| **悬垂引用** | 返回指向本帧的视图 | **编译期挡住**（深度规则）| ✅ |
| **空引用** | `NULL` 解引用 | **类型里没有空** ⇒ 不存在这件事 | ✅ |
| **未初始化** | 读到垃圾 | **默认零初始化** | ✅ |
| **越界**（能证明）| 长度 3 却写 `a[10]` | **编译期报错** | ✅ |
| **越界**（不能证明）| `a[i]`，`i` 是运行时算的 | **运行时 trap + 源码位置**（不是 UB）| ✅ |
| **隐藏机制** | GC / 异常 / `errno` / `static` | **语言里根本没有** | ✅ |
| **整数溢出** | `i32` 最大值 + 1 | wrap（`-fwrapv`），**行为有定义** | 🔸 |
| **除零** | `a / 0` | **trap 带源码位置**：`main.extc:5: trap: division by zero`（还查 `INT64_MIN / -1`）| ✅ |
| **移位超宽** | `a << 64` | **trap 带源码位置**：`trap: shift count out of range`（负数也一样）| ✅ |
| **零值毒标记** | `option<slice<u8>>` | 报错落在 **prelude 里**，用户看不见自己的代码 ⇒ **该补成检查器报错** | ⚠️ **欠** |
| **别名 / 旧数据** | 两次读进同一个缓冲后还用第一份 | **抓不住** ⇒ 靠 **API 形状** + 语法可见性 | ⛔ 边界（见 §0.6）|
| **过期视图读到的字节** | 视图过期后继续读 | **永远不是野指针、永远不崩** —— 见 §0.6 ⭐ | ✅ |
| ⭐ **不能把借来的引用存进「参数指向的对象」** | `fn bind(s: mut ref slot, t: mut ref i32) { s.r = t }` ⇒ **拒**。理由：`s` 指向的对象活得多久，**被调者不可能知道**（两个参数寿命互不相关；函数体只检查一次却有多个调用点）⇒ 要更多信息就得**写生命周期标注** ✗ —— 这是**"不写生命周期"的直接代价** ✓ **出路**：① 同一帧里改 ② **构建并返回**（`withTarget(s, t) -> slot`；Rust 的 builder / JS 的 `{...obj, k: v}` 也是这个形状 ✓）③ 大状态放进同一帧改 ⚠️ **比 Rust 严**（Rust 写个 `'a` 就过去了）| ⛔ 定位的代价 |
| **逻辑错误** | 算法写错了 | 不归语言管 | — |

**三条推论**（这就是"原则"的全部含义）：

1. **该消灭的**：悬垂、空引用、未初始化、隐藏机制 —— 已经消灭 ✓
2. **消灭不掉的**：越界、溢出、除零、移位 —— **必须失败得响亮**（trap + 位置），
   而不是静默错、更不是 UB。**除零和移位已经补上** ✅（`tests/traps/` 就是钉这两条的）
3. **根本抓不住的**：同层别名 / 旧数据 —— 抓它需要**生命周期标注**，
   而"不写生命周期"是这门语言的定位 ⇒ **这是自觉付掉的代价**，
   支付方式是「让它在源码里看得见」（P′）✓

> **对比 Rust**：Rust 也**不**保证"没有运行时错误" ——
> 越界会 panic、`unwrap()` 会 panic、`Rc<RefCell>` 运行期崩、死锁没人管。
> Rust 保证的是 **safe 代码里没有 UB**。extC 的承诺**同一档**，
> 只是把"借用检查"换成了"词法深度 + arena" ✓
>
> ⚠️ 反过来说：**extC 比 Rust 松的地方**（同层别名、内部可变性那一类）
> 要么靠 API 形状避开，要么就得承认 —— 那是**线性类型/生命周期**的领域，
> 那是另一门语言了 ✓

### 0.6 ⭐ 特性：**视图是窗口，不是快照**

主人 2026-09-18 问：「同层别名是啥」→「这个可以当成语言特性吗」—— **可以，而且应该。**

**先切干净：这里有两件事，一件是特性，一件是边界。**

| | 是什么 | 归属 |
|---|---|---|
| **别名**（两个值指向同一块）| 允许，而且**是视图的基础语义** | ✅ **特性** |
| **过期之后仍然是安全数据** | 不崩、不是 UB、不是野指针 | ✅ **特性**（P″ 白送）|
| **过期在编译期抓不住** | 编译器不报错 | ⛔ **边界**（要生命周期标注）|
| **两个 `mut ref` 同时指同一块** | 允许（Rust 不允许）| 🔸 比 Rust 松，文档说明 |

### 承诺（一句话）

> **extC 里不存在「已经被归还的存储」。**
> 因为没有手动释放（P″），存储只在**词法作用域的出口**归还；
> 而深度规则保证**引用不会活得比它指的东西长** ⇒
> 一个引用只可能指向**活着的存储**，或者**根本不存在**（编译期就被挡住了）。
>
> ⇒ **「过期」只意味着「内容变了」，永远不意味着「地址坏了」。**

**一句话版本：视图是窗口，不是快照。** 你透过窗口看的是"现在"，不是"你拿到它那一刻" ✓

**为什么这是特性而不是缺陷**：

| 语言 | 「视图指向的缓冲被换掉」会怎样 |
|---|---|
| C / C++ | **UB**（`vector` 扩容后旧指针 —— 可能崩、可能静默坏）|
| Rust | **编译期拒绝**（借用检查器）|
| **extC** | **旧块还在**（arena 到帧出口才释放）⇒ 最坏是**内容过时**，**永远不崩** ✓ |

### ⚠️ 这条承诺的一个前提（今天还没满足）

**「过期读到的永远是初始化过的字节」**要求 arena 给的区是**清零**的。
今天 `alloc<T>(n)` 是**裸 bump 分配**（不清零）⇒ 读没写过的地方 = **不确定值**
（虽然不崩，但"读到的字节是确定的"这条就不成立了）。

⇒ **补 `allocSlice<T>(n)` 时顺便清零**，承诺才完整 ✓

---

## 1. 设计原则

### 1.1 P：凡是编译期能证明的，运行时不留痕迹

**这是唯一的原则。** 所有别的设计决定都是它的推论或它的边界。

它禁的是**隐藏的机制**：

| 禁 | 为什么 |
|---|---|
| GC | 一块看不见的、会在任意时刻暂停你的东西 |
| 异常 / 栈展开 | 控制流不在语法里 |
| 运行时类型标签 | 类型信息本该在编译期用完 |
| 隐藏的分配 | 你写的代码看不见"这里申请了内存" |
| 隐藏的全局状态（`errno`、`static` 局部变量）| 行为取决于你没写的一行 |

它**不**禁**数据的拷贝**：值语义的拷贝写在类型上，是看得见的 ✓

### 1.2 P′：不能证明的，语法上必须看得见

P 的逆否命题，与 P 等价。它管的是"人的部分"：**读者必须能从句子里看出代价**。

推论：

- 可能失败的地方，**类型上**看得见（`result<T, E>`，不是哨兵值）
- 可能没有的地方，类型上看得见（`option<T>`）
- 越界可能在运行时发生 ⇒ **运行时 trap 带源码位置**（不是 UB）
- 需要人签字的地方 —— 见下 ↓

### 1.3 `!`：我签字

**凡是编译器证明不了、只能靠人担保的地方，语法上要有一个 `!`。**
它是**唯一的逃生舱标记**，而且**必须可数**：

```bash
grep -c '!' src/*.c        # "这个代码库在几处签了字？"
```

一个仓库里的 `!` 数量应当是**个位数**。它变成十位数就说明设计错了。

### 1.4 P″：没有手动分配，就没有手动释放

分配绑在**词法作用域**上 ⇒ 不需要析构函数、不需要 `free`、不需要 GC ✓
这是 arena 的机械形式，见 §4。

### 1.5 口味：显式优于推导

主人的原话：**「显式是对的，不要那么多自动推导」**。

| 显式 | 不显式 |
|---|---|
| 类型写在名字后面：`var thisIsAGoodName: i32` | 不下推结构体字面量的类型 |
| 关联函数写全：`option<i64>::some(3)` | 不从上下文猜类型实参 |
| 转换要写出来（没有 `as` 链）| 不隐式截断 |
| 泛型要写实参 | 不做类型推导 |

⚠️ **一处例外**（定案 49）：`return` 位置上可以裸写 `success(v)` / `failure(e)`。
那不是推导 —— 上下文是**你自己写的**返回类型，省掉的只是重复劳动 ✓

---

## 2. 语言表面

### 2.1 完整的一个程序

```extc
type cell = | empty | black | white

struct board {
    cell: [15][15]cell
    moves: i64
}

fn place(b: mut ref board, x: i32, y: i32, p: cell) -> result<unit, i32> {
    if x < 0 || x >= 15 || y < 0 || y >= 15 {
        return failure(1)                       // 出界
    }
    if b.cell[y][x] != cell.empty {
        return failure(2)                       // 有子
    }
    b.cell[y][x] = p
    b.moves = b.moves + 1
    return success(unit {})
}

fn main() -> i32 {
    var b: board                                // 默认零初始化
    let r = place(ref b, 7, 7, cell.black)
    match r {                                   // 穷尽检查：漏一个变体就编不过
        success(u) => { println("落子成功，第 ", b.moves, " 手") }
        failure(e) => { println("失败，原因码 ", e) }
    }
    return 0
}
```

> ✅ **第三刀已落地（2026-09-20）**：`option` / `result` 现在是 `stdlib/prelude.extc` 里的
> 两个**普通泛型枚举** ——
> ```extc
> type option<T>    = | none | some(T)
> type result<T, E> = | failure(E) | success(T)
> ```
> 零值是 tag 0（`none` / `failure`，跟以前一致），没有隐藏字段、没有魔法方法，
> 而且**载荷里可以有 `ref`**（`option<slice<u8>>` 合法 ⇒ IO 的洞封上了）✓

### 2.2 名字的规矩

| 东西 | 规矩 | 例子 |
|---|---|---|
| 类型 / 枚举 / 结构体 | **小写开头** | `board`、`gameError` |
| 变量 / 函数 / 字段 | **小写开头 camelCase** | `thisIsAGoodName`、`readLine` |
| 泛型参数 | **大写开头** | `T`、`A`、`B` |

大小写规则不是风格建议，是**语法**：它让「类型名」和「变量名」的集合**不相交**，
所以 `shape.circle(2.0)` 这种写法不需要任何歧义消解 ✓

### 2.3 有意不做的语法

| 不做 | 因为 |
|---|---|
| `as` 强转链 | 每一次有损转换都该写出来（`.truncate()` / `.checked_to_int()?`）|
| 三目 `?:` | 用 `match` 或 `if` |
| 自增 `i++` | 一个表达式不该有两个效果 |
| 隐式类型提升 | 只允许**无损**拓宽 |
| `**`、运算符重载的任意符号 | 只有 `==` / `!=` 是协议 |

---

## 3. 类型系统

### 3.1 内建

| 类别 | 类型 |
|---|---|
| 有符号 | `i8` `i16` `i32` `i64` |
| 无符号 | `u8` `u16` `u32` `u64` |
| 浮点 | `f32` `f64` |
| 其它 | `bool`、`void`；`unit`（零字段结构体）**写在 prelude 里**，不是内建 |

**没有 `int` / `long` / `char`。** 位宽写在名字里 ✓

### 3.2 聚合

| 类型 | 形态 | 语义 |
|---|---|---|
| 定长数组 `[N]T` | 值 | 按值拷贝，长度**编译期常量** |
| 视图 `slice<T>` | `{ data: ref T, len: i64 }` | **借用**，不拥有数据 |
| 可写视图 `mut slice<T>` | 同一个 C 结构体 | `mut` 是**限定词**，不是第二个类型 |
| 结构体 `struct` | 值 | 字段、方法都在体内 |
| 枚举（无载荷）| C 的 `enum` | 只有名字，回答"是哪一种" |
| 枚举（带载荷）| `struct { tag; union }` | 回答"是哪一种 + 附带的东西是什么" |
| 泛型 | 单态化 | 一份源码，每个实参组合生成一份代码 |

### 3.3 零值（定案 8）

**每个类型都有零值，`var x: T` 就是它。** 所以：

| 类型 | 零值 |
|---|---|
| 数值 | `0` |
| `bool` | `false` |
| 数组 / struct | 所有字段清零 |
| `option<T>` | 没有（`none` 排第一）|
| `result<T,E>` | 失败侧（`failure` 排第一）|
| **带载荷枚举** | **tag 0 那个变体 + 载荷清零** ⇒ **变体顺序有意义** |
| `ref T` | ⛔ **不存在** —— 引用不可为空，所以含 `ref` 的类型没有零值 |

最后一行是**一条真规则**：`var p: ref i32` 是编译错误。它同时也是 tagged union 的一条约束：
**tag 0 的载荷不能含 `ref`**（否则这个类型没有零值）✓ 复用已有规则，不新增概念。

### 3.4 转换

**只有无损拓宽是隐式的**（`i32 → i64`），其余都要写出来。转换是**方法**，不是运算符。

---

## 4. 内存模型

### 4.1 内存管理 = **栈 + arena + 静态** 的复合模型（没有堆 ✗）

| 区域 | 什么进这儿 | 什么时候死 | 谁管 |
|---|---|---|---|
| **栈** | 局部变量、**定长**数组/结构体、参数 | 出作用域 ✓ | C 编译器（**零成本** ✓ 能用寄存器 ✓）|
| **arena** | **`new` 的**（运行时大小 / 要跨作用域）| **它那只 arena 出作用域** ✓ | 编译器（**隐式**、用户无感 ✓）|
| **静态** | 全局 / 常量（深度 0）| 程序结束 ✓ | 链接器 / OS ✓ |

> ⭐ **arena = 词法作用域**（每个 `{}` 一只，见 [`ARENA.md`](ARENA.md)）✓
> 而**没有堆**：既没有 `malloc`/`free`，也没有 GC ✓
>
> ✅ **2026-09-20 落地**（PLAN A2）：arena 真的按**块**细化了 ——
> 生成的 C 是 `extc_arena __extc_a[DEPTH] = {0}`（**层数在编译期就算好**，栈上定长），
> 进块 reset、出块 release，`break`/`continue`/`return`/`?` 各自释放该退的层 ✓
> ⇒ **循环里分配的内存上界 = 一次迭代**（以前是"整个函数体"，实测 300×1MB 会爆）✓
>
> ⭐ 这里有一条**接缝**，它是整个设计里最值得记的一处：
> **检查器判引用用的"块深度"和生成的 C 选 `__extc_a[k]` 用的"块深度"必须是同一个数** ✓
> 所以 `alloc<T>(n)` 的引用深度是**当前块深度**（不是写死的 1）——
> 不然 `while { p = alloc<i32>(1) }` 里的 `p` 会在下一轮指向**已释放**的内存 ✗

⇒ 三条白送的好处：
**不需要 GC**（东西有确定的死期 ✓）· **不需要 `free`**（没有单独释放 ✓）·
**不需要析构函数**（没有"对象死掉的时刻" ✓）

⚠️ **两个实用注意**：
1. **栈上放不下大东西** —— `var big: [10000001]bool` 是 **10 MB**，
   超过默认栈（8 MB）⇒ 大数组要放**全局**（`var big: [10000001]bool` 在顶层 ⇒ 静态区 ✓）
   或等 `new`（arena ✓）✓
2. **arena 的垃圾要等到作用域结束**才回收（§4.6 / `ARENA.md` §4 ✓）—— 不是不安全，是浪费 ✓

### 4.2 唯一引用规则：词法深度

> **若引用 `r` 指向值 `v`，则 `depth(r) ≥ depth(v)`。人话：引用不能活得比被指对象长。**

深度是**纯词法**的：参数 = 0，函数体 = 1，每进一层块 +1。

**这是 extC 里最难的那半句的落地方式**：比两个整数就完了，**不写生命周期标注** ✓

支撑它的引理只有一条：**arena 是词法的** ⇒ `depth(x) ≤ depth(y) ⟹ lifetime(x) ⊇ lifetime(y)` ✓

### 4.3 今天真的查的四条

| # | 查什么 | 位置 |
|---|---|---|
| ① | **返回**的引用不能指向本帧（要求深度 0）| `return` |
| ② | **初始化**的引用不能指向更深的局部 | `let` / `var` |
| ③ | **赋值**进字段 / 元素时 | `x.f = v` |
| ④ | **借来的值不能存进深度 0 的地方**（全局）| `G = s` |

加上一条边界规则：**函数调用返回的引用，深度 = `max(所有实参)`**
（被调函数能返回的引用只有两种来源：全局/静态，或者它的实参 —— 所以这是**精确上界**）✓

### 4.4 已知的**精度缺口**（承认为保守，不是错）

检查器看的是「**名字的存储深度**」，不是「**里面的引用指哪**」。于是：

```extc
fn g(m: slice<u8>) -> slice<u8> { let v = m  return v }   // ✗ 被挡（其实是安全的）
```

🔸 **已定修法**：把 `Sym.depth`（存储，`ref v` 用）和 `Sym.refDepth`（里面的引用）**分开**。
⚠️ 必须同时保证 `ref v` 仍然用存储深度 —— 否则会引入**真的悬垂**（返回指向死掉栈槽的指针）。

### 4.5 已知的**能力缺口**（设计上接受的损失）

```extc
// ⚠️ 假设的接口形状（**我们没有选它**）：`r.line(...) -> result<slice<u8>, ioError>`
//    返回的是 `slice<u8>` —— 一个**值**，里面装着 `{ data: ref u8, len: i64 }`
let a = r.line(buf)?      // a 的 data 指向**阅读器内部**那块缓冲
let b = r.line(buf)?      // 第二次调用把同一块缓冲覆盖了
use(a)                    // ← 编译期查不出来：a 和 b 是同层的值，深度一样
```

**注意返回的是 `slice<u8>`，不是 `ref slice<u8>`** —— 视图是**值**（16 字节：指针 + 长度），
它**内部装着**一个 `ref`。所以「两个视图指同一块字节」在编译器眼里毫无异常，
**不是有人共享了 `ref`，是两个值的 `data` 字段恰好相同** ✓ 这才是别名意外的根。

⚠️ 两点要说清楚：

**① 这段是假设的 IO 形状**（`reader` 还没实现），用来说明检查器的**能力边界**，
不是今天能写出来的代码。

**② 它不是悬垂，是"内容被覆盖"。** 内存还活着，只是你手上那个视图指的地方
**被第二次读覆盖了**。差别很重要：悬垂会崩（或者更糟），这个只是**悄悄给你旧数据**。

| | 抓得住吗 |
|---|---|
| **Rust** | ✅ 借用检查器：`a` 借了 `buf`，第二次调用又要 `&mut buf` ⇒ 直接拒绝 |
| **extC** | ❌ **不写生命周期 ⇒ 抓不住** |

**缓解办法不是"检查"，是"让它在源码里看得见"** —— 这正是 IO 接口定成
「**调用者给 buffer + 返回长度**」的原因：

```extc
let n = readLine(r, mut buf[..])?   // 只给长度
let line = buf[0..n]                // 你自己切 —— 每次都在源码里写出来
```

同一个 `buf` 出现在两行里，**覆盖是写在源码里的**；
而如果接口返回"阅读器内部缓冲的视图"，读者**根本无从知道**第二次调用会毁掉第一次的结果 ✓
（真要防死：把内容**拷**出来，或者每次读进**不同的** buffer。）

> 这不是"还没做"，是那句定位的**必然代价**。
> 代价怎么付？靠 P′：**让"失效"在语法上看得见** ——
> 所以 IO 的读接口定成「**调用者给 buffer + 返回长度**」，
> 而不是"返回一个下次调用就失效的视图"。

**一句话说清 extC 的保证边界**：

> **extC 保证「没有悬垂」，不保证「没有别名意外」。**

### 4.6 arena 的代价

| 代价 | 说明 |
|---|---|
| 帧内增长留垃圾 | 反复 `push` 会留旧 buffer 到函数返回。**不是不安全**，是浪费 |
| 帧 = 释放点 | 想更早释放要么拆函数、要么以后加块级 mark/release |

---

## 5. 引用语义

### 5.1 两种引用

| 写 | 能读 | 能写 |
|---|---|---|
| `ref T` | ✅ | ✗ |
| `mut ref T` | ✅ | ✅ |

**可写性从来源继承**，只有三个来源：**绑定**（`var` 可写 / `let` 只读）、
**参数**（类型里写着）、**字段**（看它的根）。

#### 规矩：**只许去掉 `mut`，不许加**（而且**每一层**都适用）

```extc
fn total(g: slice<slice<i32>>) -> i64 { ... }        // 只要读
var rows: [2]mut slice<i32> = [a[0][..], a[1][..]]
total(rows[..])          // ✓ mut slice<mut slice<i32>> → slice<slice<i32>>
```

| 方向 | 许不许 |
|---|---|
| 可写 → 只读（任何一层）| ✅ 递归降级 |
| 只读 → 可写（任何一层）| ⛔ 那是在**要**权限 |

**为什么去掉永远安全**：**`mut` 是权限，不是布局**。C 里
`slice<mut slice<i32>>` 和 `slice<slice<i32>>` 是**同一个结构体**（都叫 `slice_slice_i32`）
⇒ 少一份权限不改变任何字节 ✓
（实现上 `ttEquals` 把 `mut` 当身份、`ttViewDowngradable` 把 `mut` 当权限 —— **两件事**，
所以 codegen 还要**按 C 名字对实例去重**，否则同一个结构体会被生成两份 ✓）

### 5.2 值位置自动解引用 —— 拿到的是**拷贝**

```extc
var n: i32 = 7
let r = ref n
var v = r                 // ← 值位置 ⇒ 解引用 ⇒ v 是**值的拷贝**
v = 9
println(n)                // 7   ← n 没变 ✓
```

| 写法 | 结果 |
|---|---|
| `let v = r` / `var v = r` / `f(r)` / `return r` | **值**（拷贝）✓ |
| **`let g: ref i32 = r`**（标了类型）| **引用本身**（别名）✓ |
| `ref x` | 取地址，**不解引用** |

**一句话**：**要值就直接用，要引用就把类型写出来。**

### 5.3 赋值：两件事靠**右边的类型**消歧

| 目标 | 右边是**值** `T` | 右边是**引用** `ref T` |
|---|---|---|
| `ref T` 绑定 / 字段 | **写进它指向的地方** | **换指向** |

### 5.4 资源（文件）也归帧

`open()` 返回 `mut ref file`（指向**帧 arena 里的槽位** ⇒ 深度 1）⇒
**跟 `alloc` 是同一条规则**、逃逸检查**一行都不用改**：

| 想干的事 | 谁拦住 |
|---|---|
| 把本帧开的文件返回出去 | 深度 1 > 0 ⇒ 编译错误 ✓ |
| `fn fwd(f: mut ref file) -> mut ref file { return f }` | ✅ 合法（转发是好的）|
| `return fwd(open("x")?)`（洗完白再返回）| `max(实参)` 规则 ⇒ 编译错误 ✓ |

---

## 6. 错误处理

### 6.1 失败是**类型**的一部分

```extc
fn place(...) -> result<unit, gameError> {
    if 有子 { return failure(gameError.occupied) }
    return success(unit {})
}
```

**没有哨兵值**（`-1` 分不清"失败"和"值就是 -1"）、**没有 `errno`**（隐藏的全局状态）、
**没有异常**（控制流不在语法里）✓

### 6.2 `?`：失败就顺着往上抛

编译期展开成三句语句（求值一次 + 判断 + return），**没有栈展开** ✓
四个合法位置：`f()?` / `let x = e?` / `x = e?` / `return e?`
（都是"整个语句能重写"的地方 —— C 没有语句表达式，这是硬约束）

### 6.3 `match`：穷尽检查

```extc
match e {
    outOfRange => { return "出界了" }
    occupied   => { return "那格已经有子了" }
}
```

**漏一个变体就编不过**。用 `if e == x { } else { }` 时，以后给枚举加了新变体，
编译器**不会提醒你漏了** —— `match` 会 ✓

⚠️ `match` 是**语句**，不是表达式（同 `?` 的理由：C 没有语句表达式）。
每条分支体是块或单个语句；被 match 的表达式只求值一次（生成的 C 就是 `switch`）。

---

## 7. 多态与分派

### 7.1 泛型是**单态化**的

每个实参组合生成一份代码 ⇒ **没有间接调用、没有虚表、没有运行时类型信息** ✓
代价：代码体积（可接受，因为用户自己写的也是这个规模）。

### 7.2 协议，不是 trait

**语言认识协议，库提供结构。**

| 协议 | 语言认识什么 | 库提供什么 |
|---|---|---|
| `==` | 遇到 `a == b` 去找类型上的 `fn ==` | 类型自己写 |
| 视图 | `data` + `len` 两个字段 | `slice<T>` 在 prelude 里 |
| `?` | 类型叫 `result`，变体叫 `success` / `failure` | `result<T,E>` 在 prelude 里 |
| 打印 | 每个类型都能打印（编译器派生 `_debug`）| —— |

没有 trait、没有接口继承、没有 `impl` 块、没有**开放**的扩展点 ✓

### 7.3 没有函数指针 —— 用**枚举 + match** 代替

这是设计里最容易被误解的一条，要说清**边界**：

| 情况 | 能不能做 |
|---|---|
| **闭集**的事件 / 状态 / 命令 / AST 节点 | ✅ **能，而且比 C 好**（穷尽检查 + 无间接调用）|
| **开集**：运行期注册插件 / 热替换 / 用户自定义回调 | ⛔ 永久不做（需要间接调用 ⇒ 违反 P）|
| 多进程模块（字节协议）| ⬜ **设计上可以**：`@main` 多入口（定案 6）⇒ 多个可执行文件 ⇒ 管道通信（**还没实现**）|

> 「没有闭包 ⇒ 不能做事件驱动」是**错的**。正确说法：
> **闭集事件用 `match`（更好），开放注册不做（换来的是"每个调用在哪都能静态找到"）。**

---

## 8. 边界：跟外部世界

### 8.1 三档机制

| 档 | 是什么 | 用户看得见 C 吗 |
|---|---|---|
| **1 · 原语** | 编译器生成（文件 IO、`readLine`、arena、trap）| 看不见，当普通函数用 |
| **2 · stdlib wrapper** | stdlib 里用 `extern` 包好，`reserved` 保护 | 看不见，而且**改不了** |
| **3 · 用户自建** | `extern!` 边界声明 | **看得见，而且必须签字** |

⚠️ 一旦走到第 3 档，"安全"就从**绝对**变成**默认**（`extern!` 之外仍然安全）✓

### 8.2 沙箱：进程是那个便宜的沙箱

子进程 + stdio 上的字节协议 + `rlimit` / `seccomp` ⇒
**操作系统给的地址空间隔离**，比语言里任何沙箱都便宜、都硬 ✓

### 8.3 ISO C 的取舍判据

**问「它的价值是不是来自 C 的某个缺失」** —— 是，就不值得包。

| 值得留 | 不值得包 |
|---|---|
| `mem*` 家族（价值来自 SIMD 内联）、`libm`、`zlib`、平台接口 | `str*` 家族（就是"C 缺的东西"）、`errno`、`scanf`、`qsort`（要函数指针）、`rand`（质量差 + 全局状态）、`assert`（归编译器）、`limits`（属于语言）|

### 8.4 IO 的分层（详见 `IO.md`）

> **读 = 整块读 + 内存里切；写 = 内存里拼 + 整块写。**
> 编译器只给 4 个裸原语，`result` / `slice` / `reader` 全用 extC 写 ✓

- 用 POSIX `read` / `write`，**不用 `stdio`**（`FILE*` 是隐藏分配 + 隐藏缓冲）
- 错误码**走返回值**，不读 `errno`（那是隐藏的全局状态）
- **不提供逐字节读** —— 那就是 `cin` 的坑（一次 `read` ≈ 0.5–1 µs，内存扫一个字节 ≈ 0.3 ns）
- 行缓冲**由调用者给**（字节总得落在某处：你给 = 看得见）

---

## 9. ⛔ 明确不做的（每条一句话）

| 不做 | 因为 |
|---|---|
| GC / 引用计数 | 隐藏机制；arena 已经解决了内存回收 |
| 异常 / 栈展开 | 控制流不在语法里 |
| 裸指针 / 指针算术 | 悬垂与越界的源头；用 `ref` / 视图 / 索引 |
| 继承 / 虚函数 | 单态化 + 枚举 + `match` 覆盖了需求，且没有间接调用 |
| 手动 `free` / `close` | 没有手动分配就没有手动释放 |
| `errno` / `static` 局部变量 / 任何隐藏全局状态 | 行为取决于你没写的一行 |
| 运行期换实现（插件表 / 策略容器 / 热替换）| 需要间接调用 ⇒ 违反 P。**永久损失** |
| `as` 强转 / 隐式截断 | 有损转换必须写出来 |
| 字符串是语言内建类型 | 字符串就是 `slice<u8>`，在 prelude 里 ✓ |
| 运算符重载（除 `==`）| 显式优于糖 |

---

## 10. 代价与损失（诚实清单）

| 损失 | 说明 | 能不能补 |
|---|---|---|
| **同帧别名失效查不出** | 见 §4.5 | ⛔ 定位的必然代价，用语法显式化来缓解 |
| **运行期换实现** | 插件 / 热替换 | ⛔ 永久（违反 P）|
| **存储深度 vs 引用深度的保守** | `let v = m; return v` 被误挡 | 🔸 已定修法（§4.4）|
| **arena 帧内留垃圾** | 长循环反复增长 | ✅ 可加 `reserve()`；不是不安全 |
| **容器拷贝 = 共享存储** | 拷贝一个容器，两份指向同一块 | ✅ 写文档 / 提供显式 `clone` |
| **没有生态** | 没有包管理器、没有第三方库 | 🔸 靠 `extern!` 接 C |
| **`alloc<T>(n)` 今天不可用** | 返回 `mut ref T`，不能索引也不能切片 | 🔸 真缺口，要补 `allocSlice<T>(n) -> mut slice<T>` |
| **学习曲线** | 要理解"深度"和"帧拥有" | ✅ 靠这份文档 + `MANUAL.md` |

---

## 11. 验收靶子

| # | 靶子 | 为什么是它 | 状态 |
|---|---|---|---|
| **1** | **五子棋**（真实、近、有验收）| 主人本来就要做的东西；能立刻暴露"能不能写真实程序" | ✅ 棋盘 / 判定 / `result` / `match` 都能写；差**输入**和**输出缓冲** |
| **2** | **自举** | 编译器自己是最大的 extC 程序 | 🔸 降级为可选的验证手段 |
| **3** | ⭐ **带生命周期的小程序**：源码 → token 视图 → arena 里的 AST → 求值 | **五子棋测不出 extC 的核心主张**（它只用定长数组 + 循环 + 递归，任何语言都有）| ⬜ 这才是真正的靶子 |

> 「五子棋不需要 arena」**不能**当作把 arena 排最后的理由 —— **依赖顺序 ≠ 优先级** ✓

---

## 12. 现在在哪（2026-09-18）

### 12.1 ✅ 已实现

| 能力 | 备注 |
|---|---|
| 词法 / 语法 / 类型检查 / 生成 C / `#line` 映射 | 编译器本体 |
| 值语义、定长数组、视图（`slice<T>` / `mut slice<T>`）| 含可写视图 |
| 引用 `ref` / `mut ref`、值位置自动解引用、换指向 | 完整语义 |
| **逃逸检查**：返回 / 初始化 / 赋值 / 借来的值 | §4.3 的四条 |
| arena 运行时 | 每帧一只；`alloc<T>(n)` 原语 |
| `let` / `var`、同层 `let` 遮蔽 | `var` 不能遮蔽同层 |
| 泛型（struct / 函数 / **枚举**）、关联函数 | 单态化 |
| `==` 协议、视图协议 | |
| `option` / `result` / `?` | 今天还是"struct + bool 标签"（见下）|
| **枚举**：无载荷、**带载荷（tagged union）**、**`match` + 穷尽检查** | 第 3 步第一、二刀 |
| 全局常量 / 变量、方法、`unit` | |
| 110+ 测试（正例能跑 + 反例必须编不过）| 116 |

### 12.2 欠账（按性质分三类）

| 类别 | 内容 |
|---|---|
| **语义债**（会让合法代码编不过 / 非法代码编过）| ① `Sym.refDepth` 拆分（§4.4）② `allocSlice<T>` 缺口（§10）③ `option<slice<u8>>` 编不过（等第三刀）|
| **语言债**（缺特性）| 第三刀：把 `option`/`result` 重写成普通枚举 + `?` 跟上 · `varArray<T>` · lambda · 输入与 argv · `for` |
| **算术 UB 债** | 除零崩了没位置 · 移位超宽静默错（溢出已用 `-fwrapv` 兜住）|

### 12.3 路线（BOOTSTRAP §8 的六步）

| 步 | 内容 | 状态 |
|---|---|---|
| 1 | 修 soundness 洞（调用深度 = `max(实参)`、借来的值不进深度 0）| ✅ |
| 2 | 同层 `let` 遮蔽 + 生成 C 改名（顺带修 C 关键字撞名）| ✅ |
| 3 | `match` + 带载荷枚举 + 重写 `option`/`result` | ✅ **已落地**（第三刀 = 定案 52′，2026-09-20）|
| 4 | `arena` 内建类型 + `arenaOf()` / `allocFrom()` | ✅ **换了个形状落地**（2026-09-20）：按块细化（定案 55）+ `new` 永远是零（定案 56）+ 块级提升（定案 63）√ ⚠️ `arenaOf()`/`allocFrom()` **没做、也不打算做** —— 那是「显式指定 arena」的思路，已被「编译期推导 + 提升」（`ARENA-FORMAL`）取代 ✗（实测 `arenaOf` 不存在 ✓；而 `./tests/arena/run.sh` 全过、循环里 300MB 不涨 ✓）|
| 5 | `varArray<T>`（prelude，extC 写）| ✅ **已落地**（prelude 里 18 处 `varArray` ✓）|
| 6 | lambda（传 / `let` 存 / 捕获，身份编译期唯一）| ⬜ **仍未做** ✓（六步里唯一真没做的）|

---

## 13. 未决问题

| # | 问题 | 摆着的选项 |
|---|---|---|
| 1 | ~~IO 里长度的类型~~ | ✅ **定了 `i64`**（定案 69，2026-09-22）—— 跟 `slice.len`/索引同类型，负数有意义（失败/EOF）✓ |
| 2 | `scan(mut a, mut b, ...)` | 要，但走**真变参**（大功能），明确推迟 |
| 3 | ~~块 `{ }` 要不要变成资源释放点~~ | ✅ **已经是了**（定案 55，2026-09-20：按块细化）—— 进块 reset、出块 release ✓（原文那句「现在是帧 = 释放点」是**改之前**的口径 ✗）|
| 4 | ~~有损转换的写法~~ | ✅ **定了 `T(x)`**（PLAN #23，2026-09-20）—— 整数收窄/换符号/浮点转整数装不下就 trap 带位置 ✓（语法选 `T(x)` 而非 C 的 `(T)x`：parser 不查符号表，`(T)x` 与括号表达式二义 ✗）|
| 5 | `allocSlice<T>` 还是别的名字 | 见 §10 |

---

## 附：文档地图

| 文档 | 回答什么 |
|---|---|
| **`LANGUAGE.md`（本文）** | **extC 是什么、为什么** |
| [`MANUAL.md`](MANUAL.md) | 怎么写 extC 程序（**只写已实现的**）|
| [`DESIGN.md`](DESIGN.md) | 原则的完整推论（P / P′ / P″ 的展开）|
| [`DECISIONS.md`](DECISIONS.md) | 哪条定了（编号定案）、哪条还欠着 |
| [`BOOTSTRAP.md`](BOOTSTRAP.md) | 自举路线、六步计划、ISO C 边界、沙箱 |
| [`IO.md`](IO.md) | 输入输出的设计（未实现）|
| [`MODULES.md`](MODULES.md) | **模块系统的调研 + 候选方案**（IO 的前置条件 —— 12 门语言怎么做、六个待拍板问题）|
| [`LIBS.md`](LIBS.md) | **库的形态**（源码分发 / 给 C 用 / `extern!` 信任声明 / 所有权映射）|
| [`REFS.md`](REFS.md) / [`ARRAYS.md`](ARRAYS.md) | 引用 / 数组的专题设计 |
| [`SYNTAX.md`](SYNTAX.md) | 命名规范与样板 |
| [`DEVLOG.md`](DEVLOG.md) | 发生过什么（决策与发现的来龙去脉）|
| [`VISION.md`](VISION.md) | 早期的一份合并版（**已被本文取代**）|
