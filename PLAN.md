# PLAN.md —— 还剩什么（唯一路线图）

> **2026-09-18 重写。** 以后再问「还有什么要做」，只看这一份就够了。
>
> 现状：**150 个测试全绿** · 正式设计文档在 [`LANGUAGE.md`](LANGUAGE.md) ·
> 原则与承诺在 [`LANGUAGE.md`](LANGUAGE.md) §0.5 / §1

---

## 0. 先说三句话

1. **没有什么是"必须现在做"的。** 语言今天**已经能写纯计算程序**（算法、棋盘、解析、状态机、
   协议分发）—— 剩下的几乎全是**加法** ✓
2. **设计阶段基本结束了。** 剩下的活**不需要新的原则**，是"力气活" ✓
3. **只有一条主线的里程碑**：让**五子棋引擎能跟人下**（那是项目一开始瞄的靶子）。

---

## 0.4 🐞 **缺陷清单（按优先级，2026-09-20 汇总）**

> 这是"**坏了 / 该响亮却没响亮**"的清单 —— 跟"新功能"分开。
> ⭐ 注意 **#1 和 #7 是同一处代码**（`typeContainsRef` 的枚举分支），一次修两个 ✓

| # | 缺陷 | 现象 | 级别 | 大小 |
|---|---|---|---|---|
| ~~**1**~~ ✅ | **枚举载荷的逃逸检查有洞**（**2026-09-20 修**，见 §0.5） | `fn make() -> box { … return box.holding(local[..]) }` **编过** ⇒ 悬垂 / **UB**。**写链表必踩**（造完返回）| **唯一能拿 UB 的路** | 中（见 §0.5）|
| ~~**2**~~ ✅ | **同一函数里两个 `match` 绑同名** | **2026-09-20 修**：check 时把 `cname` 写回 `MatchArm.binds`（跟 `Param.cname` 同一套路）✓ | — | 小 |
| **3** | 🐛 **`alloc<[4]i32>(1)` 之后索引** | extC **放行**，生成的 C 编不过（gcc：*'p' is a pointer; did you mean '->'?*）| 真 bug（不是安全洞）| 小 |
| **4** | ⚠️ **字面量越界不报编译错** | `b[10]`（`b: [4]i32`）能编过，运行时才 trap ⇒ **能证明的没在编译期报** | P′ | 小 |
| **5** | ⚠️ **无限递归无声崩溃** | 退出码 255、**没有消息、没有位置**。设计里本来有 `@recursive!`（签字）这条 | P′ | 小~中 |
| **6** | ⚠️ **arena 没内存时没位置** | `extc: out of arena memory` + `exit(1)`，没有源码位置 | P′ | 小 |
| ~~**7**~~ ✅ | **`option<slice<u8>>` 报错落在 prelude 里** | **2026-09-20 第三刀封掉**：`option` 成了枚举，tag 0 = `none` **不带载荷** ⇒ 里面有 ref 也有零值 ✓（那个"必须报错"的反例变成了正例 `examples/option-ref-payload.extc`）| — | 小 |
| **8** | 🟡 **`let v = m; return v` 被误挡**（**一半已修，2026-09-20**）| ✅ 现在**引用型绑定**单独跟踪「被指对象深度」（`Sym.refDepth`）⇒ `var cur: ?ref node = head … return cur` **合法** ✓（这就是最正常的链表搜索）。⚠️ **剩下**：**视图**（`slice<T>`）和**含引用的值**绑定还是拿槽位深度 ⇒ `var s = a[..]  return s` 仍被误挡 | 精度 | 中（动逃逸检查核心）|
| **9** | 📌 **`alloc<T>(n)` 当数组用不了** | 返回 `mut ref T`，不能索引/切片（单对象可以，当 buffer 不行）⇒ 要 `allocSlice<T>(n)` | 缺口 | 小 |
| **10** | 🟡 **裸写构造器**（**一半已修**）| ✅ `some`/`none`/`success`/`failure` 现在在**类型已知的四个位置**都认（return / 带标注的 var / 赋值 / 实参）。⚠️ **剩下**：用户自己的枚举变体（`just(x)`）还不认裸写 ⇒ 要写全 `maybe<i32>::just(x)` | UX | 小 |
| **11** | 📌 **没有 `-O` / `-march` 开关** | 驱动写死 `-O2`（`-march=native` 能让矩阵乘再翻倍）| UX | 小 |
| ~~**13**~~ ✅ | **显式解引用 `*p`** | **2026-09-20 完成** ✓ 隐式解引用已删除（只留"输出自动解引用"和"成员选择导航"两个例外）| — | — |
| ~~**17**~~ ✅ | 🚨 **泛型体里的引用规则整片被跳过**（**2026-09-20 修**；跟 §0.5 那个同族）| 泛型体是照**模板**查一次，那时 `T` 不透明，而 `typeContainsRef(TY_PARAM)` 返回 **false** ⇒ 所有"含引用才查"的规则（借来的值 / 深度）**全部早退** ✗ 实测：`struct boxT<T> { v: T  fn stash(self: mut ref boxT<T>, value: T) { self.v = value } }` 用 `boxT<slice<u8>>` 实例化后 `bt.stash(deeper[..])` 把指向更深局部的视图洗进活得更久的 `bt` ⇒ **悬垂**（具体类型版会被 "cannot store a borrowed value" 挡住 ✓）| **能拿 UB**（跟 #1 同级）| 中～大 |
| ~~**18**~~ ✅ | **泛型实例化时复查体**（**2026-09-20 做完**，跟 #17 一起）| 现在有三类走"推迟到实例化"：`==`（eqChecks）、**引用规矩**（深度/借用）、**零值**（refChecks）。模式统一：模板期记一笔 + 记好数字 ⇒ 实例化时 `typeContainsRef(tsub(T))` 决定这条规矩适不适用 ⇒ 消息里点名实例 ✓ ⚠️ **剩下的**：更多检查将来可能也要按实例复查（比如 `==` 之外的运算符协议、可打印性）—— 模式已经有了，加一类就是加一个 `recordXxx` ✓ | — | — |
| ~~**16**~~ ✅ | **枚举不能带方法 + 没有泛型自由函数** | **2026-09-20 用语法糖绕过去了**：`e ?? 兜底`（丙之后又加了乙）和 `e!` 就是 `valueOr` / `unwrap`，而且是**编译器**给的、**对所有 T 通用** ⇒ **不用**给枚举加方法、也**不用**泛型自由函数 ✓（"读值"这条路彻底通了）| — | — |
| ~~**20**~~ ✅ | 🐛 **数组里装带载荷枚举 ⇒ 生成的 C 编不过** | **2026-09-20 撞出来并修掉**：`var slots: [4]?i64` 会派生一个数组的 `_eq` helper，而 `typeHasEq` 把**所有**枚举都当成"有 `==`" —— 可带载荷枚举在 C 里是 `struct { tag; union }`，**没有 `==`** ⇒ gcc 报 `invalid operands to binary ==` ✗（类型检查那边本来就禁止比它，是 codegen 没跟它一致）。修法：`typeHasEq` 对 `TY_ENUM` 要求 `!enumHasPayload` ✓ ⚠️ 这是第三刀改类型表示的**后遗症** —— 换表示之后要把"按旧表示写死的判断"全扫一遍 |
| **21** | ✅ **`??` 的三条安全网** | ① 主体含调用 ⇒ **先算进临时变量**（#19 做完之前是报错；两种都不会让它跑两次 ✓）② **兜底那侧的深度也算**（`m ?? ref 局部` 逃逸被挡 ✓ 实测）③ 兜底那侧按结果类型**显式转换**（C 的通常算术转换会悄悄拓宽：f32 载荷配 double 字面量就是典型）| — | — |
| ~~**19**~~ ✅ | **`??` 的主体可以是调用**（**2026-09-20 做完**）| 加了通用的"**语句前缀**"机制：主体不是纯的 ⇒ 检查器打 `needTemp` 标记，codegen 在**所在语句之前**吐 `option_i64 __extc_c0 = f();`，三元写在这个变量上 ⇒ **f() 只跑一次** ✓ 主体本来是变量时**不生成临时变量**（还是纯三元）✓ 吐不出来的位置由检查器报错：**只剩 `while` 条件**（吐出来只算一次 ✗ 语义就错）—— ⚠️ 全局初始化式**不属于这条**：它本来就要求"必须是常量"，报那条更根本的原因 ✓ | — | 中 |
| **22** | 📌 **`??` 的临时变量会先算** | 需要临时变量时前缀吐在语句最前面 ⇒ `g() + (h() ?? 0)` 里 `h()` **先于** `g()` 跑（源码顺序是反的）。extC 跟 C 一样**不承诺**语句内求值顺序，所以不算毁约，但**可以收紧**：同一条语句里主体之前还有带副作用的子表达式 ⇒ 报错让他拆行 | 语义清晰度 | 中 |
| **15** | 🐛 **`match` 分支里的 `break` 跳出的是 `switch`** | **2026-09-20 修**：match 的 codegen 改成 if/else 链（`while true` + `break` 的读行循环以前**当场死循环**）✓ | — | 小 |
| **14** | 📌 **收窄不认路径** | `p.next.value` 不能写（收窄只认绑定名）⇒ 要 `var nxt = p.next` 再 `if nxt != null`。想支持路径收窄就得处理别名失效，性价比不高 | UX | 小（先记账）|
| ~~**15**~~ ✅ | **`?ref T` / `null` / 收窄** | **2026-09-20 完成** ✓ 见 MANUAL §7.6 | — | — |
| **13old** | 📌 **没有"显式解引用"（旧记录）** | 解引用**只有隐式**（值位置自动解）⇒ 最别扭的是**赋值**：`p = q` 是**换指向**，而"把 q 指的值写进 p 指的地方"**没有直接写法** ✗ 只能 `let t = q  p = t`（写 `swap` 时就是这么绕的）。**跟「显式是对的」这条原则冲突** ⇒ 建议补 **`*p`**（`ref x` 的对偶；`*` 的前缀位置在 extC 里是空的 ✓）—— 主人 2026-09-20 提出 |
| **12** | 📌 **文档债** | `;` 和 `/* */` **其实早就可用**，但没写进 MANUAL；块注释**不嵌套**也没说 | 文档 | 小 |

**建议的开工顺序**：**1 → 2 → 3**（1 挡着"能安全写链表"，2/3 是真 bug 且都小），
然后 **4–7**（都是"让失败响亮"，一鼓作气），最后 8 → 9 → IO ✓

---

## 0.5 ✅ **soundness 洞已修**（2026-09-20，A0）—— 原记录见下

**修法**：把 `typeContainsRef` 拆成**两个问题** ——
| 问题 | 看哪儿 | 谁用 |
|---|---|---|
| **能不能携带引用** | **所有**变体 / 所有字段（`typeContainsRef`）| `exprRefDepth` / `exprBorrowed` / 全局检查 |
| **有没有零值** | 带载荷枚举**只看 tag 0**（`typeLacksZeroValue`）| `var x: T` 无初始化式 · 结构体字面量省略的字段 |

**验收**：4 个攻击形状（非泛型 / 泛型 / 载荷是结构体 / 经过转发）**全部报错** ✓
正例（载荷来自参数）仍合法 ✓ 零值语义没被破坏（`| nothing | holding(slice<u8>)` 的 `var` 合法、
反序的报错）✓ **127 测试全绿** ✓

⚠️ **`option<slice<u8>>` 那条（#7）没被这次修好** —— 它走的是**另一条路**：
泛型方法在**实例化时的复查**没有覆盖到零值检查 ⇒ 仍然报
`__extc_reference_has_no_zero_value__`（落在 **prelude 的行**上）✗ 见 §0.4 #7 ✓

---

## 0.5old 🚨 原来的记录（保留，因为根因分析还在用）

```extc
type box = | empty | holding(slice<u8>)      // ← 注意：无载荷的变体排在**第一**

fn make() -> box {
    var local: [8]u8 = [65, 66, 67, 68, 0, 0, 0, 0]
    return box.holding(local[..])            // ⚠️ **编译通过**（应该报错）
}
```

**返回的载荷指向一个已经死掉的栈帧** ⇒ **悬垂 / UB** ——
这是目前**唯一已知的、能从 extC 源码拿到 UB 的路**。

**根因**：`typeContainsRef` 的枚举分支**只看 tag 0 的载荷**（因为那条规则是给"零值"用的），
于是 `exprRefDepth` 看到 `box` 就早退返回 0 ⇒ **载荷的深度从来没算过** ✗
（泛型枚举 `maybe<T>` 同样中招。）

**修法**（已经想好，`DECISIONS.md` ⑱ 里记过）：
把 `typeContainsRef` 拆成**两个问题** ——
| 问题 | 看哪儿 | 谁用 |
|---|---|---|
| **这个类型有没有零值** | 只看 **tag 0** | 零初始化检查 |
| **这个类型能不能携带引用** | 看**所有**变体 | `exprRefDepth` / `exprBorrowed` / 全局检查 |

⚠️ **修之前不要用它**（这是唯一能拿到 UB 的路）。

---

## 1. ⭐ 主线：让它**能读东西**（做完 = 五子棋能跟人下）

| # | 做什么 | 为什么挡着 | 大小 |
|---|---|---|---|
| **1** | **IO-0**：原语 `rawRead`/`rawWrite` + `readLine` / `readAll` / `nextInt` 一族 + `ioError` | **没有输入** ⇒ 只能写"自己跟自己玩"的程序。**定长数组就能当 buffer**（实测 `var buf: [256]u8` + `buf[..]` ✓）⇒ **它不挡 IO-0** | 中（设计已定，见 [`IO.md`](IO.md)）|
| **2** | **IO-1**：`open` + **帧拥有文件**（跟 arena 并排）+ `main(args)` | 读源文件 / 写生成的文件 ⇒ 自举与工具的门槛 | 中 |
| **3** | **`allocSlice<T>(n) -> mut slice<T>`**（帧 arena 里要 n 个 T，**清零**）| **长度运行时才知道**的 buffer：读未知大小的文件、`reader` 自动要 4KB、`varArray` 增长。也兑现 `LANGUAGE.md` §0.6 那条承诺（清零 ⇒ 过期读到的也是**确定的**字节）| 小 |

> ⚠️ **更正**（2026-09-18，主人追问"造不出 buffer 什么意思"时实测发现）：
> 我原来写「今天造不出 buffer」**说过头了**。
> **定长数组当 buffer 是能用的** —— `var buf: [256]u8` + `buf[..]` + 传进 `mut slice<u8>` 参数 ✓
> （顺带抓出并修掉一个真 bug：`struct reader { chunk: mut slice<u8> }` 报
> **incomplete type** —— 依赖排序按**指针**找实例，而可写视图是**影子** ⇒ 找不到 ⇒ 排错顺序。）
>
> **所以 IO-0 不被任何东西挡着，可以立刻做** ✓ 里程碑比原来估的更近。

---

## 2. 原则欠账（都是"让承诺成真"，不是新功能）

| # | 做什么 | 现在的问题 | 大小 |
|---|---|---|---|
| **4** | **`option<slice<u8>>` 报成检查器错误** | 现在报错落在 **prelude 里**（`__extc_reference_has_no_zero_value__`）⇒ 用户看不见自己的代码，**违反 P′** | 小 |
| **5** | **`Sym.refDepth` 拆分** | 检查器看「名字的存储深度」而不是「里面的引用指哪」⇒ `let v = m; return v` 被**误挡**（保守，不是错）。⚠️ 必须同时保证 `ref v` **仍然用存储深度**，否则引入真悬垂 | 中（动逃逸检查核心，要单独验）|
| **6** | **第三刀：`option`/`result` 重写成普通枚举** + `?` 跟上 + 迁 examples | 今天它们是"struct + bool 标签"⇒ **不能 `match`**、`option<slice<u8>>` 编不过。第二刀的前置（泛型枚举）已经做完了 | 中（迁移面大）|
| **7** | **字面量越界 ⇒ 编译期报错** | 现在 `b[10]`（`b: [4]i32`）**能编过**，走到运行时才 trap。**安全**（不静默踩内存）但不够早 —— MANUAL 里"能证明的编译期报错"那句**只对切片成立** ⚠️（主人问"万一爆了怎么办"时实测发现）| 小 |
| **8** | **arena 要不到内存时带位置** | 现在 `extc: out of arena memory` + `exit(1)`，**没有源码位置** ⇒ 违反 P′ 的"看得见" | 小 |
| **9** | **无限递归 ⇒ 无声崩溃** | 实测：**退出码 255、没有任何消息、没有位置**。设计里本来有 `@recursive!`（"我签字"）这条 ⇒ 该补：自递归要求签字 + 深度上限 + trap 带位置（主人 2026-09-18 攻击测试发现）| 小~中 |
| **10** | **`alloc` 的结果要能安全使用** | 实测三个问题：① `alloc<[4]i32>(1)` 后索引 ⇒ **extC 放行但生成的 C 编不过**（gcc 报 pointer/`->`）——**编译器 bug**；② `alloc<holder>(1)` 读里面的 ref ⇒ 这次 trap 住了，但**那是运气**（新 malloc 的页恰好是零）⇒ **未初始化内存读**是真潜在洞 ⇒ **`allocSlice` 必须清零**（就是第 3 项）| 小 |

---

## 3. 语言能力（"好用"的部分，全是加法）

| # | 做什么 | 什么时候需要 | 大小 |
|---|---|---|---|
| **7** | **`varArray<T>`**（prelude 用 extC 写）| 需要**动态**容器时（棋盘是定长的，所以不急）| 中 |
| **8** | **`for` 循环**（+ range）| 每次写 `var i`/`while` 三行的时候 | 小~中 |
| **9** | **lambda**（传 / `let` 存 / 捕获；身份编译期唯一）| 想给算法传"怎么比较"时（协议能顶一部分）| 中 |
| **10** | **协议补全**：`fn <`（排序）、hash | 写 `sort` / `HashMap` 时 | 小 |
| **11** | 模块 / 多文件 + `@main` 多入口 | 自举：`gomoku` + `gomoku-engine` 两个可执行文件 | 中 |
| **12** | **块 `{ }` 变释放点**（arena mark/release）| 长循环里反复分配时（现在只有"帧"一个释放点）| 小~中 |

---

## 4. 可选的（不做也不挡路）

| 项 | 说明 |
|---|---|
| **自举** | 已降级为**可选的验证手段**（主人：「我不一定完全自举，C 写语法解析底层效率很高」）|
| **TUI（termios raw mode）** | `IO.md` 里定过：不包 ncurses，只要"读一个字节 + 开关 raw 模式" |
| arena 帧内留垃圾 | 可加 `reserve()`；**不是不安全** |
| 容器拷贝 = 共享存储 | 写文档说明即可；**不是不安全** |
| `scan(mut a, mut b, ...)` 变参 | 主人要，但**明确推迟**（细节多）|

---

## 5. ⛔ 永远不做（不是"还没做"）

运行期换实现（插件 / 热替换）· GC · 异常 · 裸指针 · 继承 / 虚函数 ·
手动 `free` / `close` · `errno` / 隐藏全局状态 —— 见 [`LANGUAGE.md`](LANGUAGE.md) §9 ✓

---

## 6. ⭐ 主人定的顺序（2026-09-20）：**arena → IO → 后续**

⚠️ **一处必须点出来的耦合**：**那个 soundness 洞（§0.4 #1）就长在 arena 要用的那片代码上** ——
`typeContainsRef` / `exprRefDepth` 正是"逃逸提升"要动的分析 ✗
⇒ **修洞 = arena 的第一步**（不是额外的活），而且**不修的话，arena 的提升分析必然也是漏的** ✓

### A · arena（分五层，每层可验收）

| # | 做什么 | 为什么在这一层 |
|---|---|---|
| **A0** ✅ | **修 #1 那个洞**（**2026-09-20 完成** ✓，见 §0.5）| **arena 的逃逸分析必须长在同一片代码上**；不修则提升分析也漏 ✓ |
| **A0·补** ✅ | **顺手做完的**：`*p` 显式解引用 · `?T` 语法糖 · **`?ref T` + `null` + 非空收窄** · 引用型绑定的被指深度（#8 一半）| ⇒ **链表/树/搜索函数今天就能写** ✓ 只差跨函数接线（A1 `new` + A3 提升）|
| **A0·补 2** ✅ | **第三刀**：`option`/`result` 重写成**普通枚举** ⇒ **`option<slice<u8>>` 合法**（#7 关掉）+ match 的两个真 bug（#2 / #15）| ⇒ **IO 的先决条件满足了**：只差 A1 的缓冲区 `new i32[4096]` ✓|
| **A0old** | **修 #1 那个洞**：`typeContainsRef` 拆成「有没有零值」（看 tag 0）和「能不能携带引用」（看所有变体）| **arena 的逃逸分析必须长在同一片代码上**；不修则提升分析也漏 ✓ |
| **A1** | **`new` 形式**：`new node` / `new i32[1000]` / `new [4]i32`，**清零**，返回 `mut ref T` / `mut slice<T>` | 这是"**造 buffer**"的解锁（今天那个 `alloc<T>(n)` 拿不到手用）✓ |
| **A2** | **每个词法作用域一只 arena**：对象提到函数开头 + 进块 `reset` + 出块 `release` + 每个 `return` 按内→外释放全部 | 现在的实现是**每帧一只** ✗（设计与实现的差距）|
| **A3** | **逃逸提升**：分配流进返回值/出参/会逃逸的字段 ⇒ 提升到祖先那只 | **解锁跨函数链表/树/AST**（`fn build() -> mut ref node` 变合法）✓ |
| **A4** | 显式命名 + 显式指定 home（名字待定，主人说"不急"）| 逃生舱，最后做 ✓ |
| **A5** | 测试：跨函数链表 · AST 递归 · 循环不涨内存 · 攻击测试全绿 | 验收 ✓ |

### B · IO（设计已定在 [`IO.md`](IO.md)）

原语 `rawRead`/`rawWrite` + `readLine`/`readAll`/`nextInt` 一族 + `ioError` ——
**用 A1 的 `new i32[1000]` 当缓冲** ✓ ⇒ **五子棋能跟人下**（里程碑）✓

### C · 后续

`varArray<T>` → `for` → 函数值/lambda（⇒ 顺带解锁"安全的虚表" ✓）→ 协议补全 → 模块/`@main`

---

## 6旧. 原来的顺序（保留作参考）

```
allocSlice ──▶ IO-0 ──▶ (五子棋能跟人下 ✓ 里程碑)
                │
                ├──▶ IO-1（文件 + argv）
                │
                └──▶ 原则欠账 4 / 5 / 6（趁手热，都小）
                       │
                       └──▶ varArray ──▶ for ──▶ lambda（好用，不挡路）
```


---

## 7. 🅿️ 明确**先不动**的（主人拍过板：以后再说）

| 事项 | 现状 | 主人 2026-09-18 的话 |
|---|---|---|
| **块注释换成不烂大街的语法**（`#\| \|#` / `(* *)`）| `/* */` 能用但**不嵌套**（实测：内层 `*/` 会提前关掉注释）| 「这个问题没那么简单，涉及到我们会用什么符号表示什么含义/语法糖，再说吧，先不动」|
| **同一行多语句强制写 `;`** | `;` **已经能写**（`skipJunk` 吃掉它），只是不强制 ⇒ 例子都写成两个空格 | 「可以先不动，保留一些奇怪的特性」|

> ⚠️ 两条都**不是"忘了"**，是**有意保留** ✓

### 符号占用表（将来要动符号时从这张表开始）

| 符号 | 现在表示 | 备注 |
|---|---|---|
| `//` · `/* */` | 注释 | 块注释**不嵌套** |
| `:` | 类型标注 | `var x: i32` |
| `=` | 绑定 / 赋值 | |
| `->` | 返回类型 | `fn f() -> i32` |
| `=>` | `match` 分支 | |
| `::` | 关联函数 / 枚举构造 | `option<i64>::some` |
| `.` | 字段 / 方法 / 变体 | `b.moves` · `shape.circle(2.0)` |
| `..` · `...` | 切片区间 · 数组字面量剩余 | `a[1..3]` · `[1, 2, ...]` |
| `?` | **失败顺着往上抛** | 四个合法位置 |
| `!` | **我签字**（+ `!=`）| 唯一的逃生舱标记，**可数** |
| `@` | 属性 | `@recursive!` · `@main`（设计中）|
| `[ ]` `{ }` `( )` | 数组/索引/切片 · 块/结构体字面量 · 调用/分组 | |
| `< >` | 泛型实参 | 嵌套靠 `<` 配对，`>>` 不冲突（实测 ✓）|
| `ref` `mut` | 引用 / 可写限定词 | 关键字 |
| **`#`** | **完全没用** ⭐ | 除了生成的 C 里的 `#line`。**块注释候选** |
| `;` | 语句分隔（可用、不强制）| |
| `$` `` ` `` `\` `|>` `<|` `<-` | **没用** | 语法糖候选 |
