# REFS.md —— 引用（`ref`）现在在哪、该往哪走

> **起因**：主人做完 T5 之后说 ——
>
> > 「我感觉 week-4 的东西可能要提前了，现在 ref 这玩意儿太难受了」
>
> 本文把「难受」拆成**具体条目**，把 week-4 拆成**能分开的三块**，并给出建议顺序。
>
> ⚠️ **本文只摆方案，不替主人拍板。** 每个形状都给出「能跑的代码 + 代价 + 迁移影响」。

---

## 0. 为什么值得提前（三条理由，第一条是主人自己定的）

**① 主人的规矩本来就要求先做它。**

`PLAN.md` 开头：

> **能随时加的是「语法」（数组、`for`、`match`、模块）；
> 改起来贵的是「类型的形状」（泛型、`ref`、`result`/`option`、值语义）。**

`ref` 就在「改起来贵」的名单里，而且它现在的状态是**半成品**。
在它上面继续加东西，加的东西早晚要跟着重写 —— 这正是那条规矩想避免的事。

**② 改动面还很小（现在做最便宜）。**

| 位置 | 数量 |
|---|---|
| `self: ref T` 方法接收者 | 37 |
| 其它 `ref` 形参 | 14 |
| **返回类型里的 `ref`** | **0** ← 没有函数返回引用 |
| **struct 字段里的 `ref`** | **1** ← 只有 `slice<T>.data` |
| extC 源码里的 `ref` 总数（prelude + examples + tests） | ≈ 80 |
| 编译器源码里跟 `ref` 相关的行 | ≈ 98 |

**过半年再改，这些数字要翻好几倍。**

**③ `ref` 是「一条原则」的落点。**

> **P′：不能证明的，语法上必须看得见。**

extC 的规矩是「引用必须在类型里看得见」（所以有 `ref` 这个词）。
但现在 `ref` 看不见**两件重要的事**：它**能不能写**、它**能活多久**。
这两个看不见，P′ 就只实现了一半。

---

## 1. 现在的 `ref` 到底难受在哪（五条，每条都有能跑的反例）

### ① `ref` 到标量是个**死类型**（最直接的痛点）

| 操作 | 现在 |
|---|---|
| 透过 `ref` 读**字段** `b.n` | ✅ |
| 透过 `ref` 写**字段** `b.n = 0` | ✅ |
| 透过 `ref` 写**元素** `b.cell[1] = 9` | ✅ |
| **读 `ref` 指向的标量** `let y = p` | ❌ 报错 |
| **写 `ref` 指向的标量** `p = 5` | ❌ 报错 |
| `ref` 标量做算术 `p + 1` | ❌ 报错 |

⇒ `ref i64` 能声明，但**除了往下传什么都干不了**。
⇒ 直接后果：**`swap(a: ref i64, b: ref i64)` 写不出来**，而 extC 没有元组，
两个出参只能包 struct。

### ② `=` 在引用上**有歧义**，而且选了出人意料的那一边

```extc
var r: ref point = ref a
r = ref b          // ✅ 合法：换指向（reseat）
```

```extc
fn bump(p: ref i64) { p = 5 }   // ❌ 报错：给 ref 赋非 ref
```

两种写法长得一样，语言统一解释成「改绑定」。**读代码的人猜不出来哪个 `=` 是哪个意思。**
这等于把 `ref T` 做成了「一个不能做算术的 C 指针」，而不是「一个引用」。

### ③ `let` 是**浅**的（管名字，不管数据）

```extc
fn paint(v: slice<i32>) { v[0] = 999 }   // 签名上完全看不出它会写

let row = board[0..3]
paint(row)                                // 传进去就改了；let 没保住
```

三个缺口：拷进 `var` 就能写 / 传进函数照样能写 / `self: ref T` 方法接收者不受限。

### ④ **签名不说实话**：看 `ref T` 分不出「只读借用」和「可变借用」

`ref T` 一个词兼了两种意思（Rust 的 `&` 和 `&mut`）：

| 想要 | 但 `ref T` 表达不了 |
|---|---|
| 只读共享（大 struct 不想拷贝） | 跟「可变引用」长得一模一样 |
| 可变借用 | 只能靠「接收者是 `let` 还是 `var`」在调用点猜 |

**连带后果**：`"abc"[0] = 1` 编得过（`slice<u8>` 的 `data` 是可变引用），
而字符串字面量在 C 里是只读段 —— **写它会崩**。

### ⑤ **逃逸完全不检查**

```extc
fn bad() -> slice<i32> {
    var a: [8]i32
    return a[..]          // 返回指向局部数组的视图 —— 编得过！
}
```

今天没有任何东西拦它。这是 extC「安全像 Rust」承诺上**最大的一个洞**。

---

## 2. week-4 是**三块**，不是一块 —— 而且前两块不需要堆

| 块 | 内容 | 需要堆吗 | 该提前吗 |
|---|---|---|---|
| **A · 引用语义** | `ref` / `mut ref`、标量怎么写、`=` 是什么意思 | **不需要** | ✅ **最该提前** |
| **B · 逃逸检查** | 「引用不许活得比被指对象长」 | **不需要** | ✅ 该提前 |
| **C · arena + 动态数组** | 分配、`array<T>`、全局变量 | 需要 | ⏸ 可以等 |

**关键观察：A 和 B 跟「有没有堆」无关。**

- **A** 纯粹是类型形状问题（一个词还是两个词）。
- **B** 只需要知道**词法深度**（DESIGN §98–112 已经定了规则：比两个整数，
  比 Rust 的生命周期简单得多）。今天能被引用的对象只有**局部变量**和**静态数据** ——
  没有堆也照样检查得动。

**C 反而依赖前两块**：arena 的深度模型就是 B 用的那套；动态数组的形状
（`array<T>` 怎么创建、空数组的 `data` 怎么办）取决于引用规则的最终样子。
所以顺序上 **C 天然在后面**，这跟 G2（五子棋用不上堆）正好一致。

---

## 3. 块 A 的三个形状

### 形状 1 · `ref` 只给复合类型，标量不许有 `ref`

```extc
fn bump(n: i64) -> i64 { return n + 1 }    // 「改标量」的答案是**返回值**
x = bump(x)
```

| | |
|---|---|
| ✅ | 类型系统干净，没有死类型；完全符合值语义 |
| ❌ | `swap` 写不出来；多出参只能包 struct |
| 迁移 | 删掉标量 `ref` 的用法（今天只有「想写却写不出来」的那些，所以**几乎是零迁移**） |

### 形状 2 · 加**显式的解引用写法**（此形状的拼写待定）

```extc
fn bump(p: ref i64) { p.* = p.* + 1 }
fn swap(a: ref i64, b: ref i64) {
    let t = a.*
    a.* = b.*
    b.* = t
}
```

| | |
|---|---|
| ✅ | 能写；**「透过引用写」在语法上看得见**（正合 P′） |
| ✅ | **纯加法** —— 不改任何现有语义 |
| ❌ | 新语法；有点「指针味」（但 `ref` 本来就是 `T *`，只是把该显式的地方显式出来） |
| 迁移 | 零（今天没人写得出这些代码） |

> 拼写候选：`p.*` / `*p` / `deref p`。**主人说「别急」，这条先挂着。**

### 形状 3 · `ref T` 在值位置**自动解引用**（C++ 引用的语义）

```extc
fn bump(p: ref i64) { p = p + 1 }        // 读 p 得值，写 p 写进调用者
fn swap(a: ref i64, b: ref i64) { let t = a  a = b  b = t }
```

| | |
|---|---|
| ✅ | 最自然、零新语法，跟 `.` 自动解引用同源 |
| ✅ | 附带好处：如果裸 `ref` 只能当形参、不能存进变量，**「引用逃逸」按构造就消失一大类** |
| ❌ | **「换指向」写不出来了**（`r = ref b` 变成写值）—— 但换指向本来也没什么用 |
| ❌ | 改动面最大；跟「块 A 的另一个问题（只读 / 可变）」是同一块地 |
| ⚠️ | **不能走到底**：`slice<T>` 的 `data: ref T` 是真把引用**存起来**的，所以「引用只能当形参」这条不能推广到 struct 字段 |
| 迁移 | 需要重想每一处 `ref x` 表达式和每个 `ref` 变量的语义 |

### 还有一件事跟 A 绑在一起：**要不要 `mut ref`**

不管选形状 1/2/3，都得回答：`ref T` 到底是只读还是可变？

| 方案 | 形状 | 代价 |
|---|---|---|
| 保持一个词 | `ref T` = 可变（现状） | ④ 解决不了：签名还是不说实话，写字符串字面量还是会崩 |
| 拆成两个词 | `ref T` 只读 / `mut ref T` 可变 | **每个签名都要重想一遍**（含 prelude 的 6 个只读方法）；`slice<T>` 得分成只读视图 / 可变视图两个类型 |
| 保持一个词 + `let`/`var` 管 | 现状 + 已实现的「let 管根」 | ③ 解决了一半，④ 和「写字符串字面量会崩」还在 |

> **奶昔倾向**：形状 2 + `mut ref` 一起做 —— 形状 2 是纯加法，
> `mut ref` 是**一次性把 ④ 和「字符串崩」都解决掉**的那一刀。
> 但这是主人的语言，奶昔只摆事实。

---

## 4. 块 B：逃逸检查的规则草案

### 模型（DESIGN §98–112 已经定了，这里只是落实）

> **一只 arena = 一个词法作用域。** arena 构成嵌套树，越内层深度越大。
> 若引用 `r` 指向值 `v`，则 **`depth(r) ≥ depth(v)`**。

- **纯词法属性**：比两个整数。不需要生命周期变量、不需要解约束。
- **参数 = 深度 0**（它的被指对象在调用者那边，活得比本函数长）。

### 要检查的四件事

| # | 情况 | 判定 |
|---|---|---|
| 1 | `return v`（v 是引用/含引用的值） | 被指对象深度 > 0 ⇒ 拒绝 |
| 2 | 把引用存进 struct 字段 | 同上，按字段所在对象的深度比 |
| 3 | `o.f = v` 其中 o 的深度 < v 的深度 | 拒绝（v 活不过 o） |
| 4 | 传给 `ref` 形参 | 形参深度 0 ⇒ 被指对象深度必须 ≥ 0 ⇒ **恒真**，不用查 |

### 今天会拦住谁（用数据说话）

- **返回 `ref` 的函数：0 个** ⇒ 规则 1 不会误伤任何现有代码。
- **含 `ref` 的 struct 字段：1 个**（`slice<T>.data`）⇒ 视图天生是「可逃逸的引用」，
  这才是真正要注意的地方。

### 视图是唯一的复杂点（也是唯一的收益点）

```extc
// ✅ 必须允许：视图指向调用者的 board（形参深度 0）
fn rowView(self: ref board, y: i32, lo: i32, hi: i32) -> slice<i32> {
    return self.cell[y][lo..hi]
}

// ❌ 必须拒绝：视图指向局部数组
fn bad() -> slice<i32> {
    var a: [8]i32
    return a[..]
}
```

两者**语法上长得几乎一样**，区别只在「引用是从哪来的」。
这正是逃逸检查存在的理由 —— 也是 extC 从「语法像 Go 的普通语言」
变成 extC 的那一步。

### 实现上要动什么

- `Type` 里**还没有**挂深度的字段（T3 只留了概念上的位）⇒ 加 `int depth` 是一行。
- 检查点集中在：`EX_REF`（产生引用）、`EX_SLICE`（产生视图）、`ST_RETURN`、
  `ST_ASSIGN`、struct 字面量的字段初始化。
- 不需要改 codegen —— 检查全在 check 侧。

---

## 5. 建议的顺序

| 步 | 内容 | 大小 | 为什么这个顺序 |
|---|---|---|---|
| **1** | **定块 A 的形状**（一个决定：形状 1/2/3 + 要不要 `mut ref`） | 一次对话 | 它影响所有签名，必须先定 |
| **2** | 实现 A（编译器 + 改 ~80 处 extC 源码） | 中 | 定完立刻做，趁改动面还小 |
| **3** | 加块 B（逃逸检查，含 `slice` 的规则） | 中 | 有了 A 的引用语义才好定「深度挂在哪个类型上」 |
| **4** | 块 C（arena + `array<T>` + 全局变量） | 大 | 依赖前三步；G2 用不上，可以等 |

**第 1 步是唯一的「现在就要主人拍」的地方**，而且它不需要写代码 ——
就是选一个形状。选完剩下的都是执行。

---

## 6. 未决问题（先记下来，不急着答）

1. **解引用写法的拼写**：`p.*` / `*p` / `deref p`？
2. **`mut ref` 的拼写**：`mut ref T` / `var ref T`？方法接收者写 `self: mut ref board`？
3. **`slice<T>` 要不要分成两个类型**（只读视图 / 可变视图）？
   如果要，`a[1..3]` 出来的是哪个？（Rust 用 `&[T]` / `&mut [T]`；extC 得跟着定）
4. **形状 3 之后「换指向」真的不要了吗**？（今天 `r = ref b` 是合法的）
5. **逃逸检查要不要也管「传进函数存起来」**（把引用存进参数指向的 struct）——
   这需要引用的「传染性」分析，比深度模型贵。
6. **`@recursive` 的深度栈**（DESIGN §513–565）跟 arena 深度是同一个概念吗？
   如果是，两处最好一起设计。

---

## 附：本文引用的现状都是实测的

本文里每个 ✅/❌ 都用 `./build/extc --run` 真跑过，不是从文档抄的。
数字（0 个 `ref` 返回、1 个 `ref` 字段、80 / 98 处）是 `grep` 数出来的。
**要是哪天代码变了，这张表要跟着重数** —— 它是一份「体检报告」，不是永久事实。
