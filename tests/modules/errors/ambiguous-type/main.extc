/* ⭐ 反例：**两个模块都导出 `pair`，裸名有歧义** ⇒ 必须报错，不许挑一个 ✗
 * 为什么这条是硬要求（P′「不能证明的，语法上必须看得见」）：
 * "挑第一个匹配"会**静默绑成先注册的那个模块的类型** —— 编得过、类型是错的，
 * 比报错坏得多 ✗（真踩过：`pair` 命中 alpha 而用户以为是 beta）
 * 判据：`ttResolve` 数出所有匹配，≥2 ⇒ 报歧义并要求写限定名 ✓
 */
use alpha
use beta

fn main() -> i32 {
    var p: pair = alpha::make(5)
    return 0
}
