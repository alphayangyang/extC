// ⭐ PLAN #5：**无限递归要响亮**（以前是静默的 ✗）
//
// 以前两种表现都很难查：
//   · 被 gcc（都能把某些自递归折成循环）⇒ **静默死循环**，零输出、没有消息
//   · 真爆栈 ⇒ OS 报 `Segmentation fault` —— **不是 extC 的消息、更没有位置**
//
// 现在：**自递归**函数（传递地调到自己）进出时维护深度计数，超限就 trap 带位置 ✓
// 只对自递归函数吐守卫 ⇒ 非递归函数正常运行零额外代价（只有那两句自增/自减）✓
// 判据：必须退出码非 0 + 消息里有 `trap:` + 带 `文件:行`（tests/traps/run.sh 钉住的）✓
fn spin(n: i64) -> i64 { return spin(n) }
fn main() -> i32 { println(spin(0)); return 0 }
