# canary 的**已知缺口**（现在跑不过，故意留着）

这些是"**应该能编过**"的用例，但今天编不过 ⇒ 它们是**待修的缺陷**，不是误报。
放在这里（**不并进 `tests/run.sh`**）是为了：① 不把测试套件弄红 ② 缺口有据可查 ✓

## ① 返回"装着视图的泛型实例" ⇒ 生成的 C 编不过 ✗

```extc
struct box<T> { v: T }
fn ok(p: ref [8]u8) -> box<slice<u8>> { var b: box<slice<u8>> = { v: p[..] }  return b }
```
检查器**放行**了（正确 ✓），但 codegen 生成的 C 让 gcc 报错 ✗
⇒ **"生成的 C 编不过"属于最该防的一类**（extC 的承诺是"编过就一定编得过"）✓
⇒ 同类：`struct cfg { name: option<slice<u8>> }` 也一样 ✓

## 发现经过

这是**canary 第一批（携带引用维度）**抓出来的：5 对用例里 4 对误报 +
放开误报之后又暴露出这个 codegen 缺陷 ⇒ **写 canary 的价值当场兑现** ✓
