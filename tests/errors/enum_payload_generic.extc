// 同一形状的**泛型**版：`maybe<slice<u8>>`
type maybe<T> = | nothing | just(T)
fn make() -> maybe<slice<u8>> {
    var local: [8]u8 = [65, 66, 67, 68, 0, 0, 0, 0]
    return maybe<slice<u8>>::just(local[..])
}
fn main() -> i32 { return 0 }
