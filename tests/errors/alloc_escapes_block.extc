// 循环里分配的东西**不能存到循环外** —— arena 按块细化之后，那块内存
// 在这一轮迭代结束时就释放了（分配深度 = 当前块深度 = 2 > 目标 1）✓
fn main() {
    var p: ref i32 = alloc<i32>(1)
    var n: i32 = 0
    while n < 3 {
        p = alloc<i32>(1)
        *p = n
        n = n + 1
    }
    println(*p)
}
