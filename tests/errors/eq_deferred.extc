struct Tag {
    id: i32
}

struct Wrapper<T> {
    value: T

    fn same(self: ref Wrapper<T>, other: Wrapper<T>) -> bool {
        return self.value == other.value
    }
}

fn main() -> i32 {
    var a: Wrapper<Tag> = { value: { id: 1 } }
    var b: Wrapper<Tag> = { value: { id: 2 } }
    println(a.same(b))
    return 0
}
