// `??` 的主体要先算进临时变量（吐在**整条语句之前**）⇒ 它会跳到同语句里**更靠前的副作用**前面 ✗
// 源码读起来是 g 先、h 后，实际是 **h 先跑** ⇒ 报错让他拆两行（PLAN #22，2026-09-22 修）✓
// ⚠️ 判据是"**可观测**的副作用"：纯 getter 不算（顺序换了看不出来 ✓）
//    ⇒ 这里 `g()` 必须**会打印**，这条反例才成立 ✓
fn g() -> i64 { println("  g() 跑了")  return 1 }
fn h() -> ?i64 { return some(2) }
fn main() {
    var r = g() + (h() ?? 0)
    println("r = ", r)
}
