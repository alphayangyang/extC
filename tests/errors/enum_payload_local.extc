// 洞 #1：enum 的**载荷**来自本帧局部 ⇒ 返回出去就是悬垂。
// 以前会**编过**（因为检查器问"有没有引用"时只看 tag 0 = `empty`，回答"没有"）
type box = | empty | holding(slice<u8>)
fn make() -> box {
    var local: [8]u8 = [65, 66, 67, 68, 0, 0, 0, 0]
    return box.holding(local[..])
}
fn main() -> i32 { return 0 }
