// ⑪ 别名 + 过期（设计上允许，看它是"安全但过时"还是"崩"）
fn main() -> i32 {
    var buf: [8]u8 = [1, 2, 3, 4, 5, 6, 7, 8]
    let a = buf[0..4]                 // 视图 A
    var i: i64 = 0
    while i < 8 { buf[i] = 99  i = i + 1 }   // 把底层全改了
    println("A 现在 = ", a)            // 过时数据（安全），还是崩？
    return 0
}
