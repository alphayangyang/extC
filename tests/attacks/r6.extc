// ⑥ 想写元素正确该怎么办：`mut ref mut slice<T>` 能不能写出来
fn f(sp: mut ref mut slice<i32>) { (*sp)[0] = 9 }
fn main() -> i32 {
    var a: [4]i32 = [1,2,3,4]
    var s = a[..]
    f(ref s)
    println(a)
    return 0
}
