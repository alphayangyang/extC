extC 打印格式真值表（描述表换线**必须逐字节对齐**的东西）
生成方式：./build/extc --run <下列用例>（2026-09-21 实测，逐字复制输出）

标量: 42 3.5 true                       ← i32/f64/bool 直印
字符串字面量: hello                     ← slice<u8> 按**文本**印（不带引号）
结构体(含嵌套/切片/数组/?ref): inner { x: 5, y: true }
外层结构体: outer { a: 1, b: 2, f: 0.5, s: hi, in: inner { x: 5, y: true }, xs: [1, 2, 3], arr: [7, 8], np: <ref> }
对照结构体: inner { x: 0, y: false }
枚举无载荷: red blue                    ← 只印**变体名**
枚举带载荷: circle rect nothing          ← ⚠️ **载荷被丢掉了**（circle(2.5) 只印 circle）
泛型枚举: hold2 empty2                  ← 同上，载荷丢掉
option: some none                       ← ⚠️ 同上（some(3) 只印 some）
result: success failure                 ← 同上
切片: [1, 2, 3]
嵌套数组: [[1, 2, 3], [1, 2, 3]]
结构体切片: [inner { x: 5, y: true }, inner { x: 5, y: true }]

规则归纳（给描述表用）：
  · struct  → `名字 { 字段名: 值, … }`（空结构体是 `名字 {  }`）递归 ✓
  · 切片/数组 → `[a, b, c]`（分隔 ", "）✓
  · 枚举（含 option/result）→ **只印变体名，载荷不印** ✗
  · ?ref → `<ref>`（非空）/ 待确认空值（见下）
  · slice<u8> → 按文本印（它跟其它切片的规则**不一样** ✗）

⚠️ 两个待拍板/待确认：
  ① 载荷被丢掉：是**有意**（只看 tag）还是**漏了**？描述表方案要决定是否顺便补上
     （补上会**改变输出** ⇒ 撞'逐字节不变'的判据，需要主人点头）
  ② 空 ?ref（null）印什么？本用例只覆盖了非空 ⇒ 换线前要补一个用例抠出来

================ 补全（第二轮实测，2026-09-21）================

浮点: 1 0.1 1e+20 -2.5        ← 用 %g：**1.0 印成 `1`**（不是 1.0）✗ 描述表要照抄
整数: -7 0
空结构体: empty1 {  }          ← 两个空格（`名字 {  }`）
[N]u8 数组: [65, 66, 67, 0]     ← **数组**按数字印
[N]u8 切片: ABC                ← **切片**按文本印 ⇒ u8 的"文本规则"只对 slice<u8> 生效 ✗
bool 数组: [true, false]
null 的 ?ref: holder { np: <ref>, lb: [65, 66, 67, 0] }
非空 ?ref: holder { np: <ref>, lb: [65, 66, 67, 0] }   ← ⚠️ 两者**一模一样**！
option 数组: [some, none]

⚠️ 第三个发现（跟'载荷被丢掉'并列）：
  **null 的 ?ref 也印 `<ref>`** ⇒ 调试打印里**分不出空/非空** ✗
  （描述表方案顺手能修：空印 `<null>`、非空印 `<ref>`）—— 同样会改变输出 ⇒ 要主人拍板 ✓

⇒ 换线前的未知格子已经**清零**：每种类型的格式都实测在手 ✓

================ 关于"有多少 _debug 是活的"的诊断（第三轮，⚠️ 脚本不可靠）================

试过两版 awk 诊断（都是文本层做可达性）：
  ① 第一版：把**前向声明**行也当成调用点 ⇒ 每个 _debug 都'活' ✗
  ② 第二版：只认缩进行 ⇒ 反而几乎全'死'（big1000 说 0/1014 ✗ 明显不对）
⇒ **结论：别用文本层可达性下结论**（生成 C 的 println 展开形态比想象的绕）✗
   要做就得在**编译器的 AST 层**算（那正是目标 ③ 要实现的东西）✓

✅ **可信的数据（跟这个脚本无关，前面直接量的）**：
   · 合成压测程序（N=1000）：**2028 个 _debug**、**11,250 / 49,140 行 = 22.9%** ✗
   · 而那个程序**一次都没 println 结构体** ⇒ 这 2028 个**全是废的** ✓
   · 真实例子（varArray/gomoku-board/option-result/bt）里 _debug 只有 4~9 个 ⇒ 收益在'类型多'的程序上 ✓
⇒ ③（描述按需）的收益上限 = **约 23% 行**（压测形状），实现时用 AST 层算 ✓
