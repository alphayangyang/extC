# DECISIONS.md —— week-0 里奶昔自己拍的语法决策

> 主人说：「写的时候如果遇到什么语法目标我没有说明白的一定要跟我说」。这就是那份清单。
>
> 每一项都标了**奶昔怎么拍的**和**要不要主人改**。改一项的成本很低（parser 是分层的），但越往后改越贵。
>
> 分三类：**① 需要主人拍板**（有分歧空间）**② 从 DESIGN.md 推出来的**（不该算奶昔拍的）**③ 写代码时发现的新问题**。

---

## ① 需要主人拍板

## ① 语法决策：**已定案**

| # | 决策 | 定案 | 备注 |
|---|---|---|---|
| 1 | **分号** | **换行即语句结束**，分号可选、会被忽略 | Go 味 |
| 2 | **类型标注** | **局部可省**（从初始化式推导）；**函数签名和结构体字段必须写** | 局部短、接口稳，两边好处都占 |
| 3 | **字符串字面量的类型** | **`Slice<u8>` 视图**（静态数据的只读视图，零分配） | 跟 `view()` 同源；`str` 这个内建类型**作废** |
| 4 | **顶层全局变量** | **加。全局 = 深度 0 的 arena** | 不需要任何新规则 —— 引用规则自动禁止全局存局部地址（`0 ≥ 1` 为假）；`static` 关键字因此消失 |
| 5 | **常量 / 枚举** | **`const` 编译期常量 + 简单 `enum`（无载荷）** | `enum Color { empty, black, white }`；带载荷的标签联合 + `match` 往后放 |
| 6 | **程序入口** | **注解 `@main` 标在任意函数上**（一个文件一个） | 多入口 / 命名入口等模块系统 |

### 定案之后的连带影响（必须跟着办）

- **`Slice<u8>` 定案 ⇒ 泛型成了 week-1 的必做项。** `Slice<T>` 没泛型做不出来，不能拖。
- **`str` 作废 ⇒ `println` 必须认识 `Slice<u8>`**（现在它只认内建 `str`）。
- **`@main` 定案 ⇒ 代码生成要生成真正的 C `main` 当 shim**，去调被标注的那个函数。

## ①″ 第二批定案（来自五子棋示例代码审读）

| # | 决策 | 定案 | 连带影响 |
|---|---|---|---|
| 7 | **格式串 `{}`** | **要，但必须是编译期展开的语法糖**（运行时无解析 ⇒ 不违反 P） | 格式串必须是字面量；`{}` 个数与实参个数在编译期检查。**比多参数顺序打印更安全** |
| 8 | **默认零初始化** | **采纳**：`var b: Board` 合法，自动清零 | week-0 那条「needs an initializer」的错误要改掉。**未初始化 UB 从语言里消失** |
| 9 | **方法写在 struct 体内** | **采纳** | parser 要改：struct body 里允许 `fn`；C 生成要做名字修饰 |
| 10 | **`ref` 在调用点显式** | **采纳**：`evaluate(ref s.board, player)` | `ref` 从「类型修饰」升级成「**表达式**」 |
| 11 | **枚举 → 文本** | **编译器内建**：无载荷枚举自动有名字文本（`GameError.Occupied` → `"Occupied"`） | `println(e)` 直接可用；带载荷的标签联合以后要用户提供 |

### 决策 9 的意外收益：方法名有命名空间了

方法进 struct 体内之后，`Point.eq` 和 `Board.get` 是两个不同的名字 —— 顶层不必为了避冲突而发明 `point_eq` / `board_get` 这种名字。
**对主人讨厌的「丑名字」是一次系统性改善。**

### 决策 10 的一个待定细节：方法接收者要不要写 `ref`

示例代码**自己不一致**：

| 写法 | 示例 |
|---|---|
| 自由函数写了 `ref` | `evaluate(ref s.board, player)`、`alphabeta(ref s, ...)`、`candidates(ref real)` |
| 自由函数**没写** `ref` | `line_score(b, p, player)` —— 形参也是 `ref Board` |
| 方法调用都不写 | `b.count_dir(...)`、`s.rollback(mark)`、`self.board.place(...)` |

**奶昔按这条规则实现（不对就骂奶昔）**：

> **`.` 本身就表示「在这个值上操作」，所以方法接收者自动取地址；自由函数的 `ref T` 实参必须在调用点显式写 `ref`。**

## ①‴ 还欠着的（原 ①′）

| # | 决策 | 现状 | 卡在哪 |
|---|---|---|---|
| 12 | **`for` 循环** | 没有，只有 `while` | **四种形态全要**（主人已明确）：`for d in dirs` / `for i in 0..n` / `for dy in -2..3` / C-style。待定的是**实现顺序**，以及 §4 范围类型要不要随之落地 |
| 13 | **`int` 还是 `i32`** | 实现是 `i32` 系列 | 主人自己写过 `var thisIsAGoodName: i32`；示例里的 `int` 是 AI 写的。**奶昔按 `i32` 走**，除非主人反对 |
| 14 | **`type` 还是 `enum`** | 都没做 | 示例写的是 `export type Player = \| Black \| White \| Empty` |
| 15 | **`Result<void, E>` 还是 `Result<(), E>`** | 都没做 | 示例用 `void` |
| 16 | **`@main` vs `module main`** | 已定 `@main`，但示例是 `module main` + `fn main` | 两者并存？还是优先关系？ |
| 17 | **`Sandbox` 的 undo 栈** | 示例有，主人自己的 gomoku v1.3 说「全项目零 unmake」 | 建议以 v1.3 为准（那份是复审过的） |
| 18 | 无返回值函数 | `fn f() { }` 省略 `->` | 小：要不要强制 `-> void`？ |
| 19 | `println` 的名字 | 内建 `print` / `println` | 小 |
| 20 | 下标 / 越界 | 没做 | week-1 跟数组一起来；届时定「trap 还是 `Option`」 |
| 21 | `+=` 等复合赋值 | 词法器认，parser 不认 | 小 |
| 22 | `&&` / `\|\|` vs `and` / `or` | 用 C 味符号 | 小 |
| 23 | 只报第一个错误 | 没有错误恢复 | 中 |
| 24 | **模块系统**（`module` / `export` / `import` / `::`） | 完全没做 | 示例里每个文件都用了，而且 `Board::new()` 是刚需。**什么时候上？** |

### 命名（这条其实已经定了，只是列出来给主人确认）

`i8/i16/i32/i64/u8/u16/u32/u64`、`f32/f64`、`bool`、`str`、`void` —— **没有 `int` / `long` / `char` / `double`**。
字面量默认类型：整数 `i32`，浮点 `f64`。

---

## ② 从 DESIGN.md 推出来的（不该算奶昔拍的）

| 决策 | 出处 |
|---|---|
| `println` 无格式串 | §3「格式串是运行时解析，违反 P」 |
| 方法 = 首参数名为 `self` 的自由函数；`a.f(x)` ≡ `f(a, x)` | v0 §6 原设计，奶昔只是一致地实现 |
| 接收者自动取地址（`a.f(x)` 里如果 `self: ref T` 就传 `&a`） | 上面那条的直接推论 |
| `let` 不可变 / `var` 可变 | 主人的 `var thisIsAGoodName: i32` |
| 函数 camelCase、类型 PascalCase | 主人的命名规范 |
| 生成的 C 带 `#line` | §5「靠映射实现轻量，不靠不解析」 |

---

## ③ 写代码时才发现的问题（真收获）

### 1. 裸 `{}` 逼出了一条规则，而且它必须是统一规则

`var p: Point = {}` 能靠声明类型补全，那 `return { x: 0, y: 0 }` 呢？`f({})` 呢？

奶昔拍的是：**类型从上下文推导，只在「上下文能唯一确定类型」的三个地方允许** ——
`var x: T = {}`、`return {}`（函数返回 T）、`f({})`（形参类型 T），**推导不出来就报错，绝不猜**。

关键不是这三个位置，而是：**它必须是一条统一的规则，不能是一个特例**（§11.7）。如果只支持 `var` 那一处，它就是个特例，将来会变成坑。

### 2. arena 里的动态数组增长会留垃圾 ⚠️

`Vec`/`Buf` 增长时新块从 arena 拿，**旧块还不了**（arena 不单独回收）。编译器无所谓（跑一次就退出），但这是 **arena 模型的真实代价**，对长跑程序会有影响。

设计上要回答：`Array<T>` 要不要提供 `reserve(n)`？arena 要不要支持"回收最后一个块"？—— **这条建议写进 DESIGN.md**。

### 3. 「不建 AST」在这里被实测判死

`typeOf` 必须在**表达式生成阶段**可用（`println` 要按静态类型选格式、字段访问要知道是 `->` 还是 `.`、`let` 检查要知道可变性）。这些信息只存在于符号表和类型里 —— 也就是**只存在于建了 AST 的世界里**。

v0 那句「不解析整个 C、只重写 token」里对的那一半（C 透传）保留着；错的那一半（不建 AST）在写第三行代码的时候就撞墙了。

### 4. 类型检查现在很浅（要说清楚）

week-0 只做这几项检查：
- 未定义名字 / 未知函数 / 未知字段 / 未知类型
- 给 `let` 赋值
- 参数个数
- 裸 `{}` 推导不出来

**没做真正的类型检查**：`let x: i32 = "abc"` 会漏过去，最后由 gcc 报错。真正的类型检查器是下一步。
另外**只报第一个错误**，没有错误恢复。

---

## 还没做，但 DESIGN.md 说要有的（按优先级）

| 缺什么 | 对应设计 | 优先级 |
|---|---|---|
| **数组 `[N]T` + 下标 + 越界规则** | §4 范围类型 | **最高** —— 没有它连五子棋的棋盘都写不出来 |
| **逃逸检查 / arena** | §2 唯一引用规则 | **最高** —— 这是 extC 的命，现在 0% |
| 真正的类型检查器 | §3 | 高 |
| `Option` / `Result` / `?` | §3 | 高 |
| `struct` 之外的顶层全局变量 | §2「全局 = 深度 0 的 arena」 | 中 |
| `enum` + `match` | §3 | 中 |
| `@recursive` | §3 | v2 |
| `for` 循环 | 待定 | 中 |

> **重要提醒**：现在这个编译器**能跑通链路，但一点 extC 的安全保证都还没有**。
> 它现在是一门「语法像 Go 的普通语言」。arena 和逃逸检查接上之后，它才是 extC。
