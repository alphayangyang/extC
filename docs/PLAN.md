# PLAN.md —— 还剩什么（唯一路线图）

> **2026-09-18 重写；2026-09-22 逐行复核过一次**（每条都实测过状态，不是照抄 ✓）。
> 以后再问「还有什么要做」，只看这一份就够了 ✓
>
> **现状（2026-09-23 实测重核 ✓）**：`./check.sh quick` **13 节全绿**（完整模式 **19 节**，
> 多出的 6 节是基准）—— 测试 **257 通过 / 0 失败** · arena 5 · 警告零误报 · 泛型 3 ·
> IO 10 · extern 4 · 模块 12 · ASan 8 · 层号零漂移（98 语料）· 攻击库与基线一致 ✓
> **`tools/golden.sh check` 另跑**（95 个文件逐字节相同 ✓ —— 它**不在** `check.sh` 里 ✗，
> 别把它算进"节数"：那是本文件前两版把 11 节写成 14 节的原因之一）
>
> ⭐ **2026-09-23 记账对账**：这一行原来写着「**7 通过**（测试 **231**）· 缺陷只剩 **7 条**」✗ ——
> 那是 **09-22 IO-0 落地之前**的数（`check.sh` 当时只有 7 节），之后加了 4 节、测试 231 → 257，
> 这一行**没跟着改** ✗（跟 §0.4 里 #32/#42 一模一样的老毛病：**实现/测试涨了、记账不动** ✓）。
> 同一轮还抓到另外两处：`README.md` 状态节写「14 节 · 251 测试」、
> `extC-overview.md`/`SPEC.md` 写「251」⇒ **三处一起改成实测值** ✓
> 正式设计文档 [`SPEC.md`](../docs/SPEC.md) · 原则与承诺 [`SPEC.md`](../docs/SPEC.md) §0.5 / §1 ·
> 内存模型的形式化 [`ARENA-FORMAL.md`](../docs/topics/ARENA-FORMAL.md) · 区域工作总账 [`PLAN-REGION.md`](../docs/history/PLAN-REGION.md)

---

## 0. 先说三句话

1. **没有什么是"必须现在做"的。** 语言今天**已经能写纯计算程序**（算法、棋盘、解析、状态机、
   协议分发）—— 剩下的几乎全是**加法** ✓
2. **设计阶段基本结束了。** 剩下的活**不需要新的原则**，是"力气活" ✓
3. **只有一条主线的里程碑**：让**五子棋引擎能跟人下**（那是项目一开始瞄的靶子）。

---

## 0.4 🐞 **缺陷清单（唯一权威编号 —— 2026-09-22 逐行复核过）**

> 这是"**坏了 / 该响亮却没响亮**"的清单 —— 跟"新功能"分开。
> ⭐ **`PLAN #NN` 这个写法只指这张表**（别的文档引用它也都指这里）✓
> ⚠️ §2 有一张**同编号的旧表**（#4/#5/#6/#7/#8/#9/#10 跟本表撞车）⇒ 那边已标映射 ✓
>
> **2026-09-23 复核（**逐条实测**过一遍 —— 这一版每个数字都是**数**出来的 ✓）**：
> **53** 个编号里 —— **48 条已修** · **真缺陷 0 条**（`#5`/`#9`/`#10` 于 2026-09-23 修完；
> `#52` 于 2026-09-23 当天补上并修完；**`#53` 也在同一天补上并修完** ✓）·
> （另：**`#42` 那一格的正文里**记着一个**残留小瑕疵** —— 走 prelude 的 `==` 报错时位置指到 prelude 的行。
>   它不是独立的格，算在已修那一批里 ✓）
> ⚠️ **`#53` 当初算"缺口"不算"缺陷"**（**不漏 UB、不误拒合法代码**，是**功能缺失**）——
>   跟 `#9`（`allocSlice`）同一档、跟 `#14`（已知边界）不同档 ⇒ 别把它读成安全问题 ✓
>   **2026-09-23 当天已修完** ✓
> **5 条不是缺陷**：**#14**（已知边界，主人已拍板不做别名分析）· **#29 · #30** 等主人拍板 ·
>   **#48 · #49** 可选优化（那两格是同一件事：优化 + 它的前置量测）✓
> ⭐ **2026-09-23 补记（这一条本身就是一次记账事故）**：**#53 全限定名**只活在 `DEVLOG`
>   （五条"尝试失败"的记账 `e5da2af`→`7dd154c`），**权威表里一条都没有** ✗ ——
>   于是"今天最痛的事，在唯一权威处查不到"，正好违反本文件 §0.4 自己那条
>   「改完就回来核这张表」的规矩 ✓ ⇒ 补进表（**当天又修完**），计数跟着改 ✓
> ⇒ **48 已修 + 5 非缺陷 = 53 格** ✓（表里就是 53 格，无缺号 ✓）
> ⇒ **计数方法**（别靠脑算）：`grep -cE '^\| ~~\*\*[0-9]+\*\*~~ ✅'` = 已修；其余普通行的编号 = 未完成 ✓
> ⭐ **2026-09-23：§0.4 的真缺陷清零** —— `#5`（无限递归）· `#9`（`allocSlice`）· `#10`（裸构造器话术）全部修完 ✓
> ⭐ **2026-09-23 降级一条**：**#14（参数根收窄）从"缺陷"改成"已知边界"** ——
>   主人拍板「**不做别名分析**」（其实一直没打算做）✓ 三条依据见那一格；
>   它**既不漏 UB 也不误拒常见写法**（要链式遍历绑一个名字就行，一行）⇒ 不该占"缺陷"的位置 ✓
> ⚠️ **这一轮实测又抓到两条「早就修好了、但记账没跟上」**：
>   · **#32**（有家函数里 `new` 一律进家）—— 实测 `new` **已经按块**，只有真逃逸才进家 ✗
>   · **#5 附带的那条 UX**（`--run -O0` 报 usage）—— 实测**现在能跑**，是 #11 在 `6ae3008` 顺带修的 ✗
>   · 外加 **#42**（用户抓的：它的出路就是 #43，早已落地 ✗）—— **同一条根因，三个实例** ✓
> ⇒ **根因**：一格写了「出路是 X」或「附带问题 Y」，X/Y 做完之后**没人回来划掉它** ✓
> ⇒ **规矩（这次写死）**：修完一条，**第一件事**回来划掉它，
>   并**顺手搜一遍全表**：这一条被别处提到过吗？它的「出路 X」做完了吗？依赖它的另一格还成立吗？✓
> ⚠️ **2026-09-23 另记一个表格维护坑**：`#37` 那一行曾被一个**空行**挤出表格边界
>   （表格从中间断开 ⇒ 那一行不再渲染、编号也"看起来缺了"）✗
>   ⇒ 改这张表之后要**机械校验**：`表内编号数` 是否为 53、有没有缺号 ✓
>   （以前这里写的是 **51** ✗ —— 这个数自己也过时了：`#52` 补进来时没改 ✓
>    ⇒ **校验数的那个数，也会过时**：改完表连它一起核 ✓）

| # | 缺陷 | 现象 | 级别 | 大小 |
|---|---|---|---|---|
| ~~**1**~~ ✅ | **枚举载荷的逃逸检查有洞**（**2026-09-20 修**，见 §0.5） | `fn make() -> box { … return box.holding(local[..]) }` **编过** ⇒ 悬垂 / **UB**。**写链表必踩**（造完返回）| **唯一能拿 UB 的路** | 中（见 §0.5）|
| ~~**2**~~ ✅ | **同一函数里两个 `match` 绑同名** | **2026-09-20 修**：check 时把 `cname` 写回 `MatchArm.binds`（跟 `Param.cname` 同一套路）✓ | — | 小 |
| ~~**3**~~ ✅ | 🐛 **`alloc<[4]i32>(1)` 之后索引**（**2026-09-22 复核：已修**）| 原记录："extC 放行、生成的 C 编不过（gcc：*'p' is a pointer; did you mean '->'?*）"。**实测**：`var p = alloc<[4]i32>(1)` 之后 `p[0] = 7` ✓ 与 `(*p)[0] = 7` ✓ **两种都编得过、gcc 过得去、跑出 `ok = 7`** ✓（多半是 #25 那次"`p[..]` 切 `ref [N]T`"顺手修掉的 —— 同一族"引用打到指针上"）| — | 已修 |
| ~~**4**~~ ✅ | ⚠️ **字面量越界不报编译错**（**2026-09-23 修**）| `b[10]`（`b: [4]i32`）**能编过**，运行时才 trap ⇒ **能证明的没在编译期报** ✗ 实测运行那一侧是**好的**（`trap: index 10 out of range (length 4)`、退出码 1 ✓）⇒ 缺口**只在"编译期能证明却没报"**这一半 ✓ **修法**：`EX_INDEX` 补上跟切片**同一组判据**（半开区间 `[0,n)`；负数用 `asIntLit`；**只对 `TY_ARRAY`** —— 视图长度运行时才知道，照旧留给 trap）⇒ 现在 `error: index 10 is not inside `[4]i32` (length 4)` ✓ 判据：新正例 `examples/index-bounds.extc`（边界不误伤）+ 两条新反例；测试 **245 → 248**、golden **只多那一个新例子**（其余 89 个逐字节不变 ⇒ 纯检查器改动 ✓）⚠️ **顺带抓到**：`tests/attacks/h4_oob.extc`（标题就是"④ 字面量越界"）**本来就列在 BASELINE（允许通过）里** ⇒ 攻击库把**这个 bug 记成了"已知安全"**，已从基线删掉 ✓ | P′ | 小 |
| ~~**5**~~ ✅ | ⚠️ **无限递归 ⇒ 无声**（**2026-09-23 修**） | 修法：**只对自递归函数**（传递地调到自己）吐一个深度守卫 —— 序言 `extc_rec_enter(path,line)` 自增，**每个出口** `--__extc_rec_depth`，超 `EXTC_REC_LIMIT`（10 万）就 `trap` 带位置 ✓ 判据：`tests/traps/recursion_too_deep.extc`（`t.extc:10: trap: recursion too deep (unbounded recursion?)`、退出码 1 ✓）；合法深递归不受影响（`sumTo(10000)` 正常 ✓）；非递归函数不加守卫 ✓ ⚠️ **踩过一个坑**：第一版把递减写在**取值之前** ⇒ `return f(n)` 生成成 `--depth; return f(n);` ⇒ 尾调用每帧把计数清 0 ⇒ 守卫永不触发（`-O0` 下 gcc 折成真尾调用 ⇒ **秒退零输出**✗）⇒ 改成"先算值、再递减" ✓ | P′ | 小~中 |
| ~~**6**~~ ✅ | ⚠️ **arena 没内存时没位置**（**2026-09-22 修**）| 原来只有光秃秃一句 `extc: out of arena memory` + `exit(1)` ✗（违反 P′：能说清是哪一行却没说）。修法：分配原语收下调用点的位置 —— `extc_arena_alloc(a, n, file, line)`，六个调用点（`new T` / `new T[n]` / `alloc<T>(n)` / `@overwrite` 的格子）都传 `g->path` + 行号 ✓ 现在报 `文件:行: trap: out of arena memory (this allocation wanted N bytes)` ✓ 判据：新用例 `tests/traps/arena_oom.extc`（要 1PB ⇒ malloc 必失败，不依赖 ulimit/overcommit 的运气 ✓）+ **`tests/traps/run.sh` 收紧成"trap 消息必须带 `文件:行`"**（这种退化再也回不来 ✓）⚠️ 已知小瑕疵：OOM 发生在 prelude 里（`varArray::push` 扩容）时，位置指向 **prelude** 的行 ✗（诚实但不够友好；要更好得带调用栈，另记）| P′ | 小 |
| ~~**7**~~ ✅ | **`option<slice<u8>>` 报错落在 prelude 里** | **2026-09-20 第三刀封掉**：`option` 成了枚举，tag 0 = `none` **不带载荷** ⇒ 里面有 ref 也有零值 ✓（那个"必须报错"的反例变成了正例 `examples/option-ref-payload.extc`）| — | 小 |
| ~~**8**~~ ✅ | 🟡 **`let v = m; return v` 被误挡**（**2026-09-22 复核：两半都修完了**）| 第一半（2026-09-20）：**引用型绑定**单独跟踪「被指对象深度」（`Sym.refDepth`）⇒ `var cur: ?ref node = head … return cur` 合法 ✓。**第二半**原来记成"仍被误挡"✗ —— 实测**已经好了**：`var s = a[..]  return s` ✓ · 带标注 `var s: slice<u8> = a[..]  return s` ✓ · 装进 struct 字面量再返回 ✓ **三种都编过**（是 #26"含引用的值绑定也跟踪 refDepth"那一刀一起修掉的 ✓）| — | 已修 |
| ~~**9**~~ ✅ | 📌 **`alloc<T>(n)` 当数组用不了**（**2026-09-23 修**） | 修法：补上原语 **`allocSlice<T>(n) -> mut slice<T>`**（跟 `alloc` 同一条路，只有返回类型不同）—— 帧 arena 里要 n 个 T、返回 `{data,len}` 视图、**而且清零**（兑现 `SPEC.md` §0.6「过期读到的字节永远是初始化过的」那条承诺）✓ 逃逸语义跟 `alloc` **完全一致**（返回它 / 循环里分配到循环外都被挡 ✓）判据：`examples/allocSlice.extc`（运行时长度 + 索引 + 再切片 + 清零）+ `tests/errors/allocSlice_escape_return.extc` ✓ | 缺口 | 小 |
| ~~**10**~~ ✅ | 🟡 **裸写构造器**（**2026-09-23 修**） | 修法：**只改报错话术，不加推断**（定案 27：关联调用写全类型是故意的）⇒ 现在两条路都指路：`circle(2.0)` ⇒「`circle` is a variant of `shape`, not a function -- **did you mean `shape.circle(...)`?**」；`let s: st = ok`（无载荷）⇒「did you mean **`st.ok`**?」（这条原来报 `undefined name` 同样没指路 ✗）✓ 判据：`tests/errors/bare_variant_hint.extc`；真的没这个名字时仍是老消息 ✓ | UX（报错话术）| 极小 |
| ~~**11**~~ ✅ | **没有 `-O` / `-march` 开关**（**2026-09-20 加**：`-O0..-O3`、`-march=native`，默认仍是 `-O2`）| 见 MANUAL 驱动那一节 ✓ ⚠️ 实测：这两项**收益看负载**（矩阵乘曾经 2×；mandelbrot/binary-trees 在本机 ≈ 0）—— 别当成万能加速 ✓ ⚠️⚠️ **2026-09-22 追记：这两个开关其实一直用不了** ✗ —— 参数循环里多写了一次 `i++` ⇒ `extc -O2 f.extc` 把 `f.extc` 当被吞掉的参数 ⇒ **直接报 usage** ✗（**"没报错 ≠ 跑过了"**的又一个实例：加了开关却从没验过命令行）⇒ 已修 ✓ |
| ~~**13**~~ ✅ | **显式解引用 `*p`** | **2026-09-20 完成** ✓ 隐式解引用已删除（只留"输出自动解引用"和"成员选择导航"两个例外）| — | — |
| ~~**17**~~ ✅ | 🚨 **泛型体里的引用规则整片被跳过**（**2026-09-20 修**；跟 §0.5 那个同族）| 泛型体是照**模板**查一次，那时 `T` 不透明，而 `typeContainsRef(TY_PARAM)` 返回 **false** ⇒ 所有"含引用才查"的规则（借来的值 / 深度）**全部早退** ✗ 实测：`struct boxT<T> { v: T  fn stash(self: mut ref boxT<T>, value: T) { self.v = value } }` 用 `boxT<slice<u8>>` 实例化后 `bt.stash(deeper[..])` 把指向更深局部的视图洗进活得更久的 `bt` ⇒ **悬垂**（具体类型版会被 "cannot store a borrowed value" 挡住 ✓）| **能拿 UB**（跟 #1 同级）| 中～大 |
| ~~**18**~~ ✅ | **泛型实例化时复查体**（**2026-09-20 做完**，跟 #17 一起）| 现在有三类走"推迟到实例化"：`==`（eqChecks）、**引用规矩**（深度/借用）、**零值**（refChecks）。模式统一：模板期记一笔 + 记好数字 ⇒ 实例化时 `typeContainsRef(tsub(T))` 决定这条规矩适不适用 ⇒ 消息里点名实例 ✓ ⚠️ **剩下的**：更多检查将来可能也要按实例复查（比如 `==` 之外的运算符协议、可打印性）—— 模式已经有了，加一类就是加一个 `recordXxx` ✓ | — | — |
| ~~**16**~~ ✅ | **枚举不能带方法 + 没有泛型自由函数** | **2026-09-20 用语法糖绕过去了**：`e ?? 兜底`（丙之后又加了乙）和 `e!` 就是 `valueOr` / `unwrap`，而且是**编译器**给的、**对所有 T 通用** ⇒ **不用**给枚举加方法、也**不用**泛型自由函数 ✓（"读值"这条路彻底通了）| — | — |
| ~~**20**~~ ✅ | 🐛 **数组里装带载荷枚举 ⇒ 生成的 C 编不过** | **2026-09-20 撞出来并修掉**：`var slots: [4]?i64` 会派生一个数组的 `_eq` helper，而 `typeHasEq` 把**所有**枚举都当成"有 `==`" —— 可带载荷枚举在 C 里是 `struct { tag; union }`，**没有 `==`** ⇒ gcc 报 `invalid operands to binary ==` ✗（类型检查那边本来就禁止比它，是 codegen 没跟它一致）。修法：`typeHasEq` 对 `TY_ENUM` 要求 `!enumHasPayload` ✓ ⚠️ 这是第三刀改类型表示的**后遗症** —— 换表示之后要把"按旧表示写死的判断"全扫一遍 |
| ~~**21**~~ ✅ | **`??` 的三条安全网** | ① 主体含调用 ⇒ **先算进临时变量**（#19 做完之前是报错；两种都不会让它跑两次 ✓）② **兜底那侧的深度也算**（`m ?? ref 局部` 逃逸被挡 ✓ 实测）③ 兜底那侧按结果类型**显式转换**（C 的通常算术转换会悄悄拓宽：f32 载荷配 double 字面量就是典型）| — | — |
| ~~**19**~~ ✅ | **`??` 的主体可以是调用**（**2026-09-20 做完**）| 加了通用的"**语句前缀**"机制：主体不是纯的 ⇒ 检查器打 `needTemp` 标记，codegen 在**所在语句之前**吐 `option_i64 __extc_c0 = f();`，三元写在这个变量上 ⇒ **f() 只跑一次** ✓ 主体本来是变量时**不生成临时变量**（还是纯三元）✓ 吐不出来的位置由检查器报错：**只剩 `while` 条件**（吐出来只算一次 ✗ 语义就错）—— ⚠️ 全局初始化式**不属于这条**：它本来就要求"必须是常量"，报那条更根本的原因 ✓ | — | 中 |
| ~~**24**~~ ✅ | **跨函数分配会累积在调用者的"家" arena 里**（**压测实测，当天修掉**）| `bench/stress/`：300 轮 × 2001 节点链表（在 `buildList` 里分配、返回）⇒ extC 峰值 **15.3MB** vs C **5.9MB**（≈ 2.6×）✗ 因为 `buildList` 是"有家"函数 ⇒ 调用点传的是 **调用者的家（函数体那只）**，而不是**循环体那只** ⇒ 循环里累积到函数结束 ✗（A2 的紧致性**在跨函数这一侧丢了**）⚠️ 结果**完全正确**（跟 C 逐位相同），纯粹是多占内存。**修法**：调用点按"结果会不会逃出当前块"选 arena（不逃 ⇒ 传 `&__extc_a[当前块]` ✓）—— 这正是 A3-2 留的那个"紧 case"，需要调用点的逃逸判断 | 内存（**压测就是这么发现的**）| 中 |
| ~~**22**~~ ✅ | 📌 **`??` 的临时变量会先算**（**2026-09-22 修**：改成**报错让他拆行** ✓）| 实测：`var r = g() + (h() ?? 0)` 里 **`h()` 先于 `g()`** 跑 ✗（临时变量吐在整条语句之前）。修法：检查器加一位"本语句里已经出现过**留在原地**的调用"——遇到"要临时变量的 `??`"且旗子已举 ⇒ **报错**（教他 `var t = h() ?? 0` 拆两行 ✓，主人：「显式是对的」）✓ 判据只看**调用**（`new`/字面量/转换顺序不可观测 ⇒ 不报 ✓）；⚠️ **踩了两个误报**（都被语料当场抓出来 ✗）：① 旗子取样点放在主体查完之后 ⇒ 主体自己的调用把旗子举起来 ⇒ `var a = h() ?? 0` **第一句**就误报 ✗② 同一个 `println` 里两个 `f() ?? -1`：多个临时变量是**按源码顺序一起**提前算的 ⇒ 它们之间顺序没变 ⇒ 不该报 ✗（`@overwrite` 那套"临时变量"是同一族）⇒ 记两条教训：**"前面有副作用"要看"是不是留在原地的"**（提前算的不算）✓ 判据：新反例 `tests/errors/coalesce_order.extc` + 新正例 `examples/coalesce-order.extc` + **正例语料零误报**（5 个例子曾误报 ✓ 全好）✓ | 语义清晰度 | 中 |
| ~~**15**~~ ✅ | 🐛 **`match` 分支里的 `break` 跳出的是 `switch`**（**2026-09-20 修**）| **2026-09-20 修**：match 的 codegen 改成 if/else 链（`while true` + `break` 的读行循环以前**当场死循环**）✓ | — | 小 |
| 🅿️ **14** | 🟡 **收窄不认路径（= 参数根）**（**2026-09-23 复核后降级：这是「已知边界」，不是待办**——⚠️ **不做别名分析**，主人 2026-09-23 拍板「其实我也一直不打算做」✓） | ✅ **局部根可以**（根是**没取过地址的局部** ⇒ 没人能通过别名改它 ⇒ 事实稳定 ✓，`examples/path-narrowing.extc`）· 🅿️ **参数根不做**：调用者可能持有别名 ⇒ 事实不稳定 ⇒ 保守拒（`tests/errors/path_narrow_param_root.extc`）✓ **为什么不修**（三条依据，都实测过）：① **没有唯一性可推** —— extC 在设计上**允许别名**（`mut ref` 不承诺唯一，`SPEC.md` §0.6「两个 mut ref 指同一块」是明写的特性）⇒ 任何 sound 的分析都得从保守假设出发，**精度涨不了多少、误报会涨** ✗；要真有唯一性就得引入线性类型 = 另一门语言 ✓ ② **最小可用版本已经做了** —— `ARENA-FORMAL` §7.4 说硬门槛的"最小可用版本就是**取地址跟踪**"，而 `Sym.addressed` 早在用（局部根收窄 + 字段级强更新都靠它）✓ ③ **需求接近零** —— 全仓库用到 `?ref` 的语料里，只有 `examples/path-narrowing.extc`（**演示该语法本身**）与它的配对反例用到；`stdlib/` 与真实程序**一处都没有** ✓ **合法替代**：先 `var cur: ?ref node = ref *p` 拿到本地再 `while cur != null { … }`（实测可用，输出正确 ✓；`MANUAL` §7.6 那条「收窄只认绑定名」就是这么说的 ✓）⚠️ **将来真要动的话，不是做别名分析** —— 而是照 `#43` 那个现成模式把"收窄义务"**挪到调用点求解**（便宜一个数量级）；但还是**先量需求**再决定 ✓ || UX | 部分 |
| ~~**45**~~ ✅ | **`?ref T` / `null` / 收窄**（⚠️ 原表里这一行**跟 #15 撞号** ⇒ 2026-09-22 复核时改成 #45 ✓ 「编号唯一」这条规矩自己也得查）| **2026-09-20 完成** ✓ 见 MANUAL §7.6 | — | — |
| ~~**23**~~ ✅ | **没有显式转换的语法** | 报错一直说"有损转换**必须写出来**"，但**根本没那个语法** ✗（写 RNG 时撞上：`nextFloat`/洗牌都要它）⇒ **2026-09-20 补上 `T(x)`**：整数收窄/换符号/浮点转整数**装不下就 trap（带位置）**，能证明的零检查；浮点之间按 C（就近舍入/±inf）。⚠️ 语法选 `T(x)` 而非 C 的 `(T)x`：**parser 不查符号表**，`(T)x` 与括号表达式二义 ✓ | — | — |
| ~~**25**~~ ✅ | 🐛 **`p[..]` / `p[i]` 切 `ref [N]T` ⇒ 生成的 C 编不过**（**2026-09-20 修**） | `fn ok(p: ref [8]u8) -> box<slice<u8>> { var b: box<slice<u8>> = { v: p[..] }  return b }` 检查器**放行**（对 ✓）但 codegen 出的 C 让 gcc 报 `'p' is a pointer; did you mean to use '->'?` ✗（`p[..]` 切的是 **`ref [8]u8`**，生成的 `.data` 访问打在指针上了）—— **"生成的 C 编不过"是最该防的一类** ✓ 用例留在 `tests/canary-gaps/`（故意不并进套件，缺口有据可查）|
| ~~**26**~~ ✅ | **canary 第一批（携带引用维度）** | 5 对用例当场抓出 **4 个假阳性**（`var h: holder = { n: ref *p }  return h` 这类"从参数来的引用装进 struct/数组/泛型再返回"全被误拒 ✗）= PLAN #8 的另一半 ⇒ **已修**：含引用的值绑定也跟踪 `refDepth`（声明/赋值/元素写 三处，取 max 上界），零初始化按 0（里面只可能是 null ✓）⚠️ 修的过程中**自己开了一个洞**（`a[0] = ref local` 是**换指向**路径，更新没跟上 ⇒ `return a` 被放行 ✗），又靠同一条 canary 抓住 ⇒ 记进 #26 当"canary 的价值"实证 ✓ |
| ~~**27**~~ ✅ | **codegen 的实例集合没做传递闭包**（2026-09-21 修完）| 方法签名里提到的实例（`varArray<i64>::get() -> option<i64>`）如果没被"直接用到过"就不生成 ⇒ 生成的 C 报 `unknown type name option_i64` ✗。修法：`units` 收集之后跑**传递闭包**（扫**字段 + 方法签名 + 枚举载荷**，泛型实例**代入 T**），带**轮数上限**（超了**响亮报错**，不许静默少生成 ✗）✓ 证据：`examples/instance-closure.extc`（新编译器 `size = 1` vs 旧编译器 `C compiler failed`）✓ 顺带把五处 "逼出 option_*" 的探针**全删了**（它们本来就是绕它的）✓ | — | — |
| ~~**28**~~ ✅ | **`ttSubstitute` 不认泛型枚举实例**（真 bug，已修）| 第三刀把 `option`/`result` 变成**泛型枚举**之后 `ttSubstitute` 没跟上（枚举实例 kind 是 `TY_ENUM`，掉进 `default` 原样返回）⇒ 泛型实例的方法返回 `option<T>` **没代入** ⇒ 用户看到 `expects option_i32, found option_T` 这种没法理解的报错 ✗（同一个坑的另一半早前修过：`typeContainsRef` 的枚举分支）⚠️ **教训**：加一种类型构造时，`ttSubstitute` / `ttEquals` / `typeContainsRef` / `ttRender` 这一族"按 kind 分派"的函数**全都要扫一遍** ✓ 连带的 codegen 一点：裸构造器把模板名（`option_T`）当 C 名字用 ⇒ 改成走 `subst` + `cType` ✓ | — | — |
| **29** | 📌 **`varArray<T>` 分块化（chunked）—— 留 v2 拍板**（定案 60：**是新类型，不是替代品** —— `varArray` 永远保持连续；连续性要靠 `asSlice`/`asMutSlice` 兑现，但那被 #31 挡着）| 定案 59 算出来：**在"不能 free"的 arena 模型里，分块严格优于倍增** —— 累计分配 `⌈n/c⌉·c ≤ n + c`（chunk=256 ⇒ 1.001n）vs 倍增的最坏 4n，零拷贝，而且**块永不搬** ⇒ 顺手解决 §7.9 那条"扩容让元素引用失效"✗ 代价：存取多一次间接寻址 + **不再连续**（`slice<T>` 给不出来）⇒ 跟 `insertAt`/`eraseAt` 一起拍 ✓ 验算程序 `examples/growth-factor.extc` | 设计（内存 + 引用稳定性）| 中 |
| **30** | 📌 **"运行时原地扩"—— 记着，不选**（定案 59 §四）| 对立面是"运行时自适应"：`realloc` 式先试原地扩、不行才搬（libc / Go `growslice`）。我们的 arena 是 **bump 分配器** ⇒ 只要"这次分配还是当前块的最后一次分配"，原地扩 = `used` 往后推（零拷贝、一次指针比较）⇒ 要补：arena 记"最后一次分配 + 大小"、块留 slack（现在 `cap = n` 没余量）✗ 不选的原因：它把"引用失效"从**必然**变成**看运气**（更难解释），而且是运行时代码（不违反 P，但按 P′ 精神该让用户知道）⇒ chunked（#29）目标相同且**永不失效**，优先 ✓ | 设计备选 | 小 |
| ~~**31**~~ ✅ | **能拿 UB：把"被调者分配出来的存储"带出本函数**（2026-09-21 **全部收掉**）| 两个形状都实测过（ASan 实锤 use-after-free）。**最终修法 = 让 checker 用定理**（`ARENA-FORMAL` §3/§8）：① 算 `Addr`/`Cont` **效果摘要**（含**惰性传递闭包** + "是否完整"位）② **只在 `Addr` 全空且摘要完整时**才跳过规则 ④ ③ 家 arena 按 **E 分析**（会不会被搬出本函数）选 ⇒ 存储落在**调用者的家**里 ✓ 甲′ 的 `raiseMutRefTargets` **已删** ✓ 双向判据：三个安全形状进 `examples/`（ASan 干净）✓ 攻击库基线**一字不动** ✓ golden **只多三个新例子** ✓ | **soundness（UB）** | — |
| ~~**32**~~ ✅ | 📌 **有家函数里 `new` 一律进家**（**2026-09-23 实测复核：已修**） | `arenaRefAt(g, …)` 只看 `hasHome` ⇒ 有家函数里**任何** `new` 都进家 arena ⇒ 循环里累积到函数结束（#24 的紧致性在"自己的 `new`"这一侧丢了）✗ 同一份 E 分析本来能精确化（`cell ∉ E` ⇒ 进当前块），✅ **2026-09-22：「层号权威」这一半已经做掉了**（定案 68：检查器在闭包跑完之后用 `FuncDef.arenaSites` 统一定案，codegen 的 `hasHome` 兜底整个删掉 ⇒ **#32 不再是"要先解一个死结"，而是一个独立小步** ✓）⚠️ 剩下的是**精度**那一半："我调的人有家 ⇒ 我也有家"是查完所有函数体才算的 ⇒ 那种函数里 `new` 明明没逃逸、也得活到调用者那块结束（A3 的老行为，**费内存但不误拒也不漏 UB** ✓）⇒ 想让"其实没逃逸的 `new`"回到块层，得把 E 分析（`escapees`）也搬到闭包之后 —— **现在权威已经统一，这步是独立的** ✓ 判据（2026-09-23，看生成的 C）：非逃逸的 `fn helper()` ⇒ 签名是 `helper(void)`（**没有家**）、循环里的 `new cell` 生成 `extc_arena_alloc(&__extc_a[2], …)`（**块级** ✓）且每轮 `release(&__extc_a[2])`；而真逃逸的 `fn build() -> ?ref cell` ⇒ 签名是 `build(extc_arena *__extc_home)`、`new` 进 `*__extc_home` ✓ ⇒ 原文说的「有家函数里**任何** `new` 都进家」（精度那一半）**已经不成立**；⚠️ 那半是 2026-09-22「层号权威」统一（定案 68）之后顺带解掉的，但**这一格没被划掉** ✗（跟 #42 是同一种过时：**实现改好了、记账没跟上**）✓ | 内存 | 小~中 |
| ~~**33**~~ ✅ | **"被调者会不会分配"算得太晚**（2026-09-21 修完，跟 #31 一起）| `needsHome` 的传递闭包跑在**所有函数查完之后**，而调用者查自己函数体时就得知道答案 ✗ ⇒ `varArray::push` 自己不 `new`（是它调的 `grow` 才有）⇒ 甲′ 抓不到 varArray 那条。修法：**惰性**的 `funcAllocates`（`stmtHasNew` 或调用会分配的函数；方法按 `e->func` 递归；**环保护**：正在算的当"会"；拿不准（`func` 为空）也当"会"；结果缓存在 `FuncDef.allocState`）✓ ⚠️ 踩坑：第一版把调用插在 `else` 分支里，而 `self: mut ref` 那条走的是 `if` 分支 ⇒ **一次都没执行**（调试才看出来）✗ | soundness | — |
| ~~**34**~~ ✅ | **甲′ 的假阳性：容量足够的容器其视图也不能返回**（2026-09-21 随 #31 一起消失）| 根因是"规则用错了东西"：旧规则把**地址流**当非空地施加于所有实参（`ARENA-FORMAL` §2.3）⇒ 摘要 + E 之后，"容量够不够"根本不需要证明 —— `varArray-asSlice-return.extc`（**扩容过**的容器返回视图）现在是**正例**且 ASan 干净 ✓ | 精度 | — |
| ~~**12**~~ ✅ | 📌 **文档债**（**2026-09-22 收掉**）| 原来记的是「`;` 和 `/* */` 其实早就可用，但没写进 MANUAL；块注释**不嵌套**也没说」。**核查结果**：前两条**早就写了**（MANUAL §2 词法表：`分号可选，写了会被忽略` / `/* 可以跨行 */`）✗ —— 又是一行**过时**的记账（跟 #27 那次同一个毛病 ✗）⇒ 真正欠的只有"不嵌套"那一句，已补进 MANUAL §2 ✓ 实测：`/* 外层 /* 内层 */ 后面 ` ⇒ 内层 `*/` 就结束了，后面当代码解析（跟 C 一样）✓ | 文档 | 一行 |
| ~~**35**~~ ✅ | 🐛 **`alloc<T>(n)` 不算"用了 arena"**（**2026-09-22 撞到并修掉**）| `alloc<T>(n)` 是**内建原语**（AST 里是 `EX_GENCALL`，检查器保证只有它走这条路），而 `mayUseArena` 的判据 `exprHasNew` 只认 `new` ⇒ **只在内层块里分配**的函数被判成"不用 arena"（连 `extc_arena __extc_a[N]` 都不声明），可块里照样吐 `&__extc_a[2]` ✗ ⇒ 生成的 C **根本编不过**（gcc: `__extc_a` undeclared）。修法**一行**：`exprHasNew` 加 `case EX_GENCALL: return true;`（`stmtHasNew` 的三个用处——`mayUseArena` / `needsHome` / `funcAllocates`——语义上本来就都该算它 ✓）回归用例 `examples/alloc-in-block.extc`（旧编译器**编不过**、新的输出 `和 = 12`）✓ ⚠️ **教训比 bug 值钱**：`tests/arena/control-flow.extc` **从头到尾就是这个形状** ⇒ 它**从来没跑起来过**，而验收脚本只 grep "out of arena memory" ⇒ **A2 的验收一直是空转的** ✗ 脚本已同步收紧（编译报错也算 FAIL）—— **"没报错"不等于"跑过了"** | 真 bug（生成的 C 编不过）+ 测试空转 | 一行 |
| ~~**36**~~ ✅ | 🐛 **`T::assoc(...)` 不算"要 arena"**（**2026-09-22 撞到并修掉**）| `exprCallsNeedsHome` 只认 `EX_CALL` / `EX_METHOD`，**漏了 `EX_ASSOC`** —— 可 `T::assoc(...)` 的 `e->func` 明明是填好的（check_expr.c 那条对 assoc 一样生效）✗ ⇒ **非 main** 的函数里调"会分配的关联函数"（`varArray<i32>::withCap(1)`、`bufT<i32>::make(4)`）被判成"不用 arena"，而调用点照样吐 `&__extc_a[k]` ⇒ 生成的 C **编不过** ✗（复现：`fn helper() { var b: bufT<i32> = bufT<i32>::make(4) }`）。⚠️ **这个洞一直只对 `main` 被 `mayUseArena` 里那句 `\|\| 是 main` 遮住** —— 而那句兜底的代价是**主函数永远白带一整套 arena 样板**，于是每个 `while` 体都吐 `extc_arena_release(...)` ⇒ 实测矩阵乘 **103 ms → 34 ms（3.0×）** ✗。修法：把 `EX_ASSOC` 加进那个判据，然后**把那句兜底删掉** ✓ 回归用例 `examples/new-generic.extc`（补了非 main 的 `makeAndFill`）| 真 bug（生成的 C 编不过）+ 性能 3.0× | 一行 |
| ~~**38**~~ ✅ | 🚨 **块内 `new` 存到块外 ⇒ 悬垂**（**2026-09-22 修**：定案 63「**提升，不拒绝**」，主人拍板）| 形状：`var head: ?ref node = null` + `while cond { var n = new node  n.next = head  head = n }` ⇒ 检查器放行、`n` 分配在**循环体那只 arena**（`__extc_a[2]`）、每轮 release，而 `head` 还指着它 ⇒ **ASan 实锤 heap-use-after-free** ✗（同族的嵌套块那条是**误拒**："can only hold up to depth 0"）。⚠️ 根因：**"存进活得更久的地方"这条规则只对"借来的值"（参数派生 / 调用结果）生效**，而 `new` 既不是参数派生也不是调用结果 ⇒ 整条被跳过（PLAN #31 修的是"被调者分配的东西"，这是同族的另一半）。**修法**：`arenaLevel = min(当前块深度, 所有目的地的层)` —— 值被存进更外层就把那只 arena **提升上去**（最坏 = 函数级、自动清理的堆；主人："没必要拒绝"）✓ 落地：`Expr.arenaLevel` + `promoteInto`（顺着值的**来路**往回走：绑定 / `!` / `??` / 结构体字面量 / 枚举载荷 / 数组字面量；**提不动的照旧报错** ⇒ 永远安全方向）+ 赋值/换指向/实参三条边 + `arenaRefAt`。⚠️ **路上挖出三件事**：**(a)** `placeDepth(引用型绑定)` 答的是"它**指着**的地方在哪"（初值 `null` ⇒ 0），不是"**往哪一层存**" ⇒ 新增 `storeLayer`（定案 ㊲ 那张表欠了两轮的那一半，顺手补齐）；**(b)** 检查器算层数时 **`needsHome` 的传递闭包还没跑** ⇒ 有家函数里算出来的层数不可信 ⇒ codegen 以 `hasHome` 为准（**golden 当场抓出来**：`out-param` 的分配从家 arena 掉回块 arena ✗）；**(c)** 来路链会**断**（外面的 `head = mid` 查到时 `mid` 经手的 `cell` 已出作用域）⇒ 记来路时**压平**（`noteOrigin`）+ 跳数上限（绑定可成环 `a = b  b = a`）✓ **双向判据**：`tests/asan/`（**新的一支常设验收**）5 个形状 ASan 干净，拿**修之前**的编译器跑 ⇒ **2 个 UAF + 3 个误拒**（证明这支验收真的会咬 ✓）· 攻击库基线**一字不动** · 测试 224 → **227** 全通 · golden **只多一个新例子**（`examples/store-promotion.extc`；其余 81 个逐字节不变 ⇒ 老代码生成物一个字节没动 ✓）· `tests/arena/promote.extc` 同时验"提升按值判"（循环里 300×1MB 不涨 + 被提升的链表活到函数结束）✓ | **soundness（UB）** | 中 |
| ~~**39**~~ ✅ | 🚨 **引用型绑定的「字段表」会把 `refDepth` 刷低 ⇒ 悬垂**（**2026-09-22 修**，修 #38 的路上实测出来的；**修之前就有**）| 形状：`var x: node`（深度 2）+ `var n: mut ref node = ref x` + `n.next = null` ⇒ 那句"字段写记录"走 `refreshRootDepth`，把根的 `refDepth` 重算成 `max(各字段)` = **0**（"指向活到永远的东西"）✗ ⇒ 下一句 `keeper = n` 被放行，而 `x` 出块即死 ⇒ **ASan 实锤 stack-use-after-scope** ✗ 根因：**档2.3 的字段表是给"结构体绑定"设计的**（`h.p = …` ⇒ 根的有效深度 = max(各格)），可**引用型绑定**上 `n.next = …` 写的是**被指对象**的字段 ⇒ 两回事混用，把"我指着谁"覆盖掉了 ✗ 修法（**11 行**）：`noteFieldDepthWrite` 里对**引用型根**记下旧值，`refreshRootDepth` 之后**并回 max**（只许往"更长命"调，不许降）—— 非引用型的结构体绑定照旧可以降（那正是档2.3 强更新的用处 ✓）证据：那个用例从 `tests/canary-gaps/`（缺口）挪进 `tests/errors/`（`ref_field_write_keeps_pointee_depth.extc`）✓ · `examples/field-strong-update.extc` 照旧 ✓ · 攻击库基线一字不动 ✓ · golden **逐字节不变** ✓ | **soundness（UB）** | 11 行 |
| ~~**40**~~ ✅ | 🐛 **`(*p).f = v`（透过 `mut ref` 写字段）被误拒** —— 报的还是**误导人的**话（**2026-09-22 修**，跟 #41 是同一刀）| `fn clear(cell: mut ref node) { (*cell).next = null }` ⇒ `error: cannot rebind this: the binding is read-only` ✗ 可 `cell` 明明是 `mut ref` ✗ 根因：换指向那一支的 `slotOk` 只走 `placeRoot`，而它对 `*p` 返回 NULL —— 同一个坑 2026-09-20 修过**裸 `*p`**，`(*p).f` 漏了 ✗ 修法：新增 `pathRefsAllMut`（回答「写这个「地方」要**穿过**哪些引用、每一只是不是 `mut ref`」：显式 `*p` ✓ 隐式 `p.f`/`v[i]` ✓；**目标自己的类型不算** —— `cur = v` 写的是槽位 ✓）+ 报错分成两句（"写穿只读引用" vs "绑定只读"）✓ 正例 `examples/ref-field-write.extc`（ASan 干净）✓ | 误拒 + 错误信息误导 | 小 |
| ~~**41**~~ ✅ | 🚨 **写穿「只读」引用整条没被挡**（**2026-09-22 修 #40 时顺手实测出来的**，反向的洞）| 形状：`fn g(p: ref node) { (*p).val = 7 }` / `fn h(p: ref node) { p.next = null }` ⇒ **都编译通过** ✗（`ref T` 是只读借用，改 pointee 的字段/元素就是写穿它 ✗）根因：`pathHasReadonlyRef` 走到 `EX_DEREF` 就 `break`，而 DEREF 自己的类型是**被指对象**（不是引用）⇒ **那只引用的 mut 从来没查过** ✗；而隐式那条（`p.f`）走的是换指向那一支 —— 那一支**根本不查**只读引用（只看 `placeRoot` 的 `var`）✗ 修法：① `pathHasReadonlyRef` 补上 DEREF 那一支（显式穿过）② 换指向那一支改用 `pathRefsAllMut`（隐式穿过 ⇒ 必须 `mut ref`）✓ 反例 `tests/errors/write_through_readonly_deref.extc` + `write_through_readonly_field.extc` ✓ ⚠️ **差点自己弄坏**：第一版我把 `requireMutable` 里的 `pathHasReadonlyRef` 整个**换掉**了 ⇒ `tests/errors/call_writer_on_readonly.extc`（只读接收者调会写的方法）**从被挡变成通过** ✗ 是那一支反例当场抓出来的 ⇒ 改成**扩展**而不是替换 ✓ | **soundness（只读契约）** | 小 |
| ~~**47**~~ ✅ | **泛型自由函数 `fn f<T>(…)`**（**2026-09-22 落地** = 定案 71）（**2026-09-22 讨论库时确认的缺口**）| `sort`/`map`/`max` 这类**没法挂在某个 struct 上** ✗（实测 `fn maxOf<T>(a: T, b: T) -> T` = `expected '(' , found '<'`）。现状：`struct`/`TypeDef` 有 `typeParams` ✓、实例驻留表/mangle/每实例复查（`RefCheck`/`EqCheck`）全都在 ✓，**只有 `fn` 没有** ✗ 要做：① parser 认 `fn name<T,…>` ② `FuncDef.typeParams` + 把 ~8 处 `f->owner->typeParams` 换成 helper ③ **函数实例表**（key = (FuncDef*, targs) ⇒ mangle 名）+ 调用点**类型推导**（结构合一）④ codegen 每实例吐一份（平行于类型实例那个循环）+ #27 的 units 闭包一起走。估计 **250~350 行**；风险点：`T` 只出现在返回类型时必须显式 `f<i32>(…)`；且跟 **#44**（摘要按实例）是同一片地盘 ⇒ 建议一起做 ✓ | 能力缺口（库的根）| 中 |
| ~~**50**~~ ✅ | 🟡 **调用点按实例解析**（#44/#47 的共同尾巴）（**2026-09-23 实测复核：①② 都已完成**） | ① **泛型体里能调泛型函数** —— 加了「推迟的调用点」表（`CallCheck`，跟 `RefCheck`/`EqCheck` 同模式）：模板期先造一个"以 `T` 为实参"的实例让模板体自洽，实例化复查时按外层实例代入、把 `e->func` **改指**到真实例 ✓ 实测：`examples/generic-calls-generic.extc` ⇒ `7 42 7`（自由函数实例 + **类型实例的方法体**两条路）✓ ② **摘要按实例** = **#44**，也已修 ✓ 实测：`EXTC_DUMP_EFFECTS=1` 下同一个 `stash` 的两个实例给出 `Cont=0x2`（`boxT<slice<u8>>`）与 **`Cont=0x0`**（`boxT<i32>`）—— 两张不同的表 ✓ ⚠️ 行内原来记的那个小瑕疵（`==` 报错位置指向 **prelude** 的行号）**仍开着**，但它跟 `#42` 那格记的是**同一件事** ⇒ 已经记在 #42 那一行，不在这里重复计数 ✓ | 能力缺口（①已解）+ 精度（②）| 中 |
| ~~**51**~~ ✅ | 🐛 **`let q = f()?` / `var q = f()?` 根本编不过**（**2026-09-23 实测发现并修**）| `?` 有**四个**合法位置（`SPEC.md` §6.2），可 codegen 只在 `ST_ASSIGN`/`ST_RETURN`/`ST_EXPR` **三处**展开 ✗ ⇒ 声明初始化那一处落到 `genExpr` 的 `EX_TRY` 分支 ⇒ 报 `internal: '?' reached expression codegen (position check missed it)`。而 checker **是放行的**（`check_stmt.c` 里它走 `checkTryInner`）⇒ 两边不一致 ✓ 修法照 `ST_ASSIGN` 那条的形状补一处（声明 + 赋值，本来就一样）✓ ⚠️ **为什么长期没被发现**：语料里**一个用到这个位置的例子都没有**（`grep` 不到）—— `tests/errors/try_bad_position.extc` 只能钉"**嵌套**位置要被挡"，管不了"**合法**位置能不能用" ✗ ⇒ **反例库对"合法写法"是盲的**，那半边只能靠正例覆盖 ✓ 判据：新正例 `examples/try-positions.extc`（四个位置一次全验）· 测试 250 → **251** · golden 只多那一个新例子（其余 92 个逐字节不变 ✓）| 真 bug（生成的 C 编不过）+ 语料盲区 | 小 | 的想法：**可选编译期优化**）| 技术成立且挂钩点现成（`tt->instances` 驻留 + `ttMangle` 确定性命名 + 每实例独立生成 + `used` 门控 ✓）。⚠️ **但实测单态化不是瓶颈**：`bench/compile` 大程序 38 039 行生成 C 里**泛型实例只占 11.5%**，前端 29 ms、**gcc 681 ms（96%）** ⇒ 真正的病是"吐出来的 C 又大又啰嗦"（9.5 倍于同形手写 C）✗ ⇒ **前置顺序**：① `--no-line-map`（实测行数 −17%、gcc −7%）② 精简生成的 C ③ ccache 式产物缓存（零语言改动 ✓）④ 才轮到每实例 `.o` + 接口摘要（代价：丢跨实例内联 ⇒ 要 LTO、缓存键要含版本/旗子/平台、陈旧产物要校验、文件多）✓ | 编译时长（可选优化）| 中 |
| **48** | 📌 **按实例/按模块的编译产物缓存**（主人 2026-09-22 的想法：**可选编译期优化**）| 技术成立且挂钩点现成（`tt->instances` 驻留 + `ttMangle` 确定性命名 + 每实例独立生成 + `used` 门控 ✓）。⚠️ **但实测单态化不是瓶颈**：`bench/compile` 大程序 38 039 行生成 C 里**泛型实例只占 11.5%**，前端 29 ms、**gcc 681 ms（96%）** ⇒ 真正的病是"吐出来的 C 又大又啰嗦"（9.5 倍于同形手写 C）✗ ⇒ **前置顺序**：① `--no-line-map`（实测行数 −17%、gcc −7%）② 精简生成的 C ③ ccache 式产物缓存（零语言改动 ✓）④ 才轮到每实例 `.o` + 接口摘要（代价：丢跨实例内联 ⇒ 要 LTO、缓存键要含版本/旗子/平台、陈旧产物要校验、文件多）✓ | 编译时长（可选优化）| 中 |
| **49** | 📌 **生成的 C 比同形手写 C 大 9.5 倍 ⇒ gcc 时间 3.2×**（2026-09-22 量 #48 时顺手量到的）| 同一个程序：手写 C **4 008 行** · gcc **210 ms**；extC 生成 **38 039 行** · gcc **681 ms** ⇒ **3.2×** ✗ 体积分解（实测）：`#line` 17% · 空行 8% · arena 样板 8% · 每个方法"原型 + 定义"两行（1000 个方法 = 2000 个函数符号）。⇒ 值得单独量一次"哪一类行最贵"（是行数？是函数个数？是冗长的类型名？）**然后才动手** ✓ 这直接影响 `bench/compile` 那一维 ✓ | 编译时长 | 中 |
| ~~**42**~~ ✅ | 🟡 **嵌套容器写不出来**（**2026-09-23 复核：全部解了**）| ① `needs `T: ==`` —— **已修**（给 prelude 的 `varArray<T>` 补了 `fn ==` ✓）② **`cannot store a borrowed value…` —— 也已解** ⚠️ 这一格原来写「借用规则本身，拒绝是对的 ✗」**已经过时了** ✗：那条路是 `#43`（**2026-09-22 落地**：被调者只**发布约束**、**调用点**代入具体深度求解）—— 而 `#43` 恰好就是本格自己写的出路之一「把借用规则挪到调用点（= 大活，另记）」✓ ⇒ **`varArray<varArray<i32>>` 现在能写了**（`examples/container-nested.extc` 正例 ✓），同族的 `varArray<slice<u8>>`（字符串表）· `varArray<struct>` 也都能写 ✓ **2026-09-23 实测**：三类嵌套一次跑通；而**真危险**的形状**仍然被挡**，而且报错极清楚：`outer.push(deep[..])` ⇒「argument 2 of `push` carries a reference into a deeper scope (depth 2) than the place the callee may store it (depth 1)」✓（❌ 原来建议的"出路：扁平 CSR / slab + i32 索引"**不再需要**了 —— 那是②没解时的权宜之计 ✓）✅ **#42(c) 已做**：实例化只复查/生成"真被调到的"方法 ⇒ ①任何 struct 进容器不用再手写 `fn ==` ✓（`examples/container-of-struct.extc`；真去比容器时才要求元素可比较 —— `tests/errors/eq_needs_element_eq.extc` ✓）②生成 C 顺带少了 28%（44622 → 31977 行 ✓）③端到端编译 N=500 496 → 376 ms（−24%）✓ ⚠️ 小瑕疵（仍记着）：`==` 报错的行号指向 **prelude** 的行（路径却是用户文件）✗ | 缺口/误拒 | 中 |
| ~~**43**~~ ✅ | ⭐ **借用规则挪到调用点求解**（**2026-09-22 已落地**，主人拍板"大活儿修了"；设计+实测 = `ARENA-FORMAL` §9 + 定案 67）| 那条"借来的值不可外存"的 blanket 规则**必然误拒** ✗（被调者一次编译、不知道调用者传的是长寿还是短命）⇒ 挡了 `varArray<slice<u8>>`（字符串表）· `varArray<varArray<T>>`（动态邻接表）· 通用 `stash(dest: mut ref T, v: T)` 一整族 ✗ 修法：**被调者只发布约束（摘要 ✓ 早就算好）、调用点代入求解** ✓ 新增 `Cont(j) ⇒ exprRefDepth(实参 j) ≤ h` · 摘要不完整/带环 ⇒ **调用点保守拒**（不可分割的一半 ✗）· 被调者侧只对"**来路追得到参数**"的值放行 ✓ **实做**：`checkCallRefArgs`（两个检查**各自**决定跳不跳：`addrMaybe`/`contMaybe`）+ 被调者侧 `!destIsParam \|\| !valTracesToParam` 那一条 ✓ **路上撞出四件事**（详见 §9.5）：① 共用一个 early return ⇒ `list-return` 误拒（已拆）② **方法调用从来没跑过规则 ④**（`params` 有 `self`、`args` 没有 ⇒ 长度不等就早退 ✗）⇒ 接收者前置成 arg 0 ③ **自由/关联函数的检查跑在实参查类型之前**（`a->type` 还是 NULL ⇒ **静默跳过** ✗）⇒ 三处全部挪到实参检查之后 ④ 模板里 `T` opaque ⇒ `carrier` 判据补 `mentionsParam`（保守 ✓ 代价 = 摘要**按模板一份而非按实例一份** ⇒ 另立 **#44**）✓ **双向对拍**（`git worktree` 建 HEAD 副本当老编译器）：两个新正例 + 同层存引用 = 老编译器**三条全误拒**、新的**全过且 ASan 干净**；`stash_view_from_deeper`/`ref_arg_too_deep`/`borrowed_into_param_place`/`ref_launder_field`/`ref_launder_deref`/`generic_borrowed_store` = **一条不少全挡** ✓ ⚠️ **判据 1 诚实偏离**：攻击库基线**多了一条 `h8_launder`** —— 它本来就是**同层安全**的程序，老编译器误拒、攻击库却把它当"挡住了"记功 ✗ ⇒ 现在按"真的安全"记账，另立危险变体 `tests/errors/stash_view_from_deeper.extc` 顶住安全边界 ✓（**"挡住"必须同时是"真的不安全"**，否则那行账是假的）✓ 结果：`./check.sh` **8 通过 0 失败** · 测试 241 → **244** · 攻击库 8 条已知安全 + 其余全挡 ✓ · golden 差异 = **只多两个新例子**（其余 86 个逐字节不变 ⇒ 生成的 C 一字节没动 ✓ 纯检查器改动）| **soundness/误拒** | 大 |
| ~~**46**~~ ✅ | 🚨 **编译器自己的 UB：`exprHasCall` 从函数末尾掉出去**（**2026-09-22 做定案 68 时顺手修掉**）| `case EX_CALL/EX_METHOD/EX_ASSOC: if (callIsEffectful(…)) return true; **break;**` —— `break` 跳出 switch 就**落到函数末尾**，而那个函数返回 `bool` ⇒ **返回一个不确定的值** ✗（`-Wreturn-type` 一直在报，只是没人看那份 warning ✗）。后果：PLAN #22 那条"本语句里前面有没有**留在原位**的调用"的判据**时灵时不灵** ⇒ `??` 的临时变量顺序检查可能漏报也可能误报 ✗ 修法一行：`return false;` ✓ ⚠️ **教训**：那条 warning 是 2026-09-22 早上做 #22 时**自己写进去的** —— "改了逻辑没看完 warning"是最便宜的一次翻车 ✓ | 编译器自身 UB（判据失效）| 一行 |
| ~~**44**~~ ✅ | 🟡 **效果摘要按实例（单态化时）算**（**#43 的尾巴**，2026-09-22 主人问出来的：*"这个也不是应该在单态化的时候判断吗"*）（**2026-09-23 修**）| 摘要原来**每张模板只算一份**（`collectEffects` 在 `checkFunc` 里跑，那时 `T` 是 opaque ✗）⇒ `carrier` 只能补 `mentionsParam(T)`（**含类型参数就当带引用** ✓ 保守 ✓）⇒ **代价**：`varArray<i32>` 这种**完全不带引用**的实例也背着 `Cont` 位 ⇒ 调用点对它一律走"最坏情况"那条 ✗ **修法（两处）**：① `carrier` 那行改成看 `tsub(c, e->type)`（代入后的类型）② 复查 pass 里对**每个实例**清空继承来的摘要 + `callees`/`effState` 并用代入上下文**重算一遍** `collectEffects` ✓ ⇒ 同一模板、不同实例拿到**不同的表** ✓ ⚠️ **绝不能**用"删掉 `mentionsParam` 那一句"代替 —— 实测过：删掉之后 `tests/errors/generic_borrowed_store.extc` **从"被挡"变成"通过"**（真悬垂）⇒ 那句**是堵洞的** ✓ **判据（A/B 对照，掩码级）**：`EXTC_DUMP_EFFECTS=1` 下 `boxT<i32>::stash` = `Cont=0x0`、`boxT<slice<u8>>::stash` = `Cont=0x2`；把 `tsub` 撤回 ⇒ **两行都变 `0x2`**（老行为）⇒ 可归因 ✓ · 新正例 `examples/generic-effects-per-instance.extc`（自带 dump 命令）· `./check.sh` **14 节全绿** · 测试 249 → **250** · **golden 逐字节相同**（92 文件 ⇒ 纯摘要精化、**行为零变化** ✓）· 攻击库基线一致 · 红线仍被挡 ✓ ⚠️ **诚实记账**：这是一处**潜在精度**（`PLAN` 自己也写着"今天不误拒"，实测确实**构造不出被误拒的形状**）⇒ 它**不改变任何可见行为**，所以判据只能落在"掩码变了 + 行为没变 + 红线还在"这三条上 ✓ | 精度（潜在）/ 一致性 | 中 |
| ~~**37**~~ ✅ | 📌 **生成的 C 全用外链 ⇒ gcc 拒掉外层循环向量化**（**2026-09-22 修**，见 DEVLOG）| extC 只吐**一个 .c**（单 TU），可所有函数/全局都是**外链** ⇒ gcc 必须假设"别处还会调、指针会别名" ⇒ `-fopt-info-vec-missed` 报 `unsupported outerloop form`，**外层循环向量化被拒** ⇒ OI 数量级矩阵乘 **660 ms vs 208 ms（3.2×）** ✗。修法：**除了 `main` 全加 `static`**（一个词，白送的还有：跨函数内联、编译时长 **−53%**）✓ ⚠️ 将来若真支持多 TU，这条要改成"只导出被别的 TU 用到的" | 性能（3.2×）+ 编译时长（−53%）| 小 |
| ~~**52**~~ ✅ | 🚨 **逃逸检查放行真悬垂：有家被调者的结果被存到更浅的地方**（**2026-09-23 修**，做 `reader` 时撞出来的）| 判据是"**在块里造、往块外存**"：<br>`while i < 3 { let t = make()  b = t }` —— `make()` 分配进"调用点选的那只 arena"，而调用点在**循环体**里 ⇒ 那只就是循环体那一**块** ⇒ 块一退 `b.p` 就悬垂 ✗ **ASan 当场报 `heap-use-after-free`** ✓ 根因两处：① `exprRefDepth` 对**初始化式是调用**的绑定兜底成 `0`（字段表只在初始化式是**结构体字面量**时才填 ✗）⇒ `d=0 ≤ at=1` ⇒ 放行 ② `promoteInto2` 的"槽位更浅"那一支直接 `return true`，注释写的"里面的东西本来就住那一层"**在有家被调者上是错的** ✗ 修法：绑定时把"有家被调者 ⇒ 住在我传的那只 arena"记进 `Sym.refDepth`（`check_stmt.c`）✓ ⚠️ **为什么这条特别该常设**：**单跑一次不崩**，只有"块里造 + 块外存"才现形 ⇒ 以前 255 个测试、基准、攻击库**全绿而它是坏的** ✗ 判据：新反例 `tests/errors/escape_call_result_to_outer.extc` + 新正例 `examples/reader-home-arena.extc`（隐式 64KB 那形状必须**还能编过**，否则就是把良性的也拒了）· 测试 255 → **257** · **golden 只多一个新例子**（其余 94 个逐字节不变 ⇒ **生成 C 一字节没动，纯检查器** ✓）· `check.sh` 14/14 · 攻击库基线一致 ✓ | soundness（真 UB）| 中 |
| ~~**53**~~ ✅ | ⭐ **表达式位置写不出三段及以上的全限定名**（**2026-09-23 修完** —— 主人下令「**不要随意退出了，我相信你可以的**」⇒ 第六轮拿下 ✓）| **真根因**（前五轮都没走到这一步）：`looksLikeAssoc` 只回答「是不是限定名」、**不回答「有几段」** ⇒ 老 `parseAssoc` 自信地只读 `::` + **一个**标识符就当符号名 ⇒ `std::sys::io::STDOUT` 里它把 `sys` 当符号、游标丢在 `::io::STDOUT` 上 ⇒ 后面才炸 ✗ （**根因在 `parseAssoc`，不在那个「限定名类型块」** —— 前四轮全在改错的地方 ✗）⇒ **修法**：① `looksLikeAssoc` 返回**段数**（不再是 bool）② `parseAssoc` 把**中间段全吃进前缀**、最后一段留作符号 ⇒ 前缀就是**模块名** ✓ ③ 装载器只做一件新事：**全名对全名**匹配 `use` 表（`importedAsPath` —— `UseDecl` 有 `path` 与 `shortName` 两个字段，老 `importedAs` 只认短名 ✗）④ `EX_ASSOC` 加 `isCall` / `modPrefix`（**不发明新节点**：值位置也走 `EX_ASSOC`，装载器照旧改写成 `EX_IDENT` / `EX_CALL`）⑤ `use … as <别名>`（只换 `shortName`、`path` 留全名 ✓）⚠️ **路上我自己踩了四个坑**（全记在 `DEVLOG`）：候选前缀起点算错 · 边界判据写成 `qname[len-1]` 恒假 · **以为「最长的候选」要另外补**（其实 `std::sys::io` 就在 `::` 切出来的候选里）· 深层解析出模块后**又用短名查了一遍** ⇒ 全是我对「前缀从哪来」的误解 ✓ **判据**：新常设验收 `tests/qname/`（**6 正例 + 1 反例 + 1 结构判据**，已接进 `check.sh`）：三段常量 · 三段调用（真写出 `hi!`）· `as` 别名 · **`std::io` 与 `std::sys::io` 同文件共存**（这才是不给别名就必然撞车的那个痛点）· **深层模块的变体位置** · 深层模块的函数调用 · 没 `use` 的深层全名报**全名**并指路 ✓ · 257 测试全绿 · golden **95 个文件逐字节相同**（纯解析器/检查器改动，老语料的生成 C 一字节没动 ✓）| 缺口（功能缺失）| 中（难点在结构） |


### 下一步（**唯一权威的一处** —— 2026-09-23 **逐条实测后重写** ✓）

> ⚠️ 这张表**第六次**被抓出过时 ✗ —— 这次它第 3 行还写着 **#32**，而 #32 **实测已修** ✗
> 而这一版之前它还列着 **#44**（也刚修完）✗
> ⇒ 现在**只列 §0.4 里真开着的那几条**（对着上面那张表**数**出来的 ✓
> ⭐ **2026-09-23 第七次重核**：**真开着的一共 4 条** —— 见下表 ⚠️ —— 也就是说
>   下面那句"真缺陷 0 条"**只对 §0.4 的"缺陷"栏成立**，而 §2 那三格**还挂着旧状态** ✗
>   （**又一处"同一个事实在两张表里各写一遍"** ⇒ 以 §0.4 为准，§2 只留历史映射 ✓）
>   另有 5 条设计/可选/已知边界 #14/#29/#30/#48/#49）
> ⇒ ⚠️ 前几版这里分别写过"6 条"、"5 条 + 1 条设计"、"5 条"、"3 条" —— **每一版都是当时算错的** ✗
> ⇒ **规矩**：改完任何一条缺陷，回来做的**第二件事**就是核这一行（第一件是核 §0.4 的计数行）✓
>
> ⚠️ **测试计数同理**：`#50` 那行当时写"248 → 249"、`#44` 又加一个正例、`#52` 再加
> 一反一正 ⇒ 现在是 **257** ✓
> 这种数字**随手就会过时** —— 每次修完都回来核，别指望它自己同步 ✗
>
> ⚠️ **节数同理（2026-09-23 新加）**：`check.sh` 的 `quick` 是 **13 节**、完整是 **19 节**
> （多出的 6 节都是基准）⇒ 引用时**必须写清是哪个模式** ✗
> `README`/`PLAN`/`extC-overview` 前几版分别写过"7 / 14 / 4 节"，**三种都过时** ✓

**真缺陷：0 条** —— §0.4 里那四条（`#5` 无限递归 · `#9` `allocSlice` · `#10` 裸构造器话术 ·
`#52` 有家被调者结果外存）**2026-09-23 全部修完** ✓
剩下能做的都是**加法**（主线 + 语言能力），见下面两块 ✓

✅ **#53 全限定名已经修完了（2026-09-23）** —— 所以 IO-1 的收尾不再被它挡着：
现在**同一个文件里能同时用 `std::io` 和 `std::sys::io`**（后者加个 `as` 别名即可 ✓）·
三段以上的全名（`std::sys::io::read`）也**能写了** ✓ 见 §0.4 #53 与 `tests/qname/` ✓

**设计决定 / 可选 / 已知边界（不是待办）**：
· **#14 参数根路径收窄** —— 🅿️ **主人 2026-09-23 拍板：不做别名分析**（"其实我也一直不打算做"）✓
  理由三条（都实测过，见 §0.4 那一格）：**没有唯一性可推**（extC 本就允许别名）·
  **最小可用版本（`Sym.addressed` 取地址跟踪）已经在用** · **需求接近零**（`stdlib`/真实程序一处都没用到）✓
  合法替代是一行：`var cur: ?ref node = ref *p` 再收窄 ✓
· #29 `varArray` 分块化 · #30 运行时原地扩 ✓
· **#48 按实例/按模块的编译产物缓存**（**可选优化，不是缺陷**）—— 实测**它在解决一个不是瓶颈的问题**：
  `bench/compile` 大程序里泛型实例**只占生成 C 的 11.5%**，前端 29 ms 而 **gcc 681 ms（96%）**；
  真正的病是"生成的 C 太胖"（**#49**：9.5 倍于同形手写 C）✗ ⇒ #48 自己排了**前置顺序**：
  ① `--no-line-map`（行数 −17%、gcc −7%）② **精简生成的 C** ③ **ccache 式产物缓存**（零语言改动）
  ④ 才轮到"每实例 `.o` + 接口摘要"（代价：丢跨实例内联 ⇒ 要 LTO、缓存键含版本/旗子/平台、陈旧产物要校验）✓

> ⚠️ **#14 为什么从"缺陷"里移出来**：它**既不漏 UB，也不误拒常见写法** ——
> 只是"少了一种糖"（链式 null 检查）。占着"还能拿 UB / 违反承诺的"那一栏会误导读者，
> 以为它是安全问题 ✗ ⇒ 归到"已知边界"，跟 `SPEC.md` §4.5 那类**设计上接受的损失**同档 ✓

**然后就是加法（`SPEC.md` 主线）**：**IO-1**（`open` + 文件帧拥有 + `main(args)`）——
IO-0 主体**已落地**（2026-09-23 定案 74：`reader` + `nextInt` 一族 + 三条路 ✓）
→ `allocSlice` → `for` → lambda → 协议补全 → 模块/`@main` —— 一路到**五子棋能跟人下**那个里程碑 ✓

⚠️ **这块地方踩过三次**（记账）：① 原来写着「31 → **27** → 1 → 2 → 3」，可 #27 早在 09-21 就修完了
（`99cefa2`）⇒ 一句过时的"下一步"被原样抄了一遍 ⇒ 汇报错一轮 ✗ ② 复核时又发现 #3/#8 早就修好了、
#11old/#13old 是重复行、§2 那张同编号的表里 #4/#5/#6 也早修完了 ✗ ③ #43 落地时**又**发现本表还列着
#22/#6（都修完了 ✗）
⇒ **教训：这种"顺序/状态"行必须跟各行状态一起核**，而且**只写一处** ✓
⇒ 再加一条（这次真做到了）：**改完任何一条缺陷，第一件事就是回本表擦掉它** ✓

---

## 0.4.1 📌 **精度账与边界账（不是缺陷，但也不能忘）**

> 这一节收的是**"方向安全、只是多拒了"**和**"已知不做"**两类 —— 它们**不是** §0.4 的缺陷
> （不漏 UB），但隔一段时间就该回来看一眼 ✓
> （原来散在 `KNOWN-ISSUES.md` 里；2026-09-23 文档重组时并进这里，那个文件已删 ✓）

**① 已经清掉的一条（留作判例）**：`examples/field-strong-update.extc` 的误拒。
根因是"根的记账深度只许长大"把"`h.p = null` 之后深度降回 0"这条路堵死了；
正解是**强更新只在字段表完整时启用**（表不完整 ⇒ 缺的格可能有值 ⇒ 不许下降）。
⚠️ 当时踩过三次：两次漏了"两条码路"（普通赋值 / 引用型目标），第三次漏了"表可以不完整"，
最后那条是**对抗用例 `H1_strongupdate_missed` 当场抓到**的（`tests/run.sh` 那时是假的 255/0）✓

**② 路径敏感的 arena 选择（俗称"下沉"）—— 设计过、没落地**：
同一个 `new` 站点，只读不外存 **1.6MB** vs 多一条外存分支 **33.2MB**（200 万轮）。
机制跑通过一次（生成出 `(*(keep == 1 ? &__extc_a[1] : &__extc_a[2]))`），但判据写错 5 次 ⇒ 搁置。
详见 `topics/ARENA-SOUNDNESS.md` §12（含三条必须的护栏）✓

**③ `alloc<T>` 不进 `promoteInto`**：层号只能在"函数有没有家"那一档降，
**不能沿赋值边降** ⇒ 少数形状多占内存（不漏 UB）✓

**④ 报错分不清"真不安全"和"我算错了"**：现在两条路都会吐同一句
"would hold a reference to something that dies first" ⇒ 用户分不出该改代码还是该报 bug ✓
（`topics/ARENA-SOUNDNESS.md` §A1 有分析）✓

**⑤ 铁律自检当前报出的违反处**：`EXTC_SELFCHECK=1 ./build/extc <file>` 会核
"`refDepth` 与 `arenaLevel` 是否描述同一个事实"，扫全语料**目前报 9 处**（见 `docs/DEVLOG.md`
2026-09-23 那条）。这些是**下一轮的修复候选**，不是待办清单 —— 修掉一处就少一处，
**所以这里不抄具体名字**（抄了就一定会过时 ✗）✓

---

## 0.6 ✅ **已修**：又一条能拿 UB 的路（= PLAN #31；原为 2026-09-21 的最高优先级）

> ⚠️ **这一节是记录，不是待办** —— #31 在 2026-09-21 已经全部收掉（见 §0.4 那行 + `ARENA-FORMAL`）✓
> 标题原来写着"**待修 = 当前最高优先级**"⇒ 又是一条会自己变成假消息源的行 ✗（2026-09-22 复核时改掉）

```extc
fn mk() -> varArray<i32> {
    var v: varArray<i32> = varArray<i32>::withCap(0)   // cap 0 ⇒ 第一次 push 就扩容
    v.push(42)
    v.push(43)
    return v                                          // ⚠️ **编译通过**
}
```
**实测**（`tests/canary-gaps/return-grown-varArray.extc`）：打印 `[1002, 0]`，正确值是 `[42, 43]` ✗
`cc -fsanitize=address` 报得清清楚楚：

```
ERROR: AddressSanitizer: heap-use-after-free ... READ of size 4
freed by thread T0 here:
    #1 extc_arena_release  (t12.c:71)
    #2 mk                  (t12.extc:6)      ← 就在 return 那一刻
```

**这条为什么刺眼**：它是**最自然的写法**（"函数里造一个数组、填好、返回"），
五子棋/IO 到处都会这么写；而且**不用 `!`、不用 `extern!`** 就能拿到 ✗

### 根因：同一个容器的 buffer 落在**两只不同的 arena** 里（一处不一致）

| 动作 | 走哪条路 | 家 arena 是谁 | 结果 |
|---|---|---|---|
| **建**容器 `withCap(4)` | `EX_ASSOC`（检查器**没**给它标 `homeDepth`）| `homeArg(0)` ⇒ 有家函数的 `__extc_home` | **活的** ✓ |
| **扩容** `v.push(..)` | `EX_METHOD`，实参是本地的 `v` | `callHomeDepth` 取 `placeDepth(v)` = 1 ⇒ `&__extc_a[1]` | **块 arena，`return` 时释放** ✗ |

⇒ 新 buffer 比容器先死。容器里的指针于是悬垂，**而检查器完全不知道**：
`sym->refDepth = exprRefDepth(withCap(...))` = `EX_ASSOC` 的"取实参最大深度" = **0**（实参是 `4`），
所以 `return v` 的逃逸检查看到的是深度 0 ⇒ 放行 ✗

⚠️ **注意这里的自相矛盾**：`markCallHomeIfEscaping`（PLAN #24）**只为 `EX_CALL`/`EX_METHOD` 标**，
`EX_ASSOC` 走 `homeDepth = 0` 的默认分支 ⇒ 恰好拿到 `__extc_home`（"碰巧对了"）。
两个入口对同一个容器给出**不同**的家 ⇒ 一致性问题，不是单点 bug ✓

### 修法三个方向（**主人拍板**；都牵到 ARENA.md §7 的证明）

| 方向 | 做法 | 代价 |
|---|---|---|
| **甲 · 保守拒绝** | 调用点带 `mut ref` 实参、家深度 > 0 时，把**实参那个绑定的 `refDepth` 抬到 ≥ 家深度**（就是 `a[0] = ref local` 那条更新路径的推广）| **声音（sound）但杀掉这个写法** ✗ —— `return v` 会报错，"造完返回"就写不了了 |
| **乙 · 家 = 我的家** | `callHomeDepth` 对本地实参返回 `-1`（`__extc_home`）⇒ 跟"建"那边一致 | ⚠️ 与**规则 ④** 打架：`h` 变成 0 ⇒ 接收者自己（深度 1）就被拒 ✗ 把接收者豁免掉的话，被调者就能把**接收者的地址**存进家 arena 内存（活得比调用者帧还久）⇒ **换个洞** ✗ |
| **丙 · 容器记住出生 arena**（设计上的正解）| 给每个绑定引入「**自己内容分配在哪只 arena**」（owner depth）：`var v = f(...)` 时按该调用的家深度记下来；`push` 这类 `mut ref` 调用就传 **owner depth**，而不是 `placeDepth(v)` | 检查器 + codegen 都要加一档（正是 `codegen.c` 里 arena 那段注释写的"容器要记住自己出生在哪只 arena"），**能力上最对**：`mk` 里 `withCap` 的家是 `__extc_home` ⇒ owner = 0 ⇒ `push` 也传 `__extc_home` ⇒ buffer 活得 ≥ 调用者 ✓ 而且 `return v` 合法 ✓ |

### 26a0Fe0f 2026-09-21 追加：主人指出**甲′ 不够、丙也不够** ⇒ 要**严谨的形式推导**

> 「甲肯定是不够的…让用户显式指定 arena 位置是失败的，因为**显然这里可以做到形式推导**」

⇒ 完整推导写在 **[`ARENA-FORMAL.md`](../docs/topics/ARENA-FORMAL.md)**：把"分配放哪只区域"从**语法猜测**
改成**约束求解**，并证：① 最小解存在唯一（⇒ 不需要注解）② 按解分配 ⇒ sound
③ 比今天的规则严格更宽（**"造完返回"可证安全**）④ 原丙破在"合并了地址流与内容流"✓
落地清单与验收判据见该文 §5 ✓

### （以下为实现甲′ 时的记录，保留）主人拍板：**丙**（2026-09-21）—— 设计已写完，见 [`ARENA.md`](../docs/topics/ARENA.md) §9

> 主人原话：「**丙，但是做之前，我感觉这个编译器 src 文件过于巨大了，维护阅读都很不方便，
> 建议按照功能拆分一下部分（？）**」⇒ 顺序定成 **先拆 src（行为零变化）→ 再实现丙** ✓
> （安全网 = `tools/golden.sh`：拆分必须做到**生成的 C 逐字节相同**）

**丙的最终形状**（比上表那版细，关键是补上了"E 分析"这一半）：

| # | 规则 | 一句话 |
|---|---|---|
| E1 | **创建** | `var v = f(…)`：v 会**被搬出本函数**（v ∈ E）⇒ 家 = 我的家；否则 = 本块 |
| E2 | **扩容** | 带 `mut ref` 实参的调用（根是局部 `v`）：v ∈ E ⇒ 家 = 我的家；否则 = `v` 的块 ⇒ **这条就是 #31 的修法** |
| E3 | **逃逸检查** | 绑定的有效深度 = `max(owner, refDepth)` ⇒ `mk` 里 `owner = 0` ⇒ `return v` **合法** ✓ |

**E**（local 会不会被搬出本函数）用 AST 不动点算，风格跟现成的 `stmtHasNew` 一致；
**E 与 owner 正好互补**：∈E ⇒ 用我的家（合法返回）；∉E ⇒ 本地就不返回它（留在本块，省内存）✓
⇒ **无假阳性、也不漏 UB** ✓ 全部推导在 `ARENA.md` §9（含"为什么乙不行"）

⚠️ **在修好之前**：`varArray` 只保证"**别把在函数里扩过容的容器/元素引用带出这个函数**" ✓
（不扩容没事 —— 对照组实测正确：`withCap(4)` 装 2 个再返回 ✓）

---


### ⚠️ 2026-09-21 追查：**这根本不是 varArray 的问题，是 A3 的通病**（第二个复现）

`tests/canary-gaps/return-built-list.extc`：

```extc
fn build() -> list {
    var l: list = { head: null }
    var i: i64 = 0
    while i < 3 { push(ref l, i * 10)  i = i + 1 }   // push 里 `new node`
    return l                                          // ⚠️ 编过
}
```
实测打印 `拿到 = 0`（正确 `20 10 0`）✗ **静默错数据**

**根因是同一处**：`push(ref l, …)` 这一刀的**家 arena = 实参 `l` 所在的那只**
（`callHomeDepth` 取 `placeDepth(l)`，即它所在的块 —— 这就是 `ARENA.md` §1.2 的规则）
⇒ `new node` 落在 **`build` 的函数体块**里 ⇒ `return l` 之后块被 release ⇒ 悬垂 ✗
而检查器**没把"被调者往容器里塞的东西住哪只 arena"记回容器的深度** ⇒ 逃逸检查看不见 ✓

**对照（都是对的）**：`examples/escape-promotion.extc` 在函数里**直接用 `new`** 造
（`new` 进的是这个函数的**家** arena，活的 ✓）；`examples/out-param.extc` 的 `listDemo`
造的容器**不出这个函数** ✓ —— 所以现有测试全绿，但挡不住"返回出去"✗

### ❌ 结论：原来的**丙**（E2：`v ∈ E ⇒ 传我的家`）**不 sound**，撤回

家变成"调用者的家"之后，被调者就能把**接收者/本地实参的地址**存进
**活得比调用者帧还久**的家内存 —— 而 §7 的证明**正是靠"家 = 最浅可写目标的 arena"**才成立
⇒ 那是**换个洞**，不是修洞 ✗（`ARENA.md` §9.7 记过这条，但当时没意识到它对**本问题**也成立）

### ✅ 改成 **甲′**：保守拒绝，但**只针对"被调者往里塞过东西的容器"**

| 步 | 做什么 |
|---|---|
| 1 | 带 `mut ref` 实参、**且被调者会分配（`needsHome`）** 的调用 ⇒ 把**实参那个绑定的有效深度**抬到 ≥ 这一刀的**家深度**（= 实参自己所在 arena 的深度）|
| 2 | 之后任何"让它逃出去"（`return l`、`return v.asSlice()`、塞进更浅的 struct…）都被**现成的逃逸检查**挡住 ✓ |
| 3 | 报错要指路：「容器里的存储分配在**本帧的 arena** 里 ⇒ 请在**调用者自己**的作用域里建它（`var v = …` 然后 `fill(ref v)`），或者用 `new` **直接在函数里**建（逃逸提升 ✓）」|

⇒ 它是 **P′**：证明不了安全的写法**响亮地报错**，而不是静默 UB ✓
⇒ 而且：**不返回就不受影响**（`listDemo` 那种照旧 ✓）、**`new` 直接建**照旧 ✓
⇒ 这正是现有设计**本来就想表达**的东西（§7：可写目标的集合由实参决定 ⇒ 数据属于实参那只 arena）
   —— 只是检查器**忘了把这个事实写回容器** ✗

⚠️ `varArray` 的 `asSlice`/`asMutSlice` **要等这条修好再上** ✓

### ✅ 2026-09-21 半夜：**甲′ 落地完成**（`raiseMutRefTargets` + 惰性 `funcAllocates`）

一行代码量级的修法（检查器侧，**codegen 一个字节没动** —— 70 个例子的生成 C 逐字节相同 ✓）：

> 带 `mut ref` 实参、且**被调者会分配**（`needsHome`）的调用 ⇒ 把**实参那个绑定的有效深度**
> 抬到 ≥ 这一刀的**家深度**（`Sym.refDepth = max(refDepth, homeDepth)`）✓

实测（同一台机器）：

| 形状 | 修前 | 修后 |
|---|---|---|
| 往本地容器 push 造链表再返回（`push` 体内直接有 `new`）| 打印 `拿到 = 0`（正确 `20 10 0`）✗ 静默错数据 | **编译错误** ✓ |
| varArray 扩容后返回容器（`push → grow → new`，**转发一层**）| ASan `heap-use-after-free` ✗ | **编译错误** ✓（补齐 #33 之后）|
| 现有 215 个测试 + 攻击库基线 + 72 个例子的生成 C | 全绿 | **全绿**，而且 codegen **逐字节相同** ✓（纯检查器侧修复）|

⇒ 结论：**甲′ 是对的方向**（把"静默 UB"变成"响亮的编译错误"，正是 P′），
   只差"被调者是否分配"这一问算得不够早（`needsHome` 的传递闭包跑在所有函数查完之后）✗
⇒ 正例仍然好写：`examples/varArray-outparam.extc`（在**拥有它的作用域**里建、用 `mut ref` 传下去填）✓
## 0.5 ✅ **soundness 洞已修**（2026-09-20，A0）—— 原记录见下

**修法**：把 `typeContainsRef` 拆成**两个问题** ——
| 问题 | 看哪儿 | 谁用 |
|---|---|---|
| **能不能携带引用** | **所有**变体 / 所有字段（`typeContainsRef`）| `exprRefDepth` / `exprBorrowed` / 全局检查 |
| **有没有零值** | 带载荷枚举**只看 tag 0**（`typeLacksZeroValue`）| `var x: T` 无初始化式 · 结构体字面量省略的字段 |

**验收**：4 个攻击形状（非泛型 / 泛型 / 载荷是结构体 / 经过转发）**全部报错** ✓
正例（载荷来自参数）仍合法 ✓ 零值语义没被破坏（`| nothing | holding(slice<u8>)` 的 `var` 合法、
反序的报错）✓ **127 测试全绿** ✓

⚠️ **`option<slice<u8>>` 那条（#7）没被这次修好** —— 它走的是**另一条路**：
泛型方法在**实例化时的复查**没有覆盖到零值检查 ⇒ 仍然报
`__extc_reference_has_no_zero_value__`（落在 **prelude 的行**上）✗ 见 §0.4 #7 ✓

---

## 0.5old 🚨 原来的记录（保留，因为根因分析还在用）

```extc
type box = | empty | holding(slice<u8>)      // ← 注意：无载荷的变体排在**第一**

fn make() -> box {
    var local: [8]u8 = [65, 66, 67, 68, 0, 0, 0, 0]
    return box.holding(local[..])            // ⚠️ **编译通过**（应该报错）
}
```

**返回的载荷指向一个已经死掉的栈帧** ⇒ **悬垂 / UB** ——
这是目前**唯一已知的、能从 extC 源码拿到 UB 的路**。

**根因**：`typeContainsRef` 的枚举分支**只看 tag 0 的载荷**（因为那条规则是给"零值"用的），
于是 `exprRefDepth` 看到 `box` 就早退返回 0 ⇒ **载荷的深度从来没算过** ✗
（泛型枚举 `maybe<T>` 同样中招。）

**修法**（已经想好，`DECISIONS.md` ⑱ 里记过）：
把 `typeContainsRef` 拆成**两个问题** ——
| 问题 | 看哪儿 | 谁用 |
|---|---|---|
| **这个类型有没有零值** | 只看 **tag 0** | 零初始化检查 |
| **这个类型能不能携带引用** | 看**所有**变体 | `exprRefDepth` / `exprBorrowed` / 全局检查 |

⚠️ **修之前不要用它**（这是唯一能拿到 UB 的路）。

---

## 1. ⭐ 主线：让它**能读东西**（做完 = 五子棋能跟人下）

> **2026-09-23 复核**：**第 1 项主体已落地**（`std::sys::io` + `std::io` **已落地**，定案 73/74 ⇒
> **"能读东西"这件事本身通了**，`tests/io/` 常设验收）；第 2/3 项**还没做** ✓
> ⚠️ 这一行原来写着「这三项**一条都还没做**」—— 那是 2026-09-22 定案 73 落地**之前**的话，
> 之后没跟着改 ✗（同一天还把 `grep rawRead` 当证据 —— 而实际落地时用的名字是 `read`/`write`，
> 所以那条 grep **永远查不到**，属于"判据写错了还以为查过了" ⚠️）

| # | 做什么 | 为什么挡着 | 大小 |
|---|---|---|---|
| **1** | 🟢 **主体已落地（2026-09-23，定案 73 + 74）**：`std::sys::io` 原语（`read`/`write` + 签字）+ **`reader`（隐式 64KB）** + **`nextInt`/`nextToken`/`nextLine`/`skipSpace`** + `ioError` 两条 + `writeBytes`/`flushOut`/内建 `flush()` ✓（`tests/io/` **7 条**常设验收 ✓，含三条路 + 分块读性能）⬜ **还欠**：`readAll` · 格式化输入 B · 写指定 fd。**实测**：12MB/100 万行，逐字节 1.444s vs 分块 0.032s = **45×** ✓ | **OI 式输入能用了**；gomoku 能读协议 | 中（设计已定，见 [`IO.md`](../docs/topics/IO.md)）|
| **2** | **IO-1**：`open` + **帧拥有文件**（跟 arena 并排）+ `main(args)` | 读源文件 / 写生成的文件 ⇒ 自举与工具的门槛 | 中 | ✅ **2026-09-23 规范已定（定案 77）**：`openRead(p)->inputFile` · `openWrite(p)->outputFile`（**截断**）· `openAppend(p)->outputFile`（**追加**）—— **读型/写型是两个 struct ⇒ 误用是编译期错误** ✓ 规范全文 `SYNTAX.md` §3′ · 常设验收 `tests/fs-shape/`（正例是**能跑的原型**）✓ ⚠️ `O_*` 与 POSIX 的数**只许出现在 `std::sys::io`**，`std::fs` 与用户代码一个都不许看见 ✓
| **3** | **`allocSlice<T>(n) -> mut slice<T>`**（帧 arena 里要 n 个 T，**清零**）| **长度运行时才知道**的 buffer：读未知大小的文件、`reader` 自动要 4KB、`varArray` 增长。也兑现 `SPEC.md` §0.6 那条承诺（清零 ⇒ 过期读到的也是**确定的**字节）| 小 |

> ⚠️ **更正**（2026-09-18，主人追问"造不出 buffer 什么意思"时实测发现）：
> 我原来写「今天造不出 buffer」**说过头了**。
> **定长数组当 buffer 是能用的** —— `var buf: [256]u8` + `buf[..]` + 传进 `mut slice<u8>` 参数 ✓
> （顺带抓出并修掉一个真 bug：`struct reader { chunk: mut slice<u8> }` 报
> **incomplete type** —— 依赖排序按**指针**找实例，而可写视图是**影子** ⇒ 找不到 ⇒ 排错顺序。）
>
> **所以 IO-0 不被任何东西挡着，可以立刻做** ✓ 里程碑比原来估的更近。

---

## 2. 原则欠账（都是"让承诺成真"，不是新功能）

> ⚠️⚠️ **这张表的编号跟 §0.4 撞车**（它的 #4/#5/#6/#7/#8/#9/#10 是另一批东西）⇒
> **引用 `PLAN #NN` 一律以 §0.4 为准** ✓ 这里只保留历史（2026-09-22 复核时补了映射与状态）：

| 本表 # | ⇒ 实际归属 | 状态（**2026-09-23 再核**）|
|---|---|---|
| 本表 **4** `option<slice<u8>>` 报错落在 prelude | §0.4 的 ~~**7**~~ | ✅ **已修**（第三刀：`option` 成了枚举，tag 0 不带载荷）|
| 本表 **5** `Sym.refDepth` 拆分 | §0.4 的 ~~**8**~~ | ✅ **已修**（两半都好了）|
| 本表 **6** 第三刀（`option`/`result` 重写成枚举）| §0.4 的 ~~**16**/~~ 见 `DECISIONS` 定案 52′ | ✅ **已修**（2026-09-20）|
| 本表 **7** 字面量越界 ⇒ 编译期报错 | §0.4 的 ~~**4**~~ | ✅ **已修**（2026-09-23）|
| 本表 **8** arena 要不到内存时带位置 | §0.4 的 ~~**6**~~ | ✅ **已修**（`extc_arena_alloc(a,n,file,line)` + 六个调用点 ✓）|
| 本表 **9** 无限递归 ⇒ 无声崩溃 | §0.4 的 **5** | ⬜ **还开着**（§0.4 里那一格 2026-09-23 实测过 ✓）|
| 本表 **10** `alloc` 的结果要能安全使用 | §0.4 的 ~~**3**~~（已修）+ §1 的 **3**（`allocSlice` 还没做）| 🟡 一半 |

| # | 做什么 | 现在的问题 | 大小 |
|---|---|---|---|
| **4** | **`option<slice<u8>>` 报成检查器错误** | 现在报错落在 **prelude 里**（`__extc_reference_has_no_zero_value__`）⇒ 用户看不见自己的代码，**违反 P′** | 小 |
| **5** | ⚠️ **无限递归 ⇒ 无声**（**2026-09-23 实测复核：仍开着**） | 没有消息、没有位置 ✗ 实测两种表现：① gcc 能把某些自递归**折成循环** ⇒ 变成**静默死循环**（`fn g→h→g+1` 那个形状跑 60 秒没动静 ✗）② 真爆栈时由 OS 报 `Segmentation fault`，**不是 extC 的消息**（更没有源码位置）✗ 设计里本来有 `@recursive!`（签字）那条路 ✓ ⚠️ 顺带记一个 UX 小事：`extc --run -O0 f.extc` **报 usage**（`-O0` 和 `--run` 只能按 `-O0 --run` 的顺序？实测两种顺序都不对）⇒ 想"用 -O0 跑一遍"做不到 ✗ **2026-09-23 实测**：`fn g→h→g+1` 与 `fn self(n){return self(n+1)}` 两种形状都**静默死循环**（12 秒零输出、无 extC 消息、无位置 ⇒ 被 gcc 折成循环）✓ ⚠️ **原文附带的 UX 小毛病已不成立**：`extc --run -O0 f.extc` 现在**能跑**（实测 `--run -O0 examples/hello.extc` ⇒ 输出 8 ✓）—— 那是 #11（参数循环多写一次 `i++`）在 `6ae3008` 修掉的，这一格没跟着改 ✗ | P′ | 小~中 |
| **6** | **第三刀：`option`/`result` 重写成普通枚举** + `?` 跟上 + 迁 examples | 今天它们是"struct + bool 标签"⇒ **不能 `match`**、`option<slice<u8>>` 编不过。第二刀的前置（泛型枚举）已经做完了 | 中（迁移面大）|
| ~~**7**~~ ✅ | `varArray<T>` —— **已落地**：写在 `stdlib/prelude.extc` 里（**用 extC 写**，18 处），`push`/`pop`/`get`/`set`/`size`/`asSlice`… 都能用 ✓（2026-09-23 实测） | 现在 `b[10]`（`b: [4]i32`）**能编过**，走到运行时才 trap。**安全**（不静默踩内存）但不够早 —— MANUAL 里"能证明的编译期报错"那句**只对切片成立** ⚠️（主人问"万一爆了怎么办"时实测发现）| 小 |
| **8** | **arena 要不到内存时带位置** | 现在 `extc: out of arena memory` + `exit(1)`，**没有源码位置** ⇒ 违反 P′ 的"看得见" | 小 |
| **9** | 📌 **`alloc<T>(n)` 当数组用不了**（**2026-09-23 实测复核：仍开着**） | 返回 `mut ref T`，不能索引/切片 ⇒ 实测 `var p = alloc<i32>(4)  p[0] = 1` 报 `cannot index a value of type mut ref i32` ✓（报错本身清楚 ✓，缺的是 `allocSlice<T>(n) -> mut slice<T>` 那个原语，见 §1 第 3 项） **2026-09-23 实测**：`var p = alloc<i32>(4)  p[0] = 1` ⇒ `cannot index a value of type `mut ref i32``；切片同报 ✓；`allocSlice<i32>(4)` ⇒ `is not a built-in primitive or generic function`（**不存在** ✓）⇒ 替代路径仍然有效：`var buf: [8]u8` + `buf[..]` ⇒ 写入读回 65 ✓（与原文一致）✓ | 缺口 | 小 |
| **10** | 🟡 **裸写构造器**（**2026-09-23 实测复核：仍开着**；口径见右） | ✅ prelude 的四个（`some`/`none`/`success`/`failure`）在**类型已知的四个位置**（return / 带标注 var / 赋值 / 实参）认裸写 ✓（定案 49）⚠️ **用户自己的枚举**带载荷时必须写全路径：`shape.circle(2.0)` ✓ 裸写 `circle(2.0)` 报 `call to undefined function circle` ✗ —— ⚠️ **但这**是**定案 27 的有意决定**（「关联调用写全类型**是故意的** —— 不靠上下文猜」✓）⇒ **不加推断**（那会跟"显式优于推导"打架 ✗）⇒ 真正该做的只有一件事：**报错时指路**（"写 `shape.circle(2.0)`"）✓ 极小 ✓（~~原来那行拿 `maybe` 举例 —— `maybe` 只是文档/例子里的名字，语言里只有 `option`/`result` ✗ 已改） **2026-09-23 实测（修正原文一处措辞）**：裸写在**四个位置**认 —— 那是**只对 prelude 的枚举**成立 ✓ （`let o: ?i32 = some(3)` ⇒ 3 ✓）；**用户自己的枚举哪个位置都不认**，连 `return circle(3.0)` 也报 ✗ ⇒ 是"**prelude 特例 vs 用户枚举**"，**不是**"哪个位置"决定的（原文的四个位置列举容易读成后者 ✗）✓ 现在的报错是 `call to undefined function `circle`` + 「built-ins available: print/println」⇒ **完全没指路**（该说"写 `shape.circle(2.0)`"）✓ | UX（报错话术）| 极小 |

---

## 3. 语言能力（"好用"的部分，全是加法）

| # | 做什么 | 什么时候需要 | 大小 |
|---|---|---|---|
| ~~**7**~~ ✅ | `varArray<T>` —— **已落地**：写在 `stdlib/prelude.extc` 里（**用 extC 写**，18 处），`push`/`pop`/`get`/`set`/`size`/`asSlice`… 都能用 ✓（2026-09-23 实测） | 需要**动态**容器时（棋盘是定长的，所以不急）| 中 |
| **8** | **`for` 循环**（+ range）| 每次写 `var i`/`while` 三行的时候 | 小~中 |
| **9** | 📌 **`alloc<T>(n)` 当数组用不了**（**2026-09-23 实测复核：仍开着**） | 返回 `mut ref T`，不能索引/切片 ⇒ 实测 `var p = alloc<i32>(4)  p[0] = 1` 报 `cannot index a value of type mut ref i32` ✓（报错本身清楚 ✓，缺的是 `allocSlice<T>(n) -> mut slice<T>` 那个原语，见 §1 第 3 项） **2026-09-23 实测**：`var p = alloc<i32>(4)  p[0] = 1` ⇒ `cannot index a value of type `mut ref i32``；切片同报 ✓；`allocSlice<i32>(4)` ⇒ `is not a built-in primitive or generic function`（**不存在** ✓）⇒ 替代路径仍然有效：`var buf: [8]u8` + `buf[..]` ⇒ 写入读回 65 ✓（与原文一致）✓ | 缺口 | 小 |
| **10** | 🟡 **裸写构造器**（**2026-09-23 实测复核：仍开着**；口径见右） | ✅ prelude 的四个（`some`/`none`/`success`/`failure`）在**类型已知的四个位置**（return / 带标注 var / 赋值 / 实参）认裸写 ✓（定案 49）⚠️ **用户自己的枚举**带载荷时必须写全路径：`shape.circle(2.0)` ✓ 裸写 `circle(2.0)` 报 `call to undefined function circle` ✗ —— ⚠️ **但这**是**定案 27 的有意决定**（「关联调用写全类型**是故意的** —— 不靠上下文猜」✓）⇒ **不加推断**（那会跟"显式优于推导"打架 ✗）⇒ 真正该做的只有一件事：**报错时指路**（"写 `shape.circle(2.0)`"）✓ 极小 ✓（~~原来那行拿 `maybe` 举例 —— `maybe` 只是文档/例子里的名字，语言里只有 `option`/`result` ✗ 已改） **2026-09-23 实测（修正原文一处措辞）**：裸写在**四个位置**认 —— 那是**只对 prelude 的枚举**成立 ✓ （`let o: ?i32 = some(3)` ⇒ 3 ✓）；**用户自己的枚举哪个位置都不认**，连 `return circle(3.0)` 也报 ✗ ⇒ 是"**prelude 特例 vs 用户枚举**"，**不是**"哪个位置"决定的（原文的四个位置列举容易读成后者 ✗）✓ 现在的报错是 `call to undefined function `circle`` + 「built-ins available: print/println」⇒ **完全没指路**（该说"写 `shape.circle(2.0)`"）✓ | UX（报错话术）| 极小 |
| ~~**11**~~ ✅ | **`@overwrite`**（复用一块存储：`@overwrite var n = new node`）（**2026-09-22 三步全做完**）| 用户从 GC 语言搬过来的"每轮 new 一个新对象"在 extC 里**本质是泄漏**（实测 1e7 轮 = 159.7MB ✗，而复用是 1.7MB ✓）；显式让编译器复用 ⇒ 语法只多一个修饰符 ✓ 设计见 `DECISIONS` 定案 65 ✓ 落地三档：单值 / 定长缓冲 / **运行时长度（{ptr,cap} + 翻倍，上界 ≤ 2×最大长度）** · 格子由**调用点的帧**持有（`new` 在 callee、循环在 caller 才复用得上 ✓）· 递归各激活一块 ✓ 实测：1e7 轮 **159.7MB → 1.6MB** ✓ 零新运行时（`alloc` + `memset`）· 判据：`examples/overwrite.extc` · `tests/errors/` 三条 · `tests/arena/overwrite{,-dyn}-flat.extc` · `tests/asan/overwrite-{callee,dyn}.extc` · 攻击库基线一字不动 · golden 只多一个新例子 ✓ |
| 🟡 **11b** | 模块：**v1 已落地**（定案 70：语义导入 · 一个文件一个模块 · `@private` · 禁环）+ **per-module 名字 mangle 已落地**（2026-09-23：两个模块同名声明互不干扰 ✓；`tests/modules/samenames` 常设验收 ✓）⬜ **还欠**：`@main` 多入口（自举需要 `gomoku` + `gomoku-engine` 两个可执行文件） | 自举：`gomoku` + `gomoku-engine` 两个可执行文件 | 中 |
| ~~**12**~~ ✅ | **块 `{ }` 变释放点**（arena mark/release）| **2026-09-22 复核：早就做完了** —— 这就是 **A2「按块细化」**（2026-09-20，`tests/arena/` 验收：150MB 上限下循环里 300×1MB 跑得完）✗ 那行为什么还挂着？⇒ 又是过时行 ✓ | — |

---

## 4. 可选的（不做也不挡路）

| 项 | 说明 |
|---|---|
| **自举** | 已降级为**可选的验证手段**（主人：「我不一定完全自举，C 写语法解析底层效率很高」）|
| **TUI（termios raw mode）** | `IO.md` 里定过：不包 ncurses，只要"读一个字节 + 开关 raw 模式" |
| arena 帧内留垃圾 | 可加 `reserve()`；**不是不安全** |
| 容器拷贝 = 共享存储 | 写文档说明即可；**不是不安全** |
| `scan(mut a, mut b, ...)` 变参 | 主人要，但**明确推迟**（细节多）|

---

## 5. ⛔ 永远不做（不是"还没做"）

运行期换实现（插件 / 热替换）· GC · 异常 · 裸指针 · 继承 / 虚函数 ·
手动 `free` / `close` · `errno` / 隐藏全局状态 —— 见 [`SPEC.md`](../docs/SPEC.md) §9 ✓

---

## 6. ⭐ 主人定的顺序（2026-09-20）：**arena → IO → 后续**

⚠️ **一处必须点出来的耦合**：**那个 soundness 洞（§0.4 #1）就长在 arena 要用的那片代码上** ——
`typeContainsRef` / `exprRefDepth` 正是"逃逸提升"要动的分析 ✗
⇒ **修洞 = arena 的第一步**（不是额外的活），而且**不修的话，arena 的提升分析必然也是漏的** ✓

### A · arena（分五层，每层可验收）

> ⭐ **2026-09-20 主人拍板：先做 A2（按块细化）** —— 它是地基。
> 理由（实测过，不是感觉）：
> `tests/arena/loop-growth.extc`（循环里每次分配 1MB × 300 次）在 `ulimit -v 150000`
> 下**现在就崩**：`extc: out of arena memory` ⇒ 每帧 arena 的内存上界是**整个函数体** ✗
> 而按块细化之后同样代码同样上限应该跑完（上界 = 一次迭代）✓
> **验收命令**：`./tests/arena/run.sh`（A2 做完之前预期失败，所以暂时没并进 run.sh）
>
> 而且**现在改最便宜**：今天全语言只有 `alloc<T>(n)` 一个分配原语，几乎没有代码依赖
> "活到函数返回" ⇒ 迁移面接近 0；等 `new` / IO / `varArray` 之后收紧就贵了 ✓
> ⚠️ 先 `new` 后细化的顺序是**错的**（会把 `new` 的语义先按帧固化进例子和测试，之后重审一片）✗

| # | 做什么 | 为什么在这一层 |
|---|---|---|
| **A0** ✅ | **修 #1 那个洞**（**2026-09-20 完成** ✓，见 §0.5）| **arena 的逃逸分析必须长在同一片代码上**；不修则提升分析也漏 ✓ |
| **A0·补** ✅ | **顺手做完的**：`*p` 显式解引用 · `?T` 语法糖 · **`?ref T` + `null` + 非空收窄** · 引用型绑定的被指深度（#8 一半）| ⇒ **链表/树/搜索函数今天就能写** ✓ 只差跨函数接线（A1 `new` + A3 提升）|
| **A0·补 2** ✅ | **第三刀**：`option`/`result` 重写成**普通枚举** ⇒ **`option<slice<u8>>` 合法**（#7 关掉）+ match 的两个真 bug（#2 / #15）| ⇒ **IO 的先决条件满足了**：只差 A1 的缓冲区 `new i32[4096]` ✓|
| **A0old** | **修 #1 那个洞**：`typeContainsRef` 拆成「有没有零值」（看 tag 0）和「能不能携带引用」（看所有变体）| **arena 的逃逸分析必须长在同一片代码上**；不修则提升分析也漏 ✓ |
| ~~**A1**~~ ✅ | **`new` 形式**（**2026-09-20 完成**）：`new T` / `new [N]T` / `new T[n]`，**分配出来的一定是零**（运行时那条 alloc 自己 memset）✓ 深度 = 当前块深度（跟 A2 的接缝同一个数）| **造 buffer 解锁了** ⇒ IO 的最后一块拼图 ✓ |
| ~~**A2**~~ ✅ | **每个词法作用域一只 arena**（**2026-09-20 完成**）| `extc_arena __extc_a[DEPTH] = {0}`（层数编译期算好、栈上定长）+ 进块 reset + 出块 release；`break`/`continue` 放到循环体那层、`return`/`?` 放到第 1 层 ✓ ⭐ 接缝：`alloc` 的深度改成**当前块深度**（跟选 arena 的那个数必须是同一个）✓ 验收 `tests/arena/`（150MB 上限下不许涨）|
| ~~**A3**~~ ✅ | **逃逸提升**（**2026-09-20 三半全齐**）① 返回值提升（`fn build() -> mut ref node` ✓）② 出参按**最浅的 `mut ref` 实参**选 arena（= ARENA.md §1.2 ✓）③ **规则 ④**（调用点实参活得 ≥ 家 arena）+ callee 放宽 ⇒ **链表出参 `push` 也能写** ✓ | ✅ 见 `examples/escape-promotion.extc` / `out-param.extc` |
| **A3old** | **逃逸提升**（旧记录）：**函数体有分配 + 返回类型含引用/视图** ⇒ 多收一只隐藏参数 `extc_arena *__extc_home`，`new` 分配到**调用者选的那只** arena（调用点：自己有家就传家，没有就传当前块）✓ `needsHome` 取传递闭包 ✓ ⚠️ **剩下的**：**出参**（`fn fill(d: mut ref ?ref node) { *d = new node }`）—— 那要把家从"函数级"细化到"按调用点/按 `mut ref` 实参" | ✅ `fn build() -> mut ref node` 能写了（`examples/escape-promotion.extc`）✓ 而且**返回 `i32` 的函数不要家** ⇒ A2 的"循环不涨内存"没丢 ✓ |
| **A4** | 显式命名 + 显式指定 home（名字待定，主人说"不急"）| 逃生舱，最后做 ✓ |
| ~~**A5**~~ ✅ | 测试：跨函数链表 · AST 递归 · 循环不涨内存 · 攻击测试全绿 | **2026-09-22 复核：全齐了** —— `bench/stress`（跨函数链表 + 深递归）· `tests/arena/`（循环不涨）· `tests/attacks/`（基线一字不动）+ `tests/asan/`（新增）✓ |

### B · IO（设计已定在 [`IO.md`](../docs/topics/IO.md)）

原语 `rawRead`/`rawWrite` + `readLine`/`readAll`/`nextInt` 一族 + `ioError` ——
**用 A1 的 `new i32[1000]` 当缓冲** ✓ ⇒ **五子棋能跟人下**（里程碑）✓

### C · 后续

`varArray<T>` → `for` → 函数值/lambda（⇒ 顺带解锁"安全的虚表" ✓）→ 协议补全 → 模块/`@main`

---

## 6旧. 原来的顺序（保留作参考）

```
allocSlice ──▶ IO-0 ──▶ (五子棋能跟人下 ✓ 里程碑)
                │
                ├──▶ IO-1（文件 + argv）
                │
                └──▶ 原则欠账 4 / 5 / 6（趁手热，都小）
                       │
                       └──▶ varArray ──▶ for ──▶ lambda（好用，不挡路）
```


---

## 7. 🅿️ 明确**先不动**的（主人拍过板：以后再说）

| 事项 | 现状 | 主人 2026-09-18 的话 |
|---|---|---|
| **块注释换成不烂大街的语法**（`#\| \|#` / `(* *)`）| `/* */` 能用但**不嵌套**（实测：内层 `*/` 会提前关掉注释）| 「这个问题没那么简单，涉及到我们会用什么符号表示什么含义/语法糖，再说吧，先不动」|
| **同一行多语句强制写 `;`** | `;` **已经能写**（`skipJunk` 吃掉它），只是不强制 ⇒ 例子都写成两个空格 | 「可以先不动，保留一些奇怪的特性」|

> ⚠️ 两条都**不是"忘了"**，是**有意保留** ✓

### 符号占用表（将来要动符号时从这张表开始）

| 符号 | 现在表示 | 备注 |
|---|---|---|
| `//` · `/* */` | 注释 | 块注释**不嵌套** |
| `:` | 类型标注 | `var x: i32` |
| `=` | 绑定 / 赋值 | |
| `->` | 返回类型 | `fn f() -> i32` |
| `=>` | `match` 分支 | |
| `::` | 关联函数 / 枚举构造 | `option<i64>::some` |
| `.` | 字段 / 方法 / 变体 | `b.moves` · `shape.circle(2.0)` |
| `..` · `...` | 切片区间 · 数组字面量剩余 | `a[1..3]` · `[1, 2, ...]` |
| `?` | **失败顺着往上抛** | 四个合法位置 |
| `!` | **我签字**（+ `!=`）| 唯一的逃生舱标记，**可数** |
| `@` | 属性 | `@recursive!` · `@main`（设计中）|
| `[ ]` `{ }` `( )` | 数组/索引/切片 · 块/结构体字面量 · 调用/分组 | |
| `< >` | 泛型实参 | 嵌套靠 `<` 配对，`>>` 不冲突（实测 ✓）|
| `ref` `mut` | 引用 / 可写限定词 | 关键字 |
| **`#`** | **完全没用** ⭐ | 除了生成的 C 里的 `#line`。**块注释候选** |
| `;` | 语句分隔（可用、不强制）| |
| `$` `` ` `` `\` `|>` `<|` `<-` | **没用** | 语法糖候选 |
