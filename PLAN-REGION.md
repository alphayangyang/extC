# `PLAN-REGION.md` —— 把形式化推导落进编译器（执行计划）

> 参考 = [`ARENA-FORMAL.md`](ARENA-FORMAL.md) 全部内容（§3 约束系统、§6 证明与记账不变式、
> §7 误拒分析、§8 让 checker 用定理 + §8.5 工程降法）✓
> 目标 = goal「把 ARENA-FORMAL 的推导落进编译器」✓

## 0. 铁律（每一步都必须满足，不然不算做完）

1. **双向判据**（最重要）：
   - **该放的要放**：`tests/canary-gaps/` 里那三条**安全**的形状
     （`return-grown-varArray2`、`varArray-asSlice-return`、`built_list` 的**安全变体**）
     ⇒ 最终要搬进 `examples/` 并带 `// expect:` 断言 ✓
   - **该挡的要挡**：`tests/attacks/BASELINE` **一字不动**（攻击库通过集合不许变）✓
2. **`tools/golden.sh` 的差异必须是"预期的那一处"**：纯检查器改动 ⇒ **必须逐字节相同**；
   若某步**有意**改变分配（比如"家 arena 选择"变了）⇒ 差异必须只是 arena 实参那一类，
   **先人工看过再 `save`**，并在提交信息里写清楚"为什么这处差异是对的"✓
3. **`./check.sh`（全量）** 通过 4、失败 0 ✓
4. **每步一个提交**，提交信息写清：改了什么、判据结果、**有没有预期外的差异** ✓
5. **记账不变式**（§6.6）：任何"能改变某个值内容"的新规则，必须把写进去的东西的深度
   **取上界记回那个值**（`max` 不是覆盖）✓

## 1. 档 1（核心）：效果摘要 + 调用点求解 ⇒ 消掉"规则层"的粗

**要消掉的三件事**：① #34（容量够的容器其视图也返回不了）② "造完返回容器"被拒
③ 规则 ④ 把**地址流**当非空地施加于所有实参（`ARENA-FORMAL` §2.3）✓

### 步骤（每步都能单独验收）

| # | 做什么 | 改哪 | 验收 |
|---|---|---|---|
| **1.1** ✅ **完成**（2026-09-21）| **算摘要**：`FuncDef` 加 `addrMask`/`contMask`/`homeAddrMask`/`homeContMask`（位 = 参数）；区分**目的地**（形参容器 vs **本帧新分配的对象**）与**来源**（地址流 / 内容流 / Fresh）| `src/ast.h`、`src/check_top.c` | **已验收**：`--dump-effects` 打印；golden **逐字节相同**（71 文件）✓ 216 测试全绿（**当时**的数，历史记录 ✓）✓ 攻击库基线不动 ✓ **关键证据**：链表 `push` = `toParam[Addr=0 Cont=0] toHome[Addr=0 Cont=1]` ⇒ **地址流为空**（今天规则 ④ 却按非空处理 ✗）；`sneaky`（`n.owner = ref l.head`）= `toHome[Addr=1]` ⇒ 这类**必须继续挡** ✓ |
| **1.2** ✅ **完成**（2026-09-22，= PLAN **#43**）| **调用点求解**：`H = ⨆(调用者侧落点, Cont 已知的 δ)`；`Addr` 那条 = 规则 ④ ✓；⭐ `Cont` 那条 = 新增 `exprRefDepth(实参 j) ≤ h` ✓ | `src/check_top.c`（`checkCallRefArgs`）、`src/check_expr.c`（三处调用点）| **已验收**：攻击库**仍全挡**（`stash_view_from_deeper`/`ref_arg_too_deep`/`borrowed_into_param_place`/`ref_launder_field`/`_deref`/`generic_borrowed_store` 一条不少）✓ 新正例 `container-of-view`/`container-nested`/`borrowed-stash-sameframe`（ASan 干净）✓ 老编译器对这三条**全误拒**（`git worktree` 里的 HEAD 副本对拍 ✓）· golden 差异 = **只多两个新例子** ✓ 详见 `ARENA-FORMAL` §9.5 |
| **1.2b** ✅ **完成**（2026-09-22）| **摘要不完整 / `otherMask` 非空 ⇒ 调用点保守拒**（"不可分割的一半" ✓ 只放不挡 = 把误拒换成洞 ✗）| 同上 | 已验收：`!complete` 时对**每个含引用的实参**按最坏情况查 ✓ 消息说清"分析不出来" ✓ |
| **1.2c** ⚠️ **偏离（记账）**| 判据原写"攻击基线**一字不动**"，实做时发现 `h8_launder` 是**假阳性被当战功** ✗ ⇒ 改准账：它进基线（已知安全），危险变体另立反例 ✓ 纪律升级：**"挡住"必须同时是"这条程序真的不安全"** ✓ | `tests/attacks/BASELINE`、`tests/errors/stash_view_from_deeper.extc` | 见 `ARENA-FORMAL` §9.5 与 定案 67 补记 |
| **1.3** ✅ **完成**：`raiseMutRefTargets` 已删（深度的记账改由 1.2 的求解承担）✓ | — | 三条 canary 变正例 ✓ 攻击库不动 ✓ |
| **1.4** ✅ **完成**：三个安全形状进 `examples/`（各带 `// expect:`，ASan 复核干净 ✓）；`tests/canary-gaps/` 已清空 ✓ | `tests/`、`examples/` | 双向判据同时成立 ✓ |
| **1.5** ✅ **完成**（2026-09-22）| **文档**：`ARENA-FORMAL` §9.5（落地实录）· PLAN #43 ✅ + #44（尾巴）· PLAN 下一步表重写 · DECISIONS 定案 67 补记 · DEVLOG · MANUAL（`stash` 一族的新正例）| 文档 | `check.sh` **8 通过 0 失败** ✓ golden 只多两个新例子 ✓ |

### 工程降法（§8.5）在档 1 的落地

- **模板级摘要 + 代入**：摘要按**模板**算（`FuncDef` 是模板本尊）；实例化**只代入**
  （类型参数只影响"会不会装引用"一个布尔 ⇒ 2^k 种形状）⇒ **不要每实例重算** ✓
- **实例去重**：`varArray<i32>` 只算一份（沿用 codegen 的实例集合）✓
- **上限 + 响亮报错**：若某个形状的组合数超限 ⇒ **报错**（不许静默保守）✗
- ⚠️ **2026-09-22 实做时的代价（= PLAN #44）**：模板里 `T` 是 opaque ⇒ 收集器判不出"值里有没有引用"
  ⇒ 只能保守地"**含类型参数就当含引用**" ✓ sound ✓ 但 `varArray<i32>` 这类**纯值实例**也背 `Cont` 位 ⇒
  调用点对它走"最坏情况"那条 ⇒ **误拒面偏大**（今天不误拒靠的是 `exprRefDepth` 对纯值实参返回 0 ✓）✗
  ⇒ #44 按实例算摘要（照 `RefCheck`/`EqCheck` 的实例化复查做 ✓ 机制现成）✓

## 2. 档 2：字段/槽位级深度 + 取地址跟踪（消 §7.4 的硬门槛）

- **2.1 取地址跟踪**：每个 `Sym` 一个 `addressed` 位；`ref x` / 传 `mut ref` ⇒ 置位 ✓
- **2.2 强更新**：目标绑定**没取过地址** ⇒ `p.f := e` 可以把深度**覆盖**（不是取 max）；
  取过地址 ⇒ 退回弱更新（保守）✓ ⇒ 消掉"置空救不回来"的误拒（§7.4 命题 7.3 的双方）✓
- **2.3 字段级深度**：`Sym` 按**字段路径**记深度（至少一层：`h.p` 与 `h.q` 分开）
  ⇒ 消 PLAN #8 剩下那半（"字段里的参数引用被按槽位深度算"）✓
- 验收：新增 canary 对（safe 变体必须过、unsafe 变体必须挡）；攻击库不动 ✓

## 3. 档 3：路径事实（利用"链式只读"保证别名稳定）

- **3.1** 收窄支持**只读路径**：`p.next != null` ⇒ 在**整条链只读**（`REFS.md` 的链式只读 ✓）
  且中间没有 `mut ref` 写入的前提下，证明该路径非空 ✓
- **3.2** 深度按路径传播（同理，只读路径）✓
- 验收：`#14` 的用例（`p.next.value` 直接写）能编过；`mut` 链上的同名形状**仍要挡** ✓

## 4. 风险与回滚

| 风险 | 处置 |
|---|---|
| 某步让攻击库放松（soundness 回归）| **立即回滚该步**，把反例贴进 DEVLOG，重新推导（这正是双向判据的用处）✓ |
| 报告形状变差（P′）| 报错必须给**冲突见证**（被违反的边 / 约束环）+ "下一步怎么办" ✓ |
| 编译时间（§8.5 的 M×F）| 守三条降法；超限响亮报错；`bench` 里的编译时间不恶化 ✓ |
| 大改动难定位 | 每步一个提交 + 每步都跑全量 `check.sh` ✓ |

## 5. DoD（完成定义）

- 档 1 完成 ⟺ 三条安全 canary 在 `examples/`（带 expect）**且** `tests/attacks/BASELINE` 未变
  **且** `raiseMutRefTargets` 已删 **且** 文档更新 ✓
- 档 2 完成 ⟺ §7.4 的 `safe()` **能编过**、`unsafe()` **仍被挡** ✓
- 档 3 完成 ⟺ PLAN #14 的用例能编过，`mut` 链上的同名形状仍被挡 ✓
- 全部完成 ⟺ `./check.sh` 全量通过 4/0、`golden` 差异只有"预期的那一处"、PLAN #31/#34/#14/#8 全部收掉 ✓

## 6. ⛔ 档1 步骤 1.2 的两次绊线（2026-09-21，**回滚并留档**）

按铁律"判据不平衡就回滚"，1.2 试了两件事，**两件都被双向判据当场抓住** ✗：

| 试了什么 | 抓它的判据 | 根因 |
|---|---|---|
| **收窄规则 ④**（"`Addr` 全空 ⇒ 跳过那条保守约束"，`ARENA-FORMAL` §2.3/引理 1 的直接推论）| `tests/errors/ref_arg_too_deep` 从**必须报错**变成**通过** ✗ | **效果摘要还没有传递闭包** —— callee 里再转一手（调另一个会存地址的函数）就漏 ⇒ 此时"`Addr` 全空"是**假的** ⇒ 收窄等于放行悬垂 ✗ |
| **用 E 选家 arena**（"实参会被搬出本函数 ⇒ 家 = 我的家"，`ARENA-FORMAL` §9）| `examples/out-param.extc` 从**能编过**变成被拒 ✗ | **E 过度近似**（第一版把"任何表达式语句/条件里出现的名字"都算成逃逸 ✗）⇒ 家 arena 到处变成 `__extc_home` ⇒ 既费内存，又让规则 ④ 的 `h` 归 0 ⇒ 误拒 ✓ |

**已修的部分**（留在树里，只算不用 ⇒ 行为零变化 ✓）：
- E 分析改成**只认"交出去 / 写进更长寿的地方"**（条件、表达式语句、match 主体都不算逃逸）✓
- 规则 ④ 保持**保守**（宁误拒、不漏 UB），并在代码里留了"为什么不能现在收窄"的注释 ✓

**重试 1.2 的两个前提**（缺一个都不许收窄）：

1. **1.2a 传递闭包 ⭐**：按 `ARENA-FORMAL` §8.5 做**惰性**摘要闭包（memo + 环保护，
   拿不准当"有"），并给每个函数记一个"**摘要是否可判为完整**"的位 ⇒ **只有完整才允许跳过规则 ④** ✓
   （骨架已有：`FuncDef.callees` + `#33` 的 `funcAllocates` 那套）
2. **1.2b E 的精度**：E 必须是"**真实逃逸集的超集**，但别把无关名字全塞进来"；
   加一条自检用例：`examples/out-param.extc`（`listDemo` 里 `l` **不逃逸**）⇒ E 必须**不含** `l` ✓

⇒ 判据仍是那一条：**该放的放 + 该挡的挡 + golden 差异只是"家 arena 实参"那一类** ✓

## 7. 档 2 的实测结论（2026-09-21）：**顺序要反过来做**

| 步 | 状态 | 说明 |
|---|---|---|
| **2.1 取地址跟踪** | ✅ 完成 | `Sym.addressed` + `EX_REF` 置位；**只记不用**（行为零变化 ✓）|
| **2.2 强更新** | ⛔ **试过、没生效、已回滚** | `h.p = null` 走的不是普通赋值路径，而是 **retarget 路径**（`?ref` 型字段赋值 ⇒ 只更新**槽位**的 `refDepth`）⇒ 根 `h` 的深度没被覆盖 ⇒ `return h` 仍被拒 ✗（调试打印确认：强更新分支对 `h` 一次都没进）|
| **2.3 字段级深度** | ⬜ **应该先做** | 让 `Sym` 按**字段路径**记深度（至少一层：`h.p` 与 `h.q` 分开）⇒ ① 根的有效深度 = 各字段的最大值 ✓ ② `h.p = null` 就能真正覆盖掉那一格 ✓ ③ 顺带消掉 PLAN #8 剩半（"字段里的参数引用被按槽位深度算"）✗ |

⇒ **修订后的档 2 顺序：2.3 → 2.2 → （2.1 已完成）** ✓
⇒ 目标形状：`tests/canary-gaps/field-strong-update.extc`（做完 2.3 就该能收进 `examples/`）✓
⇒ 反例（必须一直挡）：`tests/errors/field_weak_update_aliased.extc`（取过地址 ⇒ 弱更新）✓

## 8. 档 2 ✅ 完成（2026-09-21）—— 字段级深度 + 取地址跟踪

| 步 | 状态 | 做法 |
|---|---|---|
| **2.1 取地址跟踪** | ✅ | `Sym.addressed`；`EX_REF` 时给根绑定置位 ✓ |
| **2.3 字段级深度** | ✅ | `Sym` 带一张小字段表（≤4 格）+ `otherDepth` 兜底；根的有效深度 = `max(otherDepth, 各格)`；声明时按结构体字面量填表；路径写按格记 ✓ |
| **2.2 强更新** | ✅ | 写某一格时：**没取过地址 ⇒ 覆盖**（强）；取过地址 ⇒ 取 max（弱，别名可能写别处）✓ |

**判据（双向，全过）**：
- 该放：`examples/field-strong-update.extc`（`h.p = null` 之后返回 ⇒ 合法，带 `// expect:`）✓
- 该挡：`tests/errors/field_weak_update_aliased.extc`（取过地址 ⇒ 弱更新 ⇒ 拒绝）✓
- `check.sh quick` 4/0 ✓ golden **逐字节相同** ✓ 攻击库基线**一字不动** ✓

**顺带收掉 PLAN #8 的剩下那半**（"字段里的参数引用被按槽位深度算" ⇒ 现在按字段记 ✓）；
**未做的**：没写出来的字段（零初始化）不建格 ⇒ 读它时退回保守算法（可能误拒，不是洞）✓

## 9. 档 3：路径事实（利用链式只读）⇒ 消 PLAN #14

> **2026-09-22 复核**：标题原来写"（剩）"✗ —— 档 3 的 **sound 子集已在 §10 落地**（2026-09-21）✓
> 本节保留的是**为什么做、以及一轮失败记录**（§9.1），执行结果看 §10 / §11 总账 ✓

- 3.1 收窄支持**只读路径**：`p.next != null` ⇒ 在"整条链只读且中间没有 `mut ref` 写入"的前提下证明该路径非空 ✓
- 3.2 深度按路径传播（同理，仅只读路径）✓
- 判据：#14 的用例能编过；`mut` 链上的同名形状**仍要挡** ✓

### 9.1 ⛔ 一轮失败记录（2026-09-21）：路径收窄**手工拼接把两处代码弄坏了**，已回滚

试的是 9.1 那一小块（sound 子集），做法是对的，但**用 awk/sed 按行号拼接多行 C 代码**
在上下文快用尽时不可靠 ✗（`check_lookup.c` 的 `unNarrow` 和 `narrowTarget` 被拼坏、
`check_expr.c:517` 也被替换坏了）⇒ **回滚**（`git checkout -- src/`），树回到已验证状态 ✓

**下次的正确做法**：用 `edit` 工具（按语义做**小步替换**），一步一编译一验证 ✓
**设计已经想清楚**（照抄下面的位置即可）：

| 位置 | 改什么 |
|---|---|
| `check_lookup.c` `narrowTarget` | 除了 `EX_IDENT`，再加一条：`var` 是 **一层字段路径**（`obj` 是 `EX_IDENT`）且根绑定是 **depth ≥ 1 且 `!addressed`** 的局部 ⇒ 造一个路径 key `"<cname>.<field>"`（`arenaAlloc` + `snprintf`）✓ **参数不行**（调用者可能有别名）✗ |
| `check_lookup.c` `unNarrow` | 除了精确匹配，还要砍掉**同前缀**的路径 key（`k[l] == '.'`）⇒ 对根的写 / 取地址都会让路径事实作废 ✓ |
| `check_expr.c` `EX_FIELD` | 算出字段类型后：若它是**可空引用**且路径 key 在 `isNarrowed` 里 ⇒ 返回**非空**版本（`ttRef(tt, ftype->inner)` + 抄 `mut`）—— 与 `EX_IDENT` 那条规则（`check_expr.c:111`）完全对称 ✓ |
| `check_expr.c` `EX_REF` | 置 `addressed = true` 之前先 `unNarrow(c, rs->cname)`（取地址 ⇒ 路径事实作废）✓ |

**判据**：正例 `if h.p != null { … h.p.value … }`（`h` 是没取过地址的局部）能编过；
`p.next.value`（`p` 是**参数**）**仍要报错**（PLAN #14 原文那个形状 ⇒ 硬门槛在别名上）✗

## 10. 档 3 ✅（sound 子集落地，2026-09-21）

| 步 | 状态 | 做法 |
|---|---|---|
| **3.1 只读路径收窄** | ✅ | `narrowTarget` 认**一层字段路径**，但只认「根是 `depth ≥ 1` 且 `!addressed` 的**局部**」；`unNarrow` 随根作废；`EX_FIELD` 命中路径 key ⇒ 返回**非空**版本；`EX_REF` 先作废再置 `addressed` ✓ |
| **3.2 参数根** | ⛔ **不做**（并记进"硬门槛"）| 调用者可能持有别名 ⇒ 事实不稳定 ✗ ⇒ 需要**别名分析**（`ARENA-FORMAL` §7.4 那条门槛）⇒ 记为**长期不做**，报错文案里已给合法替代（`var x = p.next`）✓ |

**判据**：`examples/path-narrowing.extc`（局部根 ⇒ 编过，带 expect）✓ ·
`tests/errors/path_narrow_param_root.extc`（参数根 ⇒ 仍报错）✓ ·
`check.sh` 全量 **通过 14，失败 0**（251 测试）✓ · 攻击库基线**一字不动** ✓

## 11. 🏁 三档总账（goal 完成度）

| 档 | 结果 | 关键证据 |
|---|---|---|
| **档1** 摘要 + 闭包 + E + 删甲′ | ✅ | `varArray-return` / `list-return` / `varArray-asSlice-return` 进 `examples/`（ASan 干净）· `ref_arg_too_deep` 仍被挡 · 老例子生成 C 逐字节不变 |
| **档2** 字段级深度 + 取地址跟踪 ⇒ 强更新 | ✅ | `field-strong-update`（放）· `field_weak_update_aliased`（挡）· golden 逐字节不变 |
| **档3** 路径事实 | ✅（sound 子集）| `path-narrowing`（放）· `path_narrow_param_root`（挡）|
| **档4**（新，2026-09-22）**块级逃逸提升**（定案 63 / PLAN #38）| ✅ | 见 §13：这是**旋钮 (B) 的机械化**（`ARENA-FORMAL` §7.2′），不是新的一档抽象：`arenaLevel = min(当前块, 各目的地)` · `tests/asan/` 5 个形状（**拿修前的编译器跑 ⇒ 2 UAF + 3 误拒**）· 攻击库基线一字不动 · golden 只多一个新例子 |
| **§8.5 工程降法** | 部分用到 | ① 惰性闭包（memo + 环保护 + "是否完整"位）✓ ② 实例去重（沿用 codegen 的实例集合）✓ ③ SCC/模板级摘要：**目前不需要**（摘要按函数算、调用图小，未出现超限）⇒ 留作扩展点 ✓ |

---

## 12. ✅ 完成：**描述表 + 通用打印器/比较器**（编译时长优化第二/三步，2026-09-22）

> 目的：把"**每个用到类型派生一份 `_debug` 打印代码**"换成"**每类型一份描述数据 + 全程序一份通用实现**" ✓
> 为什么值得做：合成压测程序（N=1000）里有 **2028 个 `_debug`、占生成 C 的 22.9% 行**，
> 而那个程序**一次都没 `println` 结构体** ⇒ 全是废的 ✗（真实例子里只有 4~9 个 ⇒ 收益集中在"类型多"的程序 ✓）

### 开工前准备的三份产物（都还在，是"换线必须逐字节对齐"的依据 ✓）

| 东西 | 在哪 | 状态 |
|---|---|---|
| 描述表类型 + 通用递归打印器 + 对拍器 | `tools/print-desc.c` | ✅ 跟编译器输出**逐字节一致**（`pair { a: 7, b: 99, xs: [1, 2, 3] }`）|
| 对拍用例 | `examples/print-desc-check.extc` | ✅ 进套件，带 `// expect:` |
| **打印格式真值表** | `tools/print-formats.txt` | ✅ 每种类型都实测抠出来了 |

### 落地结果（四个提交，每个都过一遍双向判据 ✓）

| 步 | 做了什么 | 提交 |
|---|---|---|
| ① | `_debug` **代码 → 描述常量**（struct 用 `offsetof`；枚举变体名表；数组/切片；`?ref`）；描述常量之间的**环**靠"全量前向声明"化解；顺手删掉死代码 `slice_u8_debug` | `86ba904` |
| ② | `println(x)` 结构化类型改吐 `extc_print(&x, &x_desc)`（标量仍 `printf`）；**删掉三个派生器家族** `_debug` / `_writeText` / `_name` | `99ae36d` |
| ③ | 描述表**按需**（AST 层闭包：`descRef` 顺手登记依赖，跑到不动点）；打印运行时也按需 | `2ce6feb` |
| ④ | 结构化 `==` 也走 `extc_eq(a,b,desc)`；**删掉"每数组类型一份 `_eq`"**、`genEqTest`、`typeHasEq` | `b5aa5cb` |
| 🔧 | 顺带撞到并修掉 **PLAN #35**（`alloc<T>(n)` 不算"用了 arena" ⇒ 生成的 C 编不过；`tests/arena` 验收一直空转）| `2e0008a` |

### ⭐ 实测收益（相对本次重构开始前）

| 指标 | 旧 | 新 |
|---|---|---|
| 生成 C 行数（合成 N=1000）| 41,547 | **30,483（−27%）** |
| 其中 `_debug` 派生函数 | 2011 个 / ~11,250 行 | **0** |
| **端到端编译 `extC+gcc`（N=1000）** | 2806 ms | **1535 ms（−45%）** |
| 端到端（N=3000）| 8540 ms | **4545 ms（−47%）** |
| 前端 `extC→C` | 49 ms | 48 ms（**没变** —— 收益全在 gcc 那一侧 ✓）|
| 小例子（hello / tour / gomoku）| 193 / 270 / 231 ms | 191 / 255 / 226 ms（**也略快** ✓）|
| 结构化 `==`（专门压 200 个数组类型）| 1251 ms | **971 ms（−22%）** |

⚠️ **两条反直觉但重要的结论**：
1. **行数只降 27%、时间降 45%** —— gcc 的时间大头是"**很多小函数**"，不是行数。
   换掉 11,250 行*代码*、补上 ~5,000 行*数据*，时间掉将近一半 ✓
2. ④ 那一步**行数还涨了 9%**（7,547 → 8,249）而时间降 22% —— 同一个道理 ✓

### 保住的判据（每一步都跑）

· 用 git worktree 里的**旧编译器**逐个跑 examples + tests：**实际输出逐字节一致** ✓
· `tests/attacks/BASELINE` **一字不动**（没放松任何检查）✓
· `./check.sh`（含全部基准）**全绿**；运行时性能跟 C 的关系没变（bt 仍快于 C）✓
· golden 差异逐行核对：**只有"打印/比较代码 → 描述数据"这一类** ✓

### 当初标的两个待拍板（**都按现状实现**，所以判据没被破坏 ✓）

1. **枚举载荷不印**（`circle(2.5)` → `circle`）—— 保持原样 ✓
2. **null 的 `?ref` 也印 `<ref>`** —— 保持原样 ✓
（描述表**顺手能修**这两条，等主人点头再改，见 `tools/print-formats.txt`）

### 遗留（诚实记账）

· eq-only 的 struct 描述仍带着它用不到的 `fields` 表 —— 实测占数据 ~5%、时间 ~1%，
  现在不值得再拆（要拆就得把"打印可达"和"比较可达"分成两张闭包表）✓


---

## 13. ✅ 档4（新，2026-09-22）：**块级逃逸提升** —— 把命题 7.1 机械化

**它不是"更细的事实"，而是"更长的区域"**（`ARENA-FORMAL` §7 的两个旋钮里的 (B)）：

```
arenaLevel(new) = min( 当前块层, 所有"值流向"的目的地层 )
```

- 起点是 PLAN #38（真 UB：循环里建链表 ⇒ 节点分配在循环体 arena、每轮 release ⇒ 悬垂）
- **只往更深处存 ⇒ 一个数都不变**（零影响）；往更浅处存 ⇒ 按命题 7.1 把分配搬上去 ✓
- 只沿**值的来路**往回找（绑定 / 字面量 / 载荷 / `!` / `??`），**找不到就照旧报错** ⇒ 只放宽到安全那档 ✓
- 落点四步见 `DECISIONS.md` 定案 63 的落地记录（含三个坑：`placeDepth` vs `storeLayer` ·
  `hasHome` 才是权威 · 来路链要压平）

**双向判据**（四档一视同仁）：
| 判据 | 结果 |
|---|---|
| ASan 干净 | ✅ `tests/asan/`（**新支**）5 个形状；**修之前**的编译器：2 个 heap-use-after-free + 3 个误拒（验收真的会咬 ✓）|
| 攻击库 BASELINE | ✅ **一字不动**（"借来的值"那条规则一点没松）|
| 测试 | ✅ 224 → **227** 全通 |
| golden | ✅ **只多一个**新例子（`examples/store-promotion.extc`），其余 81 个逐字节不变 |
| 内存（A2 的保证）| ✅ `tests/arena/promote.extc`：循环里 300×1MB 不涨 + 被提升的链表活到函数结束 ✓ |

**留档**：这条跟 #39（引用型绑定的字段表会刷低 `refDepth`，**修之前就有**的 UB）相邻但**正交** ——
#39 记在 `PLAN.md` + `tests/canary-gaps/` ✓
