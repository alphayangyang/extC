# `PLAN-REGION.md` —— 把形式化推导落进编译器（执行计划）

> 参考 = [`ARENA-FORMAL.md`](ARENA-FORMAL.md) 全部内容（§3 约束系统、§6 证明与记账不变式、
> §7 误拒分析、§8 让 checker 用定理 + §8.5 工程降法）✓
> 目标 = goal「把 ARENA-FORMAL 的推导落进编译器」✓

## 0. 铁律（每一步都必须满足，不然不算做完）

1. **双向判据**（最重要）：
   - **该放的要放**：`tests/canary-gaps/` 里那三条**安全**的形状
     （`return-grown-varArray2`、`varArray-asSlice-return`、`built_list` 的**安全变体**）
     ⇒ 最终要搬进 `examples/` 并带 `// expect:` 断言 ✓
   - **该挡的要挡**：`tests/attacks/BASELINE` **一字不动**（攻击库通过集合不许变）✓
2. **`tools/golden.sh` 的差异必须是"预期的那一处"**：纯检查器改动 ⇒ **必须逐字节相同**；
   若某步**有意**改变分配（比如"家 arena 选择"变了）⇒ 差异必须只是 arena 实参那一类，
   **先人工看过再 `save`**，并在提交信息里写清楚"为什么这处差异是对的"✓
3. **`./check.sh`（全量）** 通过 4、失败 0 ✓
4. **每步一个提交**，提交信息写清：改了什么、判据结果、**有没有预期外的差异** ✓
5. **记账不变式**（§6.6）：任何"能改变某个值内容"的新规则，必须把写进去的东西的深度
   **取上界记回那个值**（`max` 不是覆盖）✓

## 1. 档 1（核心）：效果摘要 + 调用点求解 ⇒ 消掉"规则层"的粗

**要消掉的三件事**：① #34（容量够的容器其视图也返回不了）② "造完返回容器"被拒
③ 规则 ④ 把**地址流**当非空地施加于所有实参（`ARENA-FORMAL` §2.3）✓

### 步骤（每步都能单独验收）

| # | 做什么 | 改哪 | 验收 |
|---|---|---|---|
| **1.1** ✅ **完成**（2026-09-21）| **算摘要**：`FuncDef` 加 `addrMask`/`contMask`/`homeAddrMask`/`homeContMask`（位 = 参数）；区分**目的地**（形参容器 vs **本帧新分配的对象**）与**来源**（地址流 / 内容流 / Fresh）| `src/ast.h`、`src/check_top.c` | **已验收**：`--dump-effects` 打印；golden **逐字节相同**（71 文件）✓ 216 测试全绿 ✓ 攻击库基线不动 ✓ **关键证据**：链表 `push` = `toParam[Addr=0 Cont=0] toHome[Addr=0 Cont=1]` ⇒ **地址流为空**（今天规则 ④ 却按非空处理 ✗）；`sneaky`（`n.owner = ref l.head`）= `toHome[Addr=1]` ⇒ 这类**必须继续挡** ✓ |
| **1.2** | **调用点求解**：`H = ⨆(调用者侧落点, Cont 已知的 δ)`；**只在 `Addr ≠ ∅` 时**施加 `R_slot(arg) ⊒ H`（规则 ④ 收窄成"按实际流"）| `src/check_top.c`（`callHomeDepth`/`checkCallRefArgs`）、`src/check_expr.c`（三处调用点）| 攻击库**仍全挡** ✓；golden 差异只允许出现在"家 arena 实参"那一类 ✓ |
| **1.3** | **删掉甲′**：移除 `raiseMutRefTargets`（它记的深度由 1.2 的求解替代）| `src/check_top.c`、`src/check_expr.c` | 三条 canary 里**安全的那几条变正例** ✓；攻击库不动 ✓ |
| **1.4** | **翻正例**：把三条 canary 从 `tests/errors/` 搬到 `examples/`，各带 `// expect:` 断言；真正的 unsafe 形状（`sneaky` 系）**留在 `tests/errors/`** | `tests/`、`examples/` | 双向判据同时成立 ✓ |
| **1.5** | **文档**：MANUAL §7.9（asSlice 的规矩改成"由推导决定"）、PLAN #31/#34 改状态、DECISIONS 定案、DEVLOG | 文档 | `check.sh` 全绿 ✓ |

### 工程降法（§8.5）在档 1 的落地

- **模板级摘要 + 代入**：摘要按**模板**算（`FuncDef` 是模板本尊）；实例化**只代入**
  （类型参数只影响"会不会装引用"一个布尔 ⇒ 2^k 种形状）⇒ **不要每实例重算** ✓
- **实例去重**：`varArray<i32>` 只算一份（沿用 codegen 的实例集合）✓
- **上限 + 响亮报错**：若某个形状的组合数超限 ⇒ **报错**（不许静默保守）✗

## 2. 档 2：字段/槽位级深度 + 取地址跟踪（消 §7.4 的硬门槛）

- **2.1 取地址跟踪**：每个 `Sym` 一个 `addressed` 位；`ref x` / 传 `mut ref` ⇒ 置位 ✓
- **2.2 强更新**：目标绑定**没取过地址** ⇒ `p.f := e` 可以把深度**覆盖**（不是取 max）；
  取过地址 ⇒ 退回弱更新（保守）✓ ⇒ 消掉"置空救不回来"的误拒（§7.4 命题 7.3 的双方）✓
- **2.3 字段级深度**：`Sym` 按**字段路径**记深度（至少一层：`h.p` 与 `h.q` 分开）
  ⇒ 消 PLAN #8 剩下那半（"字段里的参数引用被按槽位深度算"）✓
- 验收：新增 canary 对（safe 变体必须过、unsafe 变体必须挡）；攻击库不动 ✓

## 3. 档 3：路径事实（利用"链式只读"保证别名稳定）

- **3.1** 收窄支持**只读路径**：`p.next != null` ⇒ 在**整条链只读**（`REFS.md` 的链式只读 ✓）
  且中间没有 `mut ref` 写入的前提下，证明该路径非空 ✓
- **3.2** 深度按路径传播（同理，只读路径）✓
- 验收：`#14` 的用例（`p.next.value` 直接写）能编过；`mut` 链上的同名形状**仍要挡** ✓

## 4. 风险与回滚

| 风险 | 处置 |
|---|---|
| 某步让攻击库放松（soundness 回归）| **立即回滚该步**，把反例贴进 DEVLOG，重新推导（这正是双向判据的用处）✓ |
| 报告形状变差（P′）| 报错必须给**冲突见证**（被违反的边 / 约束环）+ "下一步怎么办" ✓ |
| 编译时间（§8.5 的 M×F）| 守三条降法；超限响亮报错；`bench` 里的编译时间不恶化 ✓ |
| 大改动难定位 | 每步一个提交 + 每步都跑全量 `check.sh` ✓ |

## 5. DoD（完成定义）

- 档 1 完成 ⟺ 三条安全 canary 在 `examples/`（带 expect）**且** `tests/attacks/BASELINE` 未变
  **且** `raiseMutRefTargets` 已删 **且** 文档更新 ✓
- 档 2 完成 ⟺ §7.4 的 `safe()` **能编过**、`unsafe()` **仍被挡** ✓
- 档 3 完成 ⟺ PLAN #14 的用例能编过，`mut` 链上的同名形状仍被挡 ✓
- 全部完成 ⟺ `./check.sh` 全量通过 4/0、`golden` 差异只有"预期的那一处"、PLAN #31/#34/#14/#8 全部收掉 ✓

## 6. ⛔ 档1 步骤 1.2 的两次绊线（2026-09-21，**回滚并留档**）

按铁律"判据不平衡就回滚"，1.2 试了两件事，**两件都被双向判据当场抓住** ✗：

| 试了什么 | 抓它的判据 | 根因 |
|---|---|---|
| **收窄规则 ④**（"`Addr` 全空 ⇒ 跳过那条保守约束"，`ARENA-FORMAL` §2.3/引理 1 的直接推论）| `tests/errors/ref_arg_too_deep` 从**必须报错**变成**通过** ✗ | **效果摘要还没有传递闭包** —— callee 里再转一手（调另一个会存地址的函数）就漏 ⇒ 此时"`Addr` 全空"是**假的** ⇒ 收窄等于放行悬垂 ✗ |
| **用 E 选家 arena**（"实参会被搬出本函数 ⇒ 家 = 我的家"，`ARENA-FORMAL` §9）| `examples/out-param.extc` 从**能编过**变成被拒 ✗ | **E 过度近似**（第一版把"任何表达式语句/条件里出现的名字"都算成逃逸 ✗）⇒ 家 arena 到处变成 `__extc_home` ⇒ 既费内存，又让规则 ④ 的 `h` 归 0 ⇒ 误拒 ✓ |

**已修的部分**（留在树里，只算不用 ⇒ 行为零变化 ✓）：
- E 分析改成**只认"交出去 / 写进更长寿的地方"**（条件、表达式语句、match 主体都不算逃逸）✓
- 规则 ④ 保持**保守**（宁误拒、不漏 UB），并在代码里留了"为什么不能现在收窄"的注释 ✓

**重试 1.2 的两个前提**（缺一个都不许收窄）：

1. **1.2a 传递闭包 ⭐**：按 `ARENA-FORMAL` §8.5 做**惰性**摘要闭包（memo + 环保护，
   拿不准当"有"），并给每个函数记一个"**摘要是否可判为完整**"的位 ⇒ **只有完整才允许跳过规则 ④** ✓
   （骨架已有：`FuncDef.callees` + `#33` 的 `funcAllocates` 那套）
2. **1.2b E 的精度**：E 必须是"**真实逃逸集的超集**，但别把无关名字全塞进来"；
   加一条自检用例：`examples/out-param.extc`（`listDemo` 里 `l` **不逃逸**）⇒ E 必须**不含** `l` ✓

⇒ 判据仍是那一条：**该放的放 + 该挡的挡 + golden 差异只是"家 arena 实参"那一类** ✓
