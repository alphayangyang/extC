# LIBS.md —— **库**该长什么样（extC 编译到 C 这件事，意味着什么）

> **起因**（主人 2026-09-22）：
>
> > 「先讨论库吧。主要是，不可忽略的是，**extC 是编译到 C 的语言**」
>
> 本文只摆方案 + 摆代价，不替主人拍板 ✓（跟 `IO.md` / `MODULES.md` 一样）

---

## 0′. 大白话版（先看这段，看懂了后面可以跳）

**背景一句话**：extC 自己不生成机器码 —— 它把 extC **翻译成 C**，再交给 gcc。
所以"库"这件事，我们手上一直有**两份东西**：extC 源码、和翻译出来的 C ✓

### ① C 里有个开关：这个函数"给不给外面人用"

- `static` = **只给本文件用**；不加 `static` = **外面能调** ✓
- extC 现在**把所有函数都设成"只给自己"**（`static`）—— 这是故意的：
  gcc 一看见"没人从外面调"，就敢放开优化（实测快 **3.2 倍**）✓
- 做模块的时候，**"导出的函数"正好就是"给外面用的"** ⇒ 一件事同时解决两个问题 ✓
- 比喻：**一个团队，内部互相喊名字（`static`），对外的成员挂个工牌（`export`）** ✓

### ② extC 的检查器要"借东西的对方"自报家门

**为什么**：extC 的招牌是"不悬垂"。而"会不会悬垂"取决于一件事：

```
你把一个东西借给别人，你得知道：
  ① 他会不会**转手**给别人？（转手之后，他那边什么时候收摊你管不着）
  ② 他会不会**一直留着**？（你回头要收摊了，他那还拿着你的东西 ⇒ 悬垂）
```

在 C 世界里，**函数就是个黑盒**：编译器不知道它拿你的指针去干了什么 ✗

⇒ 所以规则是：**说明书上写了"我不留你的指针"才放行；没写 ⇒ 按最坏情况拒**（安全，但难用）✓
⇒ 这就是 extC 的库**比别的语言多一份责任**：它是"借东西的对方"，必须自报家门 ✓

```extc
var x: i32 = 5
someLib.remember(ref x)      // ⚠️ 如果这个库把 `ref x` 存到全局，
                             //    函数一返回 x 就没了 ⇒ 以后读到的是垃圾 ✗
```
⇒ 库必须在说明书上说清它**会不会存**（这份说明书 = "效果摘要"，编译器能自动算 ✓）

### ③ 跟 C 的库打交道，要**签字**

C 库**没有说明书**（它不来自 extC，编译器读不懂它）⇒ 只能让**写代码的人签字**：

```extc
extern!("libc") fn read(fd: i32, buf: mut slice<u8>) -> i64
    effects Addr=0 Cont=0        // "我保证：我只往你给的 buffer 里写，不存你的指针" —— 我签字 ✓
```

- **不签** ⇒ 编译器按最坏情况算 ⇒ **你连一个普通的局部变量都传不进去** ⇒ 库等于没法用 ✗
- **签了但说错** ⇒ 出事了那是**你签的字**（跟 `p!` 那种"我知道它非空"同一族 ✓ P′ 允许 ✓）

### ④ 库有**四种给法**（大白话）

| 给法 | 大白话 | extC 好不好做 |
|---|---|---|
| **源码库** | 把**菜谱**给人家，他自己照着做 | ✅ 最容易（啥都不用冻结）|
| **二进制库** | 把**做好的菜**给人家（外加一份"目录"说明）| ⚠️ 得冻结目录格式，谁改一处别人就崩 |
| **给 C 用** | 我们本来就会"翻译成英文菜谱" ⇒ 顺手给英文厨师也行 | ⭐ 白送，但**一发出去就是承诺**（收不回来）|
| **用 C 的库** | 请外面的厨师来做一道菜 | ⚠️ 得说清规矩，**还要签字担责** |

### ⑤ 这跟 IO 什么关系

**IO = 跟操作系统打交道**。C 类语言的 IO 都是"**薄薄一层库 + 几个系统调用**" ✓
我们只有两条路：**编译器亲自给几个原语**，或者**用 `extern!` 声明 libc 的 `read`/`write`** ✓
两条路都要先有"库"这个概念 ⇒ 所以顺序是 **模块 → 库 → IO** ✓

---

## 0. 一句话（给已经懂的人看的浓缩版）

> **编译到 C ⇒ ① 我们白拿一个成熟 ABI（C 就是跨语言接口）② 库有两种消费者（extC 和 C）**
> **③ 但 C 那边没有 extC 的"效果摘要" ⇒ 跨边界必须有人签字。**

---

## 1. "编译到 C"到底给了我们什么、又欠了什么

| | 白拿的 | 欠的 |
|---|---|---|
| **ABI** | C 的调用约定/结构布局/NUL 字符串/链接器**全世界都认** ✓ | 一旦冻结就不能随便改（`slice` 的两字布局、字段顺序）✗ |
| **模块系统的物理层** | `static` vs 非 static + 链接器 = **天然的"导出/私有"实现** ✓ | —— |
| **系统调用** | libc 就在那儿（`read`/`write`/`malloc` 都能声明）✓ | 它们是**没有摘要的黑盒** ⇒ 信任问题 ✗ |
| **生态** | 理论上能调任何 C 库（zlib/sqlite/…）✓ | 类型映射 + 所有权映射 + 谁负责 `free` ✗ |
| **给 C 用** | ⭐ **extC 库能直接给 C 用户**（吐的就是 C + 一个 `.h`）—— Rust/Go 做不到这件事 ✓ | 于是"给 C 的接口"得**冻结**（那是 ABI 承诺）✗ |

---

## 2. ⭐ 关键发现：**模块的 `export` 在 extC 里有物理对应物**

```
export fn readLine(...)      ⇒ 生成 C 的**非 static** + 在生成的 .h 里给原型 ✓
fn rawRead(...)   (模块内私有) ⇒ 生成 C 的 `static` ✓
```

⇒ 这直接**解掉了 PLAN #37 那个悬着的尾巴**：

> #37 的注释写着：「将来若真支持多 TU，这条要改成"**只导出被别的 TU 用到的**"」

**那就是模块系统的 `export`** ✓ ⇒ 以后只有 `export` 的函数丢掉 `static`，
内部函数**照旧 `static`**（向量化那 3.2× 的收益基本保住 ✓）
**模块系统和 #37 互相解决** —— 这是这次讨论最值钱的一条结论 ✓

---

## 3. 库的四种分发模型（extC 各自可行吗）

| 模型 | 谁这么做 | extC 可行性 | 代价 |
|---|---|---|---|
| **① 源码库**（编译时合进同一个 TU）| Go / Zig / Nim / C++ header-only / Rust 的 crate | ✅ **最自然**：一个 `.extc`（或几个）+ 单 TU 输出，连构建系统都不需要 ✓ | 没有二进制分发；每次全量重编 |
| **② 二进制库 + 接口文件**（`.o`/`.a` + 摘要）| C（`.h` + `.a`）· Modula-2（符号文件）· Rust（rlib）| ⚠️ 要接口格式 + ABI 冻结 + 缓存/版本检查（= `MODULES.md` 方案 B）| 构建系统复杂度 ✗ |
| **③ extC 库给 C 用户**（生成 `.h` + `.c`/`.a`）| Nim（`--header`）· Vala（`.h` + `.vapi`）· Zig（`-femit-h`）| ⭐ **白送**：生成的 C + 一个手写/生成的 `.h` ✓ | 那个 `.h` 是**永久承诺**（签字画押 ✗）|
| **④ 消费 C 库**（extern 声明）| 所有 C 类语言 | ⚠️ **需要 `extern!` + 信任声明**（见 §4.3）| 边界一旦跨过去，extC 的保证到那里为止 ✗ |

⇒ **建议顺序**：**① 先做**（就是 `MODULES.md` 的方案 A）→ **④ 紧接着**（IO 需要它）→
**③ 顺手**（几乎白送）→ **② 最后**（那是构建系统的活）✓

---

## 4. extC 库特有的三件事（别的语言没有）

### 4.1 分配的**落点由调用者决定** ⇒ 库 API 天生"按区域参数化" ✓

extC 没有 `malloc`。库要产出数据，就得问调用者要区域（A3 的"家"机制 ✓）：

```extc
// 库里：`h` 落到**调用者选的那只** arena（自己有家传家、没有传当前块 ✓）
fn buildList(n: i64) -> mut ref node { ... return head }

// 调用者：`var l = buildList(10)`（在循环里 ⇒ 每轮回收 ✓，函数级 ⇒ 活到函数尾 ✓）
```

⇒ 好处：**"谁负责释放"这个问题在类型层面就不存在**（没有 free、没有 GC、没有 drop）✓
⇒ 代价：库的**内存行为是它签名的一部分**（`push` 会扩容 ⇒ 调用点按逃逸需求选 arena ✓），
   所以库文档必须写清"我一共要多少、什么时候要" ✓（`ARENA-FORMAL` §5.1 那四条近似就是这份文档）

### 4.2 ⭐ **接口必须带"效果摘要"**（否则调用点一律保守拒）

这是 extC 独有的：`PLAN #43` 之后，调用点的检查**依赖被调者的效果摘要**
（`Addr(j)` 存了 `&arg_j` / `Cont(j)` 存了从 `arg_j` 读到的指针）✓

> 摘要**不完整**（递归 / 有解析不出来的调用）⇒ 调用点**保守拒**：
> "对所有含引用的实参按最坏情况查" ✗

⇒ 所以**库的接口文件里除了类型，还要有摘要** ✓ 这不是新发明：
**Modula-2 的符号文件 / Ada 的 spec / OCaml 的 `.mli` 装的也不止类型**（还有内联体、宏、
spec 的语义），extC 只是把"**效果**"也放进去 ✓
⇒ 落点：`MODULES.md` 方案 B 的接口文件里，导出函数要带
`Addr/Cont/homeAddr/homeCont/otherMask + 是否完整` ✓（现在 `--dump-effects` 打印的就是这一切）
⇒ 附带好处：**源码库也能用**（摘要可以从源码算，不需要建接口文件 ✓ —— 等价于"每次都重新算一遍"）

### 4.3 ✅ **`extern!` 需要"信任声明"**（库讨论里最容易漏掉的一条）—— **v1 已落地，见定案 72**

C 函数是黑盒：编译器**不知道**它会不会把你的指针存下来 ✗
最保守的做法是"全按会存算" ⇒ 那就要**所有 `ref` 实参活得 ≥ 深度 0** ⇒
**等于不能传任何局部** ⇒ 这个库根本没法用 ✗✗

⇒ 所以 `extern!` 必须让**用户声明摘要**（= 签字）：

```extc
extern!("libc") fn read(fd: i32, buf: mut slice<u8>) -> i64
    effects Addr=0 Cont=0            // "我没存你的指针" —— 我签字 ✓

extern!("zlib") fn gzalloc(n: i64) -> mut ref u8
    owned                            // "这块内存归我（谁释放？⇒ 见 §5 的所有权表）"
```

- 声明错了 ⇒ **UB**（跟 `p!`、`unsafe` 同一族：**你签的字**）✓ P′ 允许（"不可证必须响亮"，而签字就是响亮 ✓）
- 声明**漏了**（不写 `effects`）⇒ 退回最保守（几乎不可用但安全 ✓）—— **安全的默认值** ✓

---

## 5. 消费 C 生态：类型与所有权的映射表（要做 `extern!` 时照着填）

| extC | C | 注意 |
|---|---|---|
| `i32`/`i64`/`u8`/`f64`/`bool` | `int32_t`/…/`bool` | 宽度必须**显式**对上（不给隐式 ✓）|
| `slice<u8>` | `const char *` + `size_t` **两个参数** | ⭐ extC 的（指针,长度）比 NUL 字符串**更安全**；跟 C 交互要**转换器**（`cstr` / 带 `\0` 的 buffer ✓）|
| `mut slice<u8>`（出参缓冲）| `char *` + `size_t` | **这是 IO 的主力形状**（调用者给 buffer ✓ `IO.md` 已定）|
| `?ref T` / `ref T` | `T *` | 零成本（定案 ㊻：没有 tag ✓）|
| `mut ref T` | `T *` | C 那边没有"只读"概念 ⇒ **只读承诺过不了边界** ✗（签字时要知道）|
| **`varArray<T>`** | ❌ **不可传** | 它带 `home: ref arena`（arena 指针）⇒ 泄露内部 ✓ 要传就传 `asSlice()` ✓ |
| struct（`export` 的）| `struct` | ⇒ 布局必须**冻结**（要一条"`@c` 布局"的规则，否则改字段顺序就崩 ✓）|
| `print`/`println` | `printf` | 输出要**同一套格式**（现在 extC 的打印器是生成的 ✓）|
| trap（越界/除零）| `abort()` | C 那边返回 `errno` 的，库要**翻译成 `result<T, ioError>`** ✓（`IO.md` 已定）|
| **arena 内存** | `malloc` 块 | 方向 1：**extC 给 C 写** ⇒ 完全安全 ✓（C 只往你给的 buffer 里写）<br>方向 2：**C 给 extC 内存** ⇒ 谁释放？⇒ 用 `IO.md` §5 那个"**帧拥有资源**"形状（`extc_files` 的推广：一个 `extc_owners`）✓ |

---

## 6. 建议的推进顺序（库这条线）

| 步 | 做什么 | 为什么在这个位置 |
|---|---|---|
| **L1** | **模块**（`MODULES.md` 方案 A：源码库 + `export` ⇒ static/非 static ✓）| 库只需要"名字 + 可见性 + 摘要" ✓ 单 TU 全留着 ⇒ #37 的优化不丢 ✓ |
| **L2** ✅ **已落地（2026-09-22 = 定案 72）** | **`extern!` + 信任声明（含 `effects`）** | 这是"**库能碰系统**"的唯一入口 ⇒ IO 的前置 ✓（也是消费 C 生态的入口 ✓）|
| **L3** | **`std.io`**（薄薄一层 wrapper，`IO.md` 档 1/2/3 就位 ✓）| 第一个真客户 ⇒ 用它验证 L1+L2 够不够 ✓ |
| **L4** | **给 C 的 `.h`**（`export` 的函数 + `@c` 布局的结构 ⇒ 生成头文件）| 几乎白送 ✓（但**签了就不能改** ⇒ 可以晚点签 ✓）|
| **L5** | **二进制分发**（`MODULES.md` 方案 B：接口文件 + 缓存）| 构建系统的活；等 L1~L4 稳定了再说 ✓ |

### 什么时候会**卡住**（诚实）

- L2 的 `effects` 声明**写起来烦**（每个 C 函数一行）⇒ 但只写一次，而且**漏写是安全的**（退回保守）✓
- 想给 C 用户用的那个 `.h` ⇒ **一旦发布就冻结**（`slice` 的布局、字段顺序、错误通道）✗
- C 库**偷偷存了你的指针**（比如注册了回调）⇒ 声明成 `Addr=0` 就是**你的 UB** ✓
  （这一条跟 `p!` 一样：**签字是你的责任** —— 正好是 P′ 那条边界 ✓）

---

## 7. ⬜ 要主人拍的（四个问题）

| # | 问题 | 选项 | 我的建议 |
|---|---|---|---|
| **LQ1** | 库**给谁用**（先定这个，别的都跟着它） | 只给 extC · 也给 C（生成 `.h`）| **先只给 extC** ✓（给 C 是**永久承诺** ⇒ 等接口稳了再签 ✓）|
| **LQ2** | 库怎么**分发** | 源码（①）· 二进制 + 接口（②）| **源码** ✓（Go/Zig 那条路；二进制是构建系统的活，往后排 ✓）|
| **LQ3** | `extern!` 的信任声明**最小形状** | 只有 `effects` · `effects` + `owned` · 全都要 | **`effects` + `owned` 两个就够** ✓（第一个防"偷偷存指针"，第二个管"谁释放"✓）|
| **LQ4** | C 结构体布局要不要**现在就冻结** | 冻结（`@c` 属性）· 等真要给 C 用再说 | **等** ✓（冻结是单向门；先让 `export` 的**函数**能出去就够 IO 用了 ✓）|

---

## 8. 验收（先写好再动手）

1. **`std.io` 是普通 extC 模块**：不 import 它就用不了 `file`（`MODULES.md` §7 那条 ✓）
2. **`extern!` 漏写 `effects` 时退回最保守**，而且**报错/警告要教怎么签** ✓
3. **黄金判据**：`examples/` + 两份 OI 横评 + 五子棋 **一行不用改**，golden 逐字节不变 ✓
4. **"库拿不到特权"**：用户模块**无法**声明 `rawRead`（那只有特权模块能 ✓）
5. 常设验收照跑：244 测试 · ASan 8 · arena 5 · 攻击库基线一字不动 · 漂移哨兵零漂移 ✓
