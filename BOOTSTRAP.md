# BOOTSTRAP.md —— 依赖顺序与「鸡生蛋」的破法

> **问题**（主人 2026-09-18）：
>
> > 「你觉得我需要怎么实现这些功能才能够避免鸡生蛋蛋生鸡？」
>
> 本文做三件事：
> ① 把**真的环**和**看着像环其实不是**的分清楚；
> ② 给出**四条破环规则**（每条都有现成的例子）；
> ③ 给出一个**每一步都不依赖后面**的顺序。

---

## 1. 先分清：哪些是真的环

| 你可能担心的环 | 真的吗 | 为什么 |
|---|---|---|
| 用 extC 写编译器，需要容器，而容器要用 extC 写 | **不是环** | 容器的第一版**用现有特性就能写**（`slice`、`option`/`result` 都是这么写的）；`array<T>` 只是「容量会自己涨」的版本 |
| arena 要用 extC 写，而写 extC 需要 arena | **不是环** | arena 不是库，是**编译器生成的运行时原语**（跟 `extc_checkedIndex` / `extc_trap` 一类） |
| 逃逸检查要 arena 才知道深度 | **不是环** | 深度不需要 arena：今天它就是**词法块深度**（几层 `{}`），参数是 0 |
| 全局变量 = 深度 0 的 arena，所以要先有 arena | **不是环** | **定长全局不需要分配**，它就是 C 的静态对象；「深度 0」只是个词法标签 |
| 自举是「全有或全无」 | **⚠️ 这个是真的** | extC 没有 C 互操作 ⇒ 不能一个文件一个文件搬。见 §4 |

**结论：五个担心里四个是假环，一个是真问题。**

---

## 2. 四条破环规则

### 规则一 · 环上必须有一环是「编译器生成的原语」

只要出现「A 要用 A 来实现」，就把 A 拆成两层：

```
上层（extC 源码写）：库、容器、算法        ← 用**已有的**特性写
下层（编译器生成 C）：原语                  ← 不再往下追问「谁实现它」
```

现成的例子（都已经在跑）：

| 上层（extC） | 下层（编译器生成的 C） |
|---|---|
| `slice<T>` 的 `get` / `find` / `==` | `slice_T_index(v, i, f, l)`（带边界检查的取值） |
| `option<T>` / `result<T,E>` 的构造与 `?` | `?` 展开成的三句语句 |
| （将来）`array<T>` 的 `push` / `len` | **arena 的 bump 分配** |

### 规则二 · 「库要用它来分配自己」的东西，只能由编译器提供

`array<T>` 要用 arena，arena 要……**停。** arena 的实现不许再问「谁给它分配」
—— 它直接 `malloc` 一块大内存，然后自己 bump。**这就是链条的底。**

> 一句话：**底由编译器生成，且底的实现里允许出现 C 和 libc。**

（DESIGN 已经把这条定成边界声明了：**标准库实现可以调用 libc（由编译器生成），用户代码不能。**）

### 规则三 · 把「深度」抽象成数字，arena 只是它的一个来源

逃逸检查要的不是 arena，是**两个整数比大小**：

| 深度从哪来 | 什么时候有 |
|---|---|
| 词法块深度（几层 `{}`） | **今天就有** |
| 函数帧 = 1、参数 = 0 | **今天就有** |
| `region r { ... }` = 深度 +1 | 等 arena 那一关 |

所以 **B（逃逸检查）今天就能做**，arena 到了只是给深度**多一个来源**。

### 规则四 · 区分「需要深度标签」和「需要分配」

| 想要 | 需要什么 |
|---|---|
| 全局常量 `let MOD = 1000000007` | **只要深度标签**（0）→ 今天就能做 |
| 全局定长对象 `var board: board` | 同上 → 今天就能做 |
| 全局**变长**对象（全局的 `array<T>`） | 真的需要 arena |

**大部分「我以为是 arena 才能做的事」其实只是缺一个词法标签。**

---

## 3. 顺序：每一步都不依赖后面

| 步 | 做什么 | 需要什么 | 需要堆吗 |
|---|---|---|---|
| **0** | 全局定长变量/常量 + 输入（`readLine`/`argv`）+ `for` | 现有特性 + `option` | ❌ |
| **1** | 引用语义 A（`mut ref` / 解引用写法 / 消灭 `=` 的歧义） | 一个决定（见 `REFS.md` §3） | ❌ |
| **2** | 逃逸检查 B（词法深度，含 `slice` 的规则） | 步 1 | ❌ |
| **3** | 带载荷枚举 + `match` | 新类型机制（标签联合） | ❌ |
| **4** | 模块系统（`import` / `export`） | 无 | ❌ |
| **5** | **arena 原语 + `array<T>` + 动态字符串** | 编译器的 C 原语 + 步 2 的深度 | ✅ |
| **6** | 自举（一块一块搬进 extC） | 上面全部 + `extern` 口子 | ✅ |

> **关键结论：步 0–4 全都不需要堆。**
> 也就是说 —— **鸡生蛋的那个「蛋」（arena）可以在很后面才孵。**
> 自举之前的语言特性，一个都不依赖它。

### 为什么这个顺序不会被后面的需求推翻

因为它**只依赖已知的东西**（`option` 已实现、`for` 是纯语法、词法深度是纯词法属性）。
而它把 arena 排在最后，正好对上两条已有的判断：

- DESIGN 自己说的：**「五子棋需要的东西比自举少得多」** —— 而 G2（五子棋）优先于 G3（自举）。
- arena 的形状**取决于引用规则的最终样子**（`array<T>` 怎么创建、空数组的 `data` 怎么办、深度挂在哪），
  所以它天然应该在 A/B 之后 —— 这跟 `REFS.md` 的结论一致。

---

## 4. 那个**真的**环：自举是「全有或全无」

### 问题

extC **没有 C 互操作** ⇒ 搬到一半的编译器**没法调用还没搬的那一半**（
比如用 extC 写的 lexer 想调到 C 写的 parser）。所以要么一次全搬（6127 行），
要么留一个口子。

「一次全搬」的失败模式很难受：**搬到一半发现缺一个特性，前面全废**。

### DESIGN 已经给了答案（引用原文）

> **`extern` 不是语言特性，是唯一的逃生舱。** 它接的是一个不受 arena 管辖的外部世界 ——
> 所以它天然在 P 的管辖之外，也就必须**显式、可见、默认关闭**（正是 P′）。
> **v0 里不做，v1 作为边界声明引入。**
>
> **定案：标准库实现可以调用 libc（由编译器生成），用户代码不能。**
> 用户写不出 `extern`，但 `string<u8>.find` 内部就是 `memchr`。

### 两条岔路

| | (a) 大爆炸 | (b) 留一个「标准库专用」的 C 调用口子 |
|---|---|---|
| 做法 | 等特性齐了，一次全搬 | 一个文件一个文件搬，没搬的部分还是 C，两边能互相调用 |
| 好处 | 语言表面干净，没有逃生舱 | 每一步都能跑、都能测、都能回退 |
| 坏处 | 搬到一半缺特性 ⇒ 全废；进度不可见 | 要设计一个「只给标准库用」的机制 |

**奶昔建议 (b)**，理由不是「省事」，而是**它让自举变成可验证的增量**：
每搬一块就跑一遍回归（现在有 82 个测试），错了就退那一块。
DESIGN 的「默认关闭、用户写不出」正好把 (b) 的风险压到最低 ——
**逃生舱存在，但 `grep -c extern` 在用户代码里等于 0。**

---

## 4.1 那个口子怎么保证安全（主人问的）

> 主人的问题：**「b 是对的，但是怎么保证安全？」**

**核心答案：安全不来自「相信 wrapper 写得好」，而来自「把不可信面积压到能一条条读出来」。**

四层，从外到内：

### 第一层 · 谁能写 —— 看**来源**，不看语法

判据用现成的 `reserved` 标记：`src/main.c` 在**载入 prelude 时**给它里面的每个
`StructDef` / `TypeDef` / `FuncDef` 打上 `reserved = true`。

> **这是「这个单元是怎么被加载的」，不是「源码里写了个什么词」。**

所以它是**伪造不了的**：用户在自己的文件里写 `extern`，编译器当场报
「这是标准库专用的边界声明」。用注释、用文件名、用一个 `@stdlib` 标记都不行 ——
**那些都能被复制，加载来源不能。**

### 第二层 · 能写多少 —— 一份短到能背下来的白名单

先分清两件容易混的事：

| | 是什么 | 谁提供 |
|---|---|---|
| **原语 primitive** | 语言契约的一部分（`extc_trap`、`extc_checkedIndex`、arena 的 bump、将来的 `readLine`） | **编译器生成**，参数形状由编译器保证 |
| **extern** | 通用的 C 逃生舱 | 标准库源码里显式声明，**要人工审计** |

> **规则：能用原语表达的，就不要用 extern。**
> 原语由编译器生成，`slice` 的 `data` 和 `len` 是**编译器配对**的；
> 而 extern 要靠 wrapper 自己自觉。**能降低一级风险，就不要省那点事。**

于是 stdlib 可调的 extern 是一份**极短**的白名单：

| 类别 | 允许 | 为什么安全 |
|---|---|---|
| 纯计算、不碰堆 | `memchr` `memcmp` `memcpy` `memmove` | 无状态、不持有指针、不分配 |
| 文件 IO | `fopen` `fread` `fwrite` `fclose` | 句柄不透明；不保留我们传的指针 |
| 退出 / 报错 | `exit` `fprintf(stderr)` | 单向，不返回 |
| **显式禁止** | `malloc` `free` `realloc`；任何「注册回调」「保存指针」的 API | 分配只归 arena（G1）；回调会让指针活过调用期 |

⚠️ `malloc` **不算** stdlib 的 extern —— 它是 **arena 原语**的内部实现（规则二）。
**G1（用户永不写 malloc/free）因此不受影响。**

### 第三层 · 怎么用 —— 六条可查规则

| # | 规则 | 挡住的是 |
|---|---|---|
| 1 | **返回类型不许是 `ref T`** | C 会返回 NULL，而 `ref` 不可为空 —— 让 NULL 进来就是把这条承诺作废 |
| 2 | **`slice` 的长度由编译器从 slice 取**，不许调用点另传一个数字 | 「长度和缓冲不一致」这类经典 C bug 在边界上复活 |
| 3 | **借出只在调用期间有效**：不许把指针交给「C 会存下来」的 API | 悬垂指针（这也是白名单要人工看一眼的那一条） |
| 4 | **错误必须变成 `result<T,E>`** | errno 穿出来 |
| 5 | **公开签名里不许出现来自 C 的 `ref`** | 用户拿到裸指针 ⇒ 逃逸检查失效 |
| 6 | **`extern` 出现的地方可数**（见下：审计） | 「逃生舱长出几百个」 |

⚠️ **第 1 条有个现成的坑**：`option<ref T>` 今天**造不出来**（`ref` 没有零值，
见 DECISIONS 33′）。所以「可能为 NULL 的 C 指针」**必须死在 wrapper 里** ——
要么 arena 里分配成非空，要么用句柄（`i64`）表。**NULL 不许穿进 extC 的类型系统。**

### 第四层 · 混合编译期怎么不出事 —— **对拍**

自举会有一段时间「C 和 extC 混着编」，那个接口是 bug 的温床。两手：

1. **每搬一块就跑全套回归**（现在 82 个测试当契约），错了就退那一块。
2. **对拍**：同一个输入，用「C 版编译器」和「extC 版编译器」各跑一遍，
   **比较生成的 C 逐字节相同**。

   这一招特别适合编译器 —— **输出是纯文本，可以逐字节比**。
   而且因为一次只搬一块，一旦有差异，**当场就知道是那一块的问题**。

   （这正是奶昔写树状数组时用的同一招：BIT vs 暴力求和，2 万次零不一致。
   同一把锤子，砸的是同一类钉子。）

### 审计：把「少」变成可验证的

DESIGN 里给 `!` 定的那个用途，正好能用在这里：

> **危险可以在源码里数出来** —— `grep -c '!'` 就是一次逃生舱审计。
> **而且它是稀缺的** —— 一旦 `!` 长出几百个，它就跟 `interface{}` 一样失去意义了。

所以第 6 条规则应该是：**`extern` 声明必须带签字符号**（设计里已有 `!`：

| 写法 | 说的是 |
|---|---|
| `a[i]!` | 越界会 trap，我知道 |
| `@recursive!` | 用真调用栈，可能栈溢出，我知道 |
| **`extern! fn memchr(...)`** | **这个函数不受 extC 管辖，我签字** |

于是 `grep -c 'extern!'` == **逃生舱的数量**，一个数字。

> ⚠️ **诚实说明**：**后缀 `!`（作为签字）现在还没实现** ——
> 现在只有前缀 `!`（逻辑非）。所以这一条要连带把「签字」机制建起来。
> 在那之前可以用 `extern` 关键字本身当锚点（`grep -c '^extern'`），
> 等 `!` 落地再统一。**两种数法的数字都应该很小 —— 那才是关键，不是用哪个符号数。**

### 一句话

> **extC 的安全性来自「默认全开」**：arena + 逃逸检查 + 不可为空引用 + 零初始化 + 边界检查。
> `extern` 是**唯一的例外**，而例外的代价被三件事压住 ——
> **少（白名单）、窄（六条规则）、可数（grep）。**
>
> 目标也不是消灭它，而是**让它一直保持稀少**：能用原语就不用 extern，
> 能进白名单就不新开口子。

---

## 4.2 库策略：哪些 C 库值得包（主人问的）

> 主人的问题：**「但是别的库怎么办，C 不是有很多库吗，哪些有价值哪些没有？」**

### 判据：价值在**算法**还是**平台**，而不是在「C 缺的东西」

> **一个 C 库值得包，当且仅当它的价值在「算法复杂度」或「平台接口」，
> 而不在「C 缺少某个语言特性」。**

因为**一大批 C 库存在的理由就是「C 没有字符串/容器/泛型」** ——
glib、APR、各种 `libc` 的 `str*` 封装、几百个容器库，全是这类。
**它们的价值在 extC 里等于零** —— extC 自己的 stdlib 本来就该提供更好的
（`slice<u8>` 比 `char *` 好，这是整个项目的出发点）。
包它们等于**把 C 的缺点接回来**。

### 成本模型：包装成本 ∝ **状态面**，不 ∝ 行数

| 库的形态 | 包装成本 | 风险 |
|---|---|---|
| **纯函数**（`memchr` `memcpy` `libm` 的 `sqrt`） | 几行 wrapper | 极低 |
| **缓冲区进、缓冲区出**（`zlib` 的 `compress`） | 十几行 | 低（不持有指针就行） |
| **自己分配、自己持句柄**（`SQLite` `libpng` `libcurl`） | 要一整套生命周期机制 | **高**（见下面 G1 的冲突） |
| **对象图 + 回调 + 宏**（GTK `LLVM`） | 一个全职项目 | 极高，不做 |

**跟库有多少行代码关系不大** —— 跟它有多少「活过单次调用的状态」关系最大。

### 四类清单

| 类别 | 例子 | 价值 | 说明 |
|---|---|---|---|
| **算法重、无状态** | `libm` `zlib` `stb_image` `PCRE2` `mbedtls` / `libsodium` | ★★★ | 没人想重写；而且包起来最安全。**加密尤其：绝对不要自己写** |
| **平台接口** | POSIX、文件、网络、线程、`termios` / `ncurses` / `SDL` | ★★★ 必需 | **这些不是「库」，是操作系统** —— 不走 C 边界就碰不到 |
| **自己管生命周期** | `SQLite` `libpng` `libcurl` | ★★ 高但贵 | 需要「作用域资源」机制（现在的 arena 只管内存，不管句柄） |
| **价值就是「C 缺的东西」** | glib / APR / `str*` 家族 / 各种容器库 | ☆ **零** | stdlib 自己该有，别接回来 |
| **巨型框架** | GTK / Qt / LLVM / ICU | ☆ 不做 | 会把 extC 变成「另一门语言的胶水」 |

### 三档机制：库怎么进来

| 档 | 是什么 | 用户看得见 C 吗 |
|---|---|---|
| **1 · 原语** | 编译器生成（文件 IO、`readLine`、终端 raw mode） | 看不见，当普通函数用 |
| **2 · stdlib wrapper** | stdlib 源码里用 `extern` 包好，`reserved` 保护 | 看不见，而且**改不了** |
| **3 · 用户自建** | v1 的边界声明（`extern!`） | **看得见，而且必须签字** |

⚠️ **第 3 档改变的是语言的承诺**：一旦用户能调 C，
「安全」就从**绝对**变成**默认**（`extern!` 之外仍然安全）。
这跟 `!` 的语义是一致的（`!` = 我签字），但要意识到：
**那时候的 extC 是「默认安全」，不是「保证安全」。**

### ⭐ 对主人的五子棋：答案是「一个库都不需要」

DESIGN 里那两个可执行文件的分工，**已经把这个问题解决了**：

| 可执行文件 | 依赖 |
|---|---|
| `gomoku`（TUI 前端 + 命令层 + 对局管理） | **ncursesw** |
| `gomoku-engine`（纯协议引擎，零 UI 代码） | **libc**（v2 可能加 pthread） |

而且 DESIGN 定了：**「TUI 永远 spawn 引擎子进程，不在进程内链接引擎代码」**、
**「TUI 与引擎走 stdio 管道」**。

> **所以 extC 版的五子棋 = 引擎，零库依赖。**

- 引擎是**纯计算 + stdin/stdout 上的 ASCII 行协议** ⇒ 正好是 extC 最擅长、
  而且**完全不需要任何 C 库**的形态。
- ncurses 那半边**本来就在 C 那边**，一行都不用搬。
- 当初定「引擎/前端分进程」的**目的是可独立迭代（答辩素材）**，
  **副作用是引擎零库依赖** —— 设计提前解决了一个未来的问题。

**如果哪天想用 extC 写 TUI**，最省的路不是包 ncurses，而是
**`termios`（raw mode 开/关）+ ANSI 转义序列** ——
那是一个**很小的原语**（读一个字节 + 开关 raw 模式），比包整个 curses API 便宜得多，
而且 ANSI 转义是纯字符串输出，extC 写起来很自然。

### 一个要拍板的冲突：G1 vs「自己管生命周期的库」

DESIGN 的 **G1** 是「**永不写 `malloc`/`free`/`close`**」。
但任何自己管理资源的库（SQLite 打开数据库、libcurl 持有连接）都要求你**显式关闭**。

两条路：

| | 做法 | 代价 |
|---|---|---|
| **(a)** | 把「**作用域资源**」做成机制：编译器在作用域退出时生成 cleanup | 这其实就是 RAII，但**由编译器生成、只给资源类型用**，不是用户可写的 `defer` |
| **(b)** | 这类库 v1 之前一概不包 | 简单，但 SQLite/libcurl 就没了 |

**奶昔建议 (a) 但排在很后面** —— G2（五子棋）不需要它，
而且它的形状取决于 arena 的最终样子（DESIGN 已经说过「file → OS 句柄也在 arena 里」，
(a) 只是把这句话推广到外部库的句柄）。

---

## 4.3 ISO C：基线要先做，但只需要碰其中一小块

> 主人的定调：**「我做肯定是先做 ANSI C（或者 ISO C）」**
>
> 对 —— 第三方库是后面的事，**ISO C 才是基线**。而它的好消息是：
> **ISO C 的接口面可以量化，而且比想象的小得多。**

### 数据：编译器自己只用了 18 个 libc 函数

（`grep` 出来的真实调用次数）

| 函数 | 次数 | 头文件 | 换成 extC 之后 |
|---|---|---|---|
| `strcmp` | **106** | `<string.h>` | **消失** → `slice<u8>` 的 `==`（prelude 里，已实现 ✓） |
| `strlen` | 6 | `<string.h>` | **消失** → `.len`（一个字段，零成本） |
| `printf` `fprintf` `fputs` `vsnprintf` `snprintf` | 52 | `<stdio.h>` | **大部分消失** → `println` 是**编译期展开**；拼串用 StringBuilder |
| `memset` `memcpy` `memcmp` | 11 | `<string.h>` | **留着**（编译器能为它生成 SIMD） |
| `fopen` `fread` `fwrite` `fclose` | 8 | `<stdio.h>` | **留着** → 原语 |
| `malloc` `free` | 4 | `<stdlib.h>` | **只给 arena 原语** |
| `exit` | 4 | `<stdlib.h>` | **留着** → trap 原语 |
| `time` | 1 | `<time.h>` | 可选原语 |

> **结论：ISO C 只需要做大约 10 个函数，就能写自举编译器。**

### ISO C 的 29 个头文件，extC 只需要碰 6 个

| 档 | 头文件 | 怎么处理 |
|---|---|---|
| **① 整个消失** | `<stdint.h>` `<stdbool.h>` `<stddef.h>` | extC 已经内建了它们：`i32`/`u8`/`bool`，`size_t` 一律用 `i64`，`NULL` 不存在 |
| **② 包成原语 / wrapper** | `<string.h>` 的 **mem\***、`<stdio.h>` 的文件 IO、`<stdlib.h>` 的 `exit`、`<math.h>`、`<time.h>` | 有价值、要包，而且**大多数能包得很薄** |
| **③ 价值为零（语言特性取代）** | `<string.h>` 的 **str\***、`<stdio.h>` 的 `printf`/`scanf`、`<ctype.h>`、`<stdlib.h>` 的 `atoi`/`rand`/`qsort`、`<errno.h>`、`<assert.h>`、`<limits.h>` | 每条都要能说清「为什么 extC 不需要」 |
| **④ 永远不要** | `<setjmp.h>` `<locale.h>` `<signal.h>` `<stdarg.h>`（语言层）`<fenv.h>` | 隐藏状态 / 非局部跳转 ⇒ **直接违反 P** |

### ⭐ 最值钱的一条切分：`mem*` vs `str*`

> **`str*` 家族的全部价值来自「C 字符串没有长度」。extC 有长度 ⇒ 价值为零。**
> **`mem*` 家族的价值来自「编译器能把它内联成一条指令或用 SIMD」⇒ 有价值，值得留。**

- `strlen(s)` → extC 里就是 `s.len`：**一次字段读取**。
- `memchr(p, c, n)` → glibc 里是手写 AVX2：**真优化**，别重写。

这条切分能直接推广到别的库：**问「它的价值是不是来自 C 的某个缺失」** ——
是，就不值得包（这正是 §4.2 的判据）。

### 几个具体的「零价值」要能讲出为什么

| ISO C 的 | 为什么 extC 不需要 |
|---|---|
| `<ctype.h>` 的 `isdigit(c)` | 参数是 `int`，传负字节（`(char)0x80`）**是 UB** —— ISO C 最经典的陷阱之一。extC 里写 `c >= 97 && c <= 122`（显式字节比较，没有陷阱） |
| `<errno.h>` | **隐藏的全局状态**，而且「失败靠读一个全局变量」正是 P 要禁的形态 ⇒ `result<T,E>` 取代 ✓ 已实现 |
| `<stdio.h>` 的 `scanf` | 不可静态检查的格式串 + 缓冲区溢出温床 ⇒ `readLine` + `parseInt` |
| `<stdlib.h>` 的 `qsort` | **它要函数指针，而 extC 没有闭包/函数指针** ⇒ 包不了。正确做法是 prelude 里的**泛型排序 + 「序」协议**（像 `==` 那样让类型定义 `fn <`）—— **C 用回调解决的问题，extC 用协议 + 单态化解决，而且没有间接调用** |
| `<stdlib.h>` 的 `rand` | 质量差、全局状态、不可复现 ⇒ prelude 里给一个好的 PRNG（`examples/fenwick.extc` 已经手写了一个） |
| `<assert.h>` | 归编译器管（trap 机制 ✓ 已有） |
| `<limits.h>` | 常量属于**语言**（`i32` 的最大值）—— ⚠️ 但 extC **还没有全局常量**，而且 `i32::max()` 这种「内置类型的关联函数」现在也不支持（`::` 只对 struct 实现了）⇒ 这是个待补的小口子 |

### 顺序（做 ISO C 的时候）

| 步 | 内容 | 为什么先 |
|---|---|---|
| 1 | `mem*` 家族（5 个）+ `<math.h>` | 最小、最安全、`str*` 的替代品都要用它 |
| 2 | 文件 IO（`fopen`/`fread`/`fwrite`/`fclose`） | 读源文件、写生成代码 —— **自举的硬需求** |
| 3 | trap 侧的 `exit` + `stderr` | 已经有 `extc_trap` ✓ 只差把它正式化成原语 |
| 4 | `time`（可选） | 超时、bench |
| — | **其余一概不做** | 由语言特性取代 |

> **关键呼应**：「先做 ISO C」跟「自举」这两件事**重合**——
> 因为**编译器自己就是 ISO C 用户**。所以「支持 ISO C 所需的函数面」
> ≈「自举所需的函数面」≈ **那 10 个函数**。
> 这把自举的门槛一下子压到了很低的水平。

---

## 4.4 兼容模式与沙箱：能买到什么，买不到什么

> **主人的想法**：允许内联 C 之后，可以有一种兼容模式跑原生 C 代码，
> **跑的方式是沙箱**。

### 对的那一半：沙箱把「**安全问题**」变成「**可靠性问题**」

| | 不加沙箱 | 加了沙箱 |
|---|---|---|
| C 的 UB 能伤到谁 | **extC 的内存**（把 extC 的不变量也搞坏） | **只有它自己**，最多返回垃圾 |
| 后果 | 语言承诺作废 | **落进 `result<T,E>` 的领域**（垃圾可以被检查、被丢弃、被重试） |

这是**唯一**能让「C 生态」和「extC 的安全承诺」共存的路子。
靠审计（§4.1 那六条规则）撑不住大库 —— **靠沙箱可以**。

### 要小心的一半：**三种沙箱，只有两种给内存安全**

| 档 | 形态 | 资源隔离 | **内存安全** | 成本 |
|---|---|---|---|---|
| **1 静态沙箱** | `extern!` 的声明名单 = 允许碰的外部世界 | 设计上 | ❌ | **零** |
| **2 进程沙箱** | 子进程 + 字节协议（stdin/stdout） | ✅ rlimit / seccomp | ✅ **操作系统给的地址空间隔离** | 一次 IPC |
| **3 WASM 沙箱** | C → wasm32 + 宿主 | ✅ 能力模型 | ✅ 越界**变 trap** | 一个独立项目 |

**第 1 档不是沙箱，是审计** —— 它就是 §4.1，今天的设计。

**第 2 档最便宜，而且主人自己已经在用了**：

> gomoku 的「**TUI 永远 spawn 引擎子进程**」+「走 stdio 管道」
> —— **那就是一个进程沙箱。**

**第 3 档要说得准确**：WASM 给的是**内存安全**
（越界写被 trap 挡住、指针碰不到宿主内存），**不是「C 语义安全」**——
有符号溢出、求值顺序、同一线性内存里的 use-after-free 仍然是 UB。
所以 WASM 的价值是「**把一部分 UB 变成 trap**」，不是「消灭 UB」。

### ⭐ 粒度决定工具（最关键的一条）

| 粒度 | 用什么 | 为什么 |
|---|---|---|
| **每次调用**（`memchr`） | **薄 wrapper + 审计** | 每次 IPC 就毁掉了 memchr 存在的意义 |
| **每个库**（SQLite / PNG 解码） | **进程沙箱** | 粗粒度，一次 IPC 可接受；能把「不可信」关在外面 |
| **每个程序**（插件 / 用户脚本） | **WASM** | 要跑的是**别人的代码**，而且需要细粒度调用 |

> **沙箱是按「粗粒度边界」用的，不是按「每次函数调用」用的。**

推论：**沙箱不替代 §4.1，它补充 §4.1。**
- 细粒度、可信（ISO C / libc / 白名单里的库）⇒ 薄 wrapper + 六条规则
- 粗粒度、不可信（第三方大库、用户插件）⇒ 沙箱

### 沙箱买不到的（四条，都要说清）

1. **它不修「算错了」。** 沙箱只限定破坏范围 —— 所以**六条规则仍然需要**
   （类型、`slice` 长度配对、NULL 死在 wrapper 里、错误变 `result`）。
   **两条腿，不是二选一。**
2. **它不给你性能。** 每次跨边界都要拷贝 / 序列化。
3. **它自己是新的攻击面**（宿主接口 / ABI）。
4. **它是 extC 会引入的最重的运行时机制**（见下）。

### 跟 P 的关系（这条必须讲清楚，否则会被当成破例）

沙箱听起来是「运行时机制」，跟「凡是编译期能证明的，运行时不留痕迹」冲突。**不冲突**：

- P 管的是**语言的默认机制** —— GC、异常、vtable 之所以被禁，是因为它们**隐式**发生
- 沙箱是**显式签字的例外**（`extern!` / `sandbox`），完全符合 **P′**（不能证明的，语法上必须看得见）

> **沙箱不是「语言的运行时」，是「跟外部世界的边界」。**
> 边界本来就在 P 的管辖之外 —— DESIGN 对 `extern` 说的那句话原样适用。

### 还有一个漂亮的副作用：对拍变得天然

进程沙箱的边界是**字节**，所以一次交互可以**完整记录并重放**。
主人 DESIGN 里的「协议 + 日志」正是这个形状 —— 于是：

- 沙箱里的组件可以**离线重放**调试（不用重现整个程序状态）
- 换实现时可以做**差分测试**：同一串输入喂给两版实现，比较输出。
  **跟 §4.1 第四层（自举期的对拍）是同一把锤子。**

### 建议的落法（顺序）

| 步 | 做什么 | 前提 |
|---|---|---|
| 1 | **静态沙箱**（= §4.1 那六条规则 + 白名单） | **今天就能做** |
| 2 | 需要包大库时，上**进程沙箱**：统一形态 = 「一个子进程 + 一个字节协议 + 一个 `result<T,E>` 的接口」 | 借用 gomoku 已有的引擎协议形状 |
| 3 | WASM **只在有明确需求时**做（比如「要跑不可信的用户插件」） | ⚠️ 自己写解释器是一个独立项目；用 wasmtime/wasmer 就引入一个大依赖（而且它自己也是 C/Rust） |

**奶昔的建议**：1 现在就做，2 等第一个「真的大到不值得自己写」的库出现时再做，3 先别想。
理由跟 §4.2 一致：**大部分需求不需要沙箱，需要的那个需求出现之前，
沙箱的形态也没法定下来。**

---

## 4.5 沙箱当一等公民：为什么它在这里特别值

> **主人的想法**：「extC 不仅安全，还能在**不肘击主进程**的情况下跑 C 代码 ——
> 这不是神了。我甚至可以允许沙箱包裹 extC 代码块。」

### 先说诚实的话：机制的哪一部分是新的

| 部分 | 新吗 |
|---|---|
| 进程隔离、能力限制、把不可信代码关起来 | **不新**（CGI / Chrome 渲染进程 / seccomp / WASM 都是） |
| **跨边界是「有类型的」**：编译器从类型生成编解码 | **少见** |
| **跟「值语义 + 无异常」组合起来** | **真的少见** |

所以「神」的地方不是沙箱，是**沙箱变成了语言里一个五行的东西，而且跨过去的东西是有类型的、
不是一串要自己 parse 的字节**。

### ⭐ 最大的收获：沙箱 = **不用异常的错误恢复**

这条奶昔觉得比「跑 C 代码」更值：

> **extC 现在没有从错误里恢复的办法** —— 越界 trap 就是 `exit(1)`，**整个进程死**。
> 沙箱把「trap」从「进程死亡」变成「**一次失败的调用**」，调用者拿到
> `result<T, sandboxError>` 可以处理它。

于是 extC 得到了一条罕见的组合：

| | 拿到什么 |
|---|---|
| extC 的静态安全 | 「这段代码**不可能**有 UB」 |
| 沙箱 | 「这段代码就算有 UB / 是 C / 是恶意的，**也伤不到别人**」 |
| 两者叠加 | **可证明的安全 + 被强制的隔离**，而且**没有异常、没有栈展开** |

而且它**符合 P**：失败不是靠隐藏的控制流（异常）传播的，而是靠**一个在边界上显式的值** ✓
这正是 DESIGN 说的「错误归 `result`」的延伸 —— 只不过边界从函数变成了进程。

**顺带的三个具体好处**：

1. **硬资源限制**：给组件定 CPU 时间 / 内存上限，由 **OS 强制**（不是协作式的自觉检查）。
   ⚠️ 这对主人的五子棋是**直接命中**：引擎搜索的**超时**正好是这个东西
   （DESIGN 里就有超时，现在是靠协作检查，沙箱可以靠 OS 剪掉）。
2. **崩溃即重启**：组件挂了可以重开（crash-only 设计），主进程不受影响。
3. **热替换**：换一个组件的实现，接口不变。

### 为什么在 extC 里特别便宜：**编解码可以由编译器生成**

跨边界要序列化。一般语言里这是**手写**的（`gob` / `json` / 手工读字节）。
而 extC 的编译器**本来就在遍历任意类型的所有字段** —— `_debug`、`==`、零值、
结构体字面量，全是这套机制。所以「按类型生成 encode/decode」**用的是同一台机器** ✓

> 这是「extC 有值语义 + 编译器已经会走类型」带来的红利。
> 换一门有别名、有析构顺序、有 vtable 的语言，这件事要贵得多。

而且约束很自然：**含 `ref` 的类型跨不了边界**（指针在别的地址空间没有意义）——
这跟 `option<ref T>` 造不出来（§4.1）是同一类约束，而且它**自动**把危险类型挡在边界外。

### 但要纠正一处：边界必须是**被声明的**，不能是「隐式捕获的代码块」

主人说「沙箱包裹 extC 代码块」—— 形状上对，但有个必须钉住的地方：

> **什么跨过边界、跨过去的类型是什么，必须在源码里看得见（P′）。**

如果是「一个块，隐式捕获周围变量」⇒ 那就是**隐藏机制**（哪些变量被拷过去了？
多大？有没有 `ref`？），跟 P′ 直接冲突。

所以正确形状是**声明的边界**，块只是它的一种用法：

```extc
// 组件沙箱：一次 spawn，多次调用（粗粒度）
sandbox codec {
    fn decode(buf: slice<u8>) -> result<image, codecError>   // 接口写在这里
    caps { }                                                  // 能力列表：空 = 什么都碰不到
}

// 块沙箱：跑一次，失败是值（细粒度用法的补充）
let r = sandbox call riskyCompute(args)     // → result<i64, sandboxError>
```

两种用法的差别只有一个：**spawn 几次**。协议、编解码、错误映射是同一套。

### 粒度纪律（这条决定它会不会变成性能灾难）

| 用法 | 划算吗 |
|---|---|
| **一次调用干很多活**（解码一张图、跑一次搜索、渲染一帧） | ✅ 这就是它存在的理由 |
| **会话式**（spawn 一次，后面 N 次调用只付 IPC 的钱） | ✅ gomoku 引擎就是这个形状 |
| **热循环里调小函数**（`memchr`） | ❌ 一次 IPC 就毁掉一切 |

> **沙箱边界上的接口必须是「粗活」。** 这也正好解释了为什么 §4.1 的薄 wrapper
> 和这里不冲突：**一个是细粒度可信，一个是粗粒度不可信。**

### 代价（四条，别只看好处）

1. **可移植性死在原语里**。`fork`/`exec` 是 POSIX；Windows 是 `CreateProcess`。
   ⚠️ **主人的环境同时有 WSL 和 Windows**，所以这一条是真实成本，
   而不是理论问题。**沙箱原语是 extC 里最不可移植的一块** —— 它应该被明确标注。
2. **调试变难**：错误跨了边界，两边都要有好的追踪（好在边界是字节，**可以完整记录并重放** ✓ 见 §4.4）。
3. **它是 OS 级原语** ⇒ 跟 `readLine` / 文件 IO 一样归**编译器生成**，不是 extC 源码。
   （帧协议、错误映射这些**可以**用 extC 写 ✓）
4. **它买不到「算对了」**：隔离只限定破坏范围 —— 所以 §4.1 的六条规则仍然要。

### 对主人的两个项目各意味着什么

| | 沙箱带来什么 |
|---|---|
| **五子棋** | 引擎**本来就是子进程** ⇒ 沙箱化几乎零成本；换来**超时/内存由 OS 强制**、引擎崩了主进程还在、协议可重放对拍。**这是 DESIGN 已经写好的形状，只是把它从「约定」升级成「机制」** |
| **extC 编译器** | ⚠️ **不划算**：编译器的各阶段共享巨大的 AST，跨边界要序列化整棵树。**「把编译阶段沙箱化」听着酷，但成本远大于收益** —— 值得说清楚，免得以后走弯路 |

### 建议

**不要把沙箱排进主路线。** 它的形状取决于第一个真实需求（哪个库？超时？插件？），
而现在（G2）那个需求还没出现 —— 五子棋的进程边界是**约定的协议**，
已经够用，而且**它随时可以升级成机制**（因为它已经是字节边界了）。

**要做的话，第一步是最小的那个**：让 `sandbox` 组件能用「一个子进程 + stdio 协议」
跑起来，接口是 `result<T,E>`，能力列表默认空。就这些。

---

## 5. 依赖顺序 ≠ 优先级：靶子的选择

> **主人 2026-09-18 的定调**：
>
> > 「我的目标也不只是五子棋，如果只是为了让我方便写五子棋的话那我直接用 C 写也不是不行。
> > 主要是我就是觉得 C C++ 都太垃圾了（C 太原始 C++ 太混乱），
> > Go 我觉得很好但是不够安全，Rust 太陡峭了。」

### 这段话修正了本文 §3 的一条论证

§3 的结论「**步 0–4 都不需要堆**」是**依赖顺序**上的事实，它没错，仍然成立：
arena 不会挡住它前面的任何一步。

但奶昔当时顺手用「**五子棋不需要 arena**」当过「arena 可以排最后」的**理由** ——
**这条理由站不住**，因为：

| | |
|---|---|
| 五子棋**不是目标**，它是**冒烟测试** | 目标是语言本身 |
| **五子棋测不出 extC 的核心主张** | 它用的是定长数组 + `while` + 递归 —— **这些任何语言都有**。arena、逃逸检查、引用规则**一个都没被验证** |

> **所以「五子棋不需要 arena」不是「arena 不重要」，
> 而是「五子棋这个靶子不够格」。**

**依赖顺序和优先级是两件事**：arena 可以最后做而**不阻塞**任何东西；
但如果语言的身份才是目标，那就该**主动**把它提前。

### 定位：把主人的四句话翻译成设计取舍

主人对四门语言的判断，正好能逐条翻译成 extC 的具体决定：

| 抱怨 | 具体在哪 | extC 的答案 |
|---|---|---|
| **C 太原始** | 没有字符串、没有容器、没有泛型；`char *` 靠 `\0` 猜长度；`static` 三层含义；手工 GC | `slice<u8>` 带长度；prelude 里的容器；泛型单态化；**arena + 逃逸检查（G1：永不写 `free`）** |
| **C++ 太混乱** | 一个特性有五种写法；重载/模板/SFINAE/隐式转换；UB 藏在语法里 | **一门语言一件事**；无隐式转换（只允许无损失拓宽）；`!` 是唯一的签字符号；统一规则、无特例 |
| **Go 很好但不够安全** | `nil`、运行期 panic、没有 sum type、`interface{}`、GC 停顿 | **不可为空引用**；trap 带 extC 位置；`option`/`result` + `?`；无 interface/反射；**无 GC**（arena） |
| **Rust 太陡峭** | 要**写**生命周期、借用检查器跟编译器打架、`unsafe` 边界模糊 | **深度是词法的** —— 比两个整数，**用户不写生命周期**（DESIGN §111 原话）；**唯一的逃生舱是 `extern!`，可 `grep` 数出来** |

> **一句话定位**：**Rust 的安全保证 + 不写生命周期 + Go 那么少的语法量。**

### 靶子：三个，各测不同的东西

| 靶子 | 测什么 | 现在能写吗 |
|---|---|---|
| **1 · 五子棋引擎**（冒烟） | 数组、切片、递归、`result`、stdio 协议、输入 | ✅ 基本能（缺输入） |
| **2 · 一个需要「生命周期」的小程序**（核心） | **arena、逃逸检查、视图** —— 例如「源码 → token 视图 → AST（arena 里的节点）→ 求值」，错误用 `result` 传播 | ⚠️ **能跑，但会绕过核心机制**：用固定容量池代替 arena，用「我知道它安全」代替逃逸检查 |
| **3 · extC 编译器自己**（自举） | 全部 | ❌ 差 §3 那 10 个函数 + 动态数组 |

**关键认识：靶子 2 才是能验证语言的那一个。**
它今天**能写**（固定容量池 + `while`），但那样写出来的程序**测不出任何承诺** ——
它只是「用 extC 语法写的 C」。

> **要让它真的测出承诺，需要 A（引用语义）+ B（逃逸检查）。**
> 这也是为什么 §3 的表里 **步 1 和步 2 值得优先** ——
> 它们不是「顺便做的收尾」，**它们是 extC 成为 extC 的那一步**。

### 对 §3 顺序的唯一修改

**不动依赖顺序，只改优先级标注**：

| 步 | 内容 | 依赖上可以排在哪 | 但建议的优先级 |
|---|---|---|---|
| 0 | 全局定长 + 输入 + `for` | 随时 | 低（**能让靶子 1 跑起来就够了**） |
| **1** | **引用语义 A** | 随时 | **高** ← 改 |
| **2** | **逃逸检查 B** | 跟着 A | **高** ← 改 |
| 3 · 4 | 带载荷枚举 + `match`、模块 | 随时 | 中（靶子 2 和 3 需要） |
| 5 | arena + `array<T>` | 在 A/B 之后 | 中高（**靶子 2 的 arena 版本需要**） |
| 6 | 自举 | 最后 | 目标本身 |

---

## 6. C 后端：什么能转译，什么要小心

> **主人的问题**：「这么搞完之后我的代码最后还能被转译成 C 吗？」

### 结论：能，而且**每一条都是无聊的 C**

| extC 特性 | 生成的 C | 复杂度 |
|---|---|---|
| `[N]T` / 多维数组 | `struct { T data[N]; }` | 无聊 |
| `slice<T>` | `struct { T *data; int64_t len; }` | 无聊 |
| 下标 + 边界检查 | 可证明时 `a.data[2]`，否则 `a.data[extc_checkedIndex(...)]` | 无聊 |
| `option` / `result` | 一个带 `bool` 标签的结构体 | 无聊 |
| `?` | 三句语句（求值一次 + 判断 + return） | 无聊 |
| 泛型 | 按实例单态化（一份 C 结构体 / 函数） | 无聊 |
| `::` 关联函数 | 名字修饰过的普通 C 函数 | 无聊 |
| **arena** | **bump 指针 + mark/release** | 无聊 —— `src/base.c` 里**就是这么写的** |
| **逃逸检查** | **什么都不生成** | **零** |
| `mut ref` / `ref` | `T *`（只读那档可以映射成 `const T *`） | 无聊 |
| 全局（深度 0） | C 的全局对象 | 无聊 |
| `for` | C 的 `for` | 无聊 |
| `match` + 带载荷枚举 | `switch` + 带标签的结构体 | 无聊 |
| 模块 | 名字前缀 / 多个 `.c` | 无聊 |
| `@recursive` | **帧数组 + 循环** | 无聊 |
| trap | `fprintf(stderr, ...); exit(1)` | **已经这样了** |
| 沙箱：编解码 + 协议 | 生成的普通 C 函数 | 无聊 |
| 沙箱：**能力列表** | **链接期**（不链那个符号 ⇒ 调用即**链接错误**） | **零运行时成本** |
| 沙箱：spawn / pipe | `fork` / `exec` / `pipe`（POSIX） | C 库调用 |
| 沙箱：seccomp | 生成的静态 BPF 数组 + `seccomp()` | C 库调用 |
| 线程（设计稿） | `pthread` | C 库调用 |

### ⭐ 为什么后端能一直这么无聊：**P 的第二个作用**

> **P（凡是编译期能证明的，运行时不留痕迹）不只是安全原则 ——
> 它同时保证了「后端能保持无聊」。**

每个特性只可能是两种形态之一：

1. **编译期删掉**（逃逸检查、类型检查、边界证明、可变性检查）⇒ **生成的 C 里一个字都没有**
2. **落成一个普通数据结构**（arena = bump 指针、`option` = 带标签的结构体、`@recursive` = 帧数组）

**越往后做，越多的东西从运行时挪到编译期或者链接期** —— 而 C 恰好擅长表达这两样。

### 四个压力点（诚实说）

| # | 压力点 | 严重程度 |
|---|---|---|
| 1 | **算术语义还没定义**（见下，今天有真 UB） | ⚠️ **真债务** |
| 2 | **输出形态**：沙箱会让输出变成「一组 C 文件 + 链接方式」而不是单文件 | 中 —— 可以用**自 exec**（BusyBox 那招：一个二进制，按 argv 决定当父还是当子）保持单文件 |
| 3 | **`fork` / `seccomp` / `setrlimit` 是 POSIX/Linux，不是 ISO C** ⇒ 沙箱是最不可移植的一块 | 中（主人同时在 WSL 和 Windows） |
| 4 | 以后若要**回调/闭包** ⇒ 函数指针 + context 指针（胖指针） | 低 —— 仍是普通 C，只是 codegen 复杂度上升 |

### ⚠️ 债务清单：extC 现在从 C 那里**继承了三处算术 UB**

（奶昔实测的，不是推的）

| # | 情况 | 现在 | 应该 |
|---|---|---|---|
| 1 | **有符号溢出**（`i32` 最大值 `+ 1`） | 生成 `(x + 1)` —— **C 里是 UB**。`-O1` 下碰巧绕回，`-O2` 的 strict-overflow 假设可以让 `if (x+1 > x)` 被优化掉 | ✅ **已修**：驱动加 `-fwrapv`，把「碰巧」变成「保证」。⚠️ 但**语言语义本身还没定案**（绕回 vs trap） |
| 2 | **除零** | 崩掉（SIGFPE），**没有 extC 位置的消息** | 应该 trap 带位置 ⇒ 生成前加一次比较（除法本来就要几十个周期，一次比较可以接受） |
| 3 | **移位超过位宽**（`a << 40` 对 `i32`） | **静默算错**，只有一个 gcc warning 漏到用户面前 | 要么按位宽取模（定义清楚），要么 trap |

> **这一条比它看起来重要**：extC 的整个卖点是「不给 UB」，
> 而这三处 UB 是**从 C 继承来的**，不是 extC 自己写错的。
> 也就是说 —— **「编译到 C」这个决定，同时意味着「要负责把 C 的 UB 一处一处堵上」**。
> 好消息是只有算术这一小块，而且堵法都很便宜。

---

## 7. 行动清单

### 排序的唯一依据：**会不会推翻已经写好的东西**

| | 类型 | 该早做还是晚做 |
|---|---|---|
| **改语义** | 会让现有代码失效（A/B 会改 `ref` 的语义 ⇒ 80 处 extC + 98 处编译器） | **必须早做** —— 否则别的东西会长在它上面，然后一起重写 |
| **只加不改** | 加法，不使任何现有代码失效 | **随时能做**，晚做也不会被推翻 |

这条规则解释了两件看起来矛盾的事：

- **为什么 A/B 排最前**：它们**改语义**。
- **为什么沙箱可以排很后**：它**只加不改**，而且**天生跟 `ref` 解耦** ——
  因为含 `ref` 的类型**根本跨不过进程边界**（§4.5），所以 `ref` 怎么改都影响不到它。

### 清单

**A 组 · 身份路线**（改语义，必须早做）

 - [x] **A1 · 定引用语义的形状** ✅ 已完成 —— **形状 3（值位置自动解引用）+ `mut ref`**（见 [`REFS.md`](REFS.md) §3）
- [x] **A2 · 实现 A** ✅ 已完成 —— 五步全绿（`mut ref`、自动解引用、写穿、只读强制、视图可写性）；
      全仓库 `ref` 逐处判断**不用人工做**，编译器把 9 处全指了出来
- [x] **B · 逃逸检查** ✅ —— 词法深度 `depth(r) ≥ depth(v)`，参数 = 0、函数体 = 1；
      `fn bad() -> slice<i32> { var a: [8]i32; return a[..] }` **现在是编译错误**（以前静默跑出 0）。
      欠账：把引用传进函数、函数存起来（要传染性分析，见 REFS.md §6）
- [ ] **C · arena + `array<T>`**（**下一个大块**）（靶子 2 的 arena 版本需要；形状取决于 A/B）

**B 组 · 加法**（只加不改，随时穿插，不会被 A 推翻）

- [ ] **输入 + argv**（`readLine(buf)` / `parseInt` / `args`）—— PLAN 漏的洞；
      做完**五子棋就能真的跟人下**
- [ ] **算术 UB 三处**（除零 trap 带位置 / 移位取模；溢出已用 `-fwrapv` 兜住）—— ⛔ 要拍板：绕回 vs trap
- [ ] **全局常量 + 全局变量**（深度 0 的标签，**不需要 arena**）—— 算法题要 `MOD`
- [ ] **`for` 四种形态 + 范围类型**（范围类型还能让可证明的索引检查消失）
- [ ] **带载荷枚举 + `match`**（靶子 2 和自举都要）
- [ ] **模块系统**（自举要）

**C 组 · 奖励关卡**（nb，但不急）

- [ ] **沙箱最小原型**：`sandbox` 组件 + 一个子进程 + stdio 协议 + `result<T,E>` 接口
      - 需要的原语只有三个：`spawn` / `pipe 读写` / `wait`
      - 编解码**复用现成的类型遍历**（跟 `_debug`、`==`、零值是同一台机器）
      - 建议时机：**A/B 做完之后**当奖励；或者在 A 的间隙当调剂
      - 它买到的最大一件东西是 §4.5 说的那条：**trap 从「进程死亡」变成「可处理的失败值」**

**D 组 · 终点**

- [ ] **自举**（一块一块搬，靠 §4.3 那 10 个函数 + §4.1 的对拍）

### 如果今天就想动手，选一个

| 想干什么 | 选 |
|---|---|
| 推进语言的身份 | **A1**（读 `REFS.md` §3，选形状）—— 只要一次对话，不用写代码 |
| 想立刻看到产出、不想做决定 | **输入 + argv**（B 组第一条，最小、最有用、做完五子棋能玩） |
| 想兑现承诺（不给 UB） | **算术 UB 三处** |
| 想尝一口 nb 的 | **沙箱最小原型**（技术上不需要等 A/B，但要接受它比 A 次要） |


---

## 5. 防新环的纪律（这条比顺序更重要）

> **每加一个特性，它必须能用「它之前」的特性写出来 —— 包括测试和示例。**
>
> 如果它需要它自己，那就有环，破法是**把它降级成编译器生成的原语**（规则一）。

现成的检验手段：**降级版靶子**（PLAN 里已经有了）

- 把目标程序**裁成当前语言编得了的样子**，放进 `examples/`
- 每完成一块特性，就把被切掉的那段**加回来一格**
- **推不动的时候，说明缺的东西比想象的早** —— 这就是环出现了

五子棋已经这么用了（`examples/gomoku-board.extc` → `examples/option-result.extc` 里的 `placeLine`）。
自举也可以这么用：先写一个「**只认 `int` 的玩具编译器**」跑通闭环，再一格一格加。

---

## 6. 一个反直觉但很重要的洞见

DESIGN §9 里已经点破了，值得单独拎出来：

> **tokenizer 不需要任何堆分配 —— token 是源代码的 `view`。** 只有符号表才分配。

也就是说：**自举里最热、最大的一块（词法 + 语法 + 类型检查）几乎零堆操作。**
arena 在自举路上的紧迫性**比想象中低得多**。

同理，五子棋也几乎用不到堆（15×15 是编译期常数）。
**G2 和 G3 两个目标都不急着要 arena** —— 这也是为什么它可以安心排在最后。

---

## 7. 一张总图

```
                     今天就能做（不需要堆）
   ┌──────────────────────────────────────────────┐
   │ 全局定长变量/常量 · 输入(readLine/argv) · for │
   │ 引用语义 A · 逃逸检查 B · 带载荷枚举+match     │
   │ 模块系统                                      │
   └──────────────────────────────────────────────┘
                          ↓
   ┌──────────────────────────────────────────────┐
   │ arena 原语（编译器生成的 C）                  │
   │   ↓                                          │
   │ array<T> · 动态字符串 · 全局变长对象          │
   └──────────────────────────────────────────────┘
                          ↓
   ┌──────────────────────────────────────────────┐
   │ 自举（一块一块搬，靠一个「只给标准库用」的口子）│
   └──────────────────────────────────────────────┘
```

**自下而上每一层，只用它上面已经有的东西。**
