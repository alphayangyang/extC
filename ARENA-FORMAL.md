# `ARENA-FORMAL.md` —— arena 规则的**严格推导**

> 主人 2026-09-21：「**甲肯定是不够的，这不是优秀的做法**。一种最保守的做法是让用户显式指定
> arena 位置，但这是失败的，因为**显然这里可以做到形式推导**。也就是说，我的形式推导不够严谨。」
>
> 这份文档就是那笔账：把"存储放哪只 arena"从**语法猜测**（今天的 `callHomeDepth`）
> 变成**约束求解**（§3），并证明：① 它 sound（定理 3）② 它**永远有解**（定理 2 ⇒ 不用注解）
> ③ 它比今天的规则严格更宽（推论 4.2）④ 原丙为什么破、拆开哪一条就成立了（§4.3）
>
> 记号沿用 `ARENA.md`：**深度越小活得越久**；`R ⊒ S` 读作"**R 活得 ≥ S**"
> （`R.depth ≤ S.depth`）。真正的序是**区域树**，深度只是它的一个线性化（§5.1）✓

---

## 0. 问题陈述

**要判定的问题**：一个函数体里的 `new`（= 分配点 `S`），它的对象该放进哪只 arena？

今天（`callHomeDepth`，`ARENA.md` §1.2）的答案是**语法**的：

> `home(f)` = 最浅的那个 `mut ref` **实参本身**所在的那只 arena

本文要证的是：这条规则**只在附加前提下 sound**（引理 1），
而那条前提**不是必须的** —— 真正的约束是关于「**被存进容器里的那些值**」的，
跟"实参自己住哪"是两件事 ✗。分开之后（§2.3），"造完返回"**可以证安全**（推论 4.4）✓

---

## 1. 模型

### 1.1 语法片段（够用即可）

```
e ::= x | n | ref x | new T[n] | e.f | e[i] | f(e⃗) | { e⃗ } | e as T
s ::= x := e | x.f := e | x[i] := e | f(e⃗) | return e | s; s | if e s s | while e s
```

### 1.2 区域、区域树、寿命

- **区域（region）** = 一个 arena 实例。程序运行中的区域集合 `Reg` 带一棵**树** `⊑_tree`
  （词法块的嵌套），根 = `ρ_top`（程序级，最后才回收）。
- **寿命**：区域在进入其块时**诞生**、离开时**回收**（`extc_arena_release`）。
  于是 `R ⊒ S`（R 活得 ≥ S）当且仅当 `S ⊑_tree R`（R 是 S 的**祖先**）✓
- 实现里的**深度** `d(R) ∈ ℕ` 是 `⊑_tree` 的一个**保序线性化**：
  `R ⊒ S ⟹ d(R) ≤ d(S)`。反过来**不成立**（兄弟块同深度但**互不包含**）⇒ §5.1 的近似来源 ✓
- **函数的形式区域参数**：`home` 是一个**区域形参** `H`（A3 的 `__extc_home`）——
  它在调用点被实例化成调用者作用域里的某只区域 ✓

### 1.3 语义与安全（要证的性质）

配置 `⟨σ, λ⟩`：`σ : Addr ⇀ Value`（堆/栈上所有对象），`λ : Reg → {live, dead}`。

- `Addr` 带区域：`σ(a) = (R, v)` 记 `reg(a) = R`。
- **指针值** `Ptr(a)`；`deref(a)` 要求 `λ(reg(a)) = live`，否则**悬垂** ✗
- 分配 `newρ v`：在区域 ρ 里取新地址（我们**唯一要推导的东西**就是每个分配点的 ρ）。
- **安全（safety）**：一条 trace 里**每次 `deref` 都命中活区域**。程序 sound ⟺ 所有 trace 安全 ✓

### 1.4 关键：区域是**一个**（不是"一块内存"）

`reg(a)` 是**地址的属性**，一旦分配就定了。这让"搬容器"这件事变得可分析：

> 容器 `v` 的**槽位**在区域 `R_v` 里，但它**里面的指针**指向区域 `ρ` 的对象。
> **拷贝 `v`**（返回、赋值、塞进别的 struct）只搬**槽位**（指针值），**不搬 `ρ` 的对象** ✗
> ⇒ 只要 `v` 或它的副本活到某个时刻，`ρ` 就必须**也**活着 ✓

**这就是 #31 的全部内容**：`mk()` 里 `v` 的槽位和 buffer 都在 `mk` 的块区域里
⇒ 拷贝出到调用者之后，副本活着、buffer 已经回收 ✗

---

## 2. 先证"今天那条规则"的边界（为什么甲′ 只能是补丁）

### 2.1 两种流：**地址流** vs **内容流**

看被调者往"家内存"（分配点 S 的区域 H）里存东西，值的来源只有两种：

| 名称 | 形状 | 值的区域 `ρ(val)` 是 | 例子 |
|---|---|---|---|
| **地址流 (A)** | 存一个**实参本身**（或其字段）的地址 | `R_slot(实参)` —— 实参**槽位**所在区域 | `n.owner = ref l`、`n.owner = ref l.head` |
| **内容流 (B)** | 存一个**从实参里读出来的指针**（或其派生） | `δ(实参)` —— 实参**所指对象图**所在区域 | `n.next = l.head` ← **链表就是这么写的** |
| （不计）| 存**新分配的**、`null`、不可变标量 | H 自己 | `n.next = null`、`n.value = v` ✓ 恒成立 |

### 2.2 引理 1（存储安全的两条前提）

**引理 1**：分配点 S 用区域 `H` 是 sound 的 ⟺ 对**每一个**可能被存进 S 的对象图的指针值 `p`，
若 `reg(p) = ρ_p`，则 `H ⊒ ρ_p`（H 活得 ≥ 被存对象的区域）✓

*证明（⇒ 方向的关键一步）*：设 `p` 被存进区域 H 里的槽位。若 `H ⋢ ρ_p`，
则存在 trace：`ρ_p` 先于 H 回收（或二者不可比）⇒ 该槽位仍可读（H 活着），
读出的 `p` 指向 dead 区域 ⇒ deref 悬垂 ✗ 与 safety 矛盾 ✓
（⇐ 方向：区域内对象与区域同生共死，且 `H ⊒ ρ_p` ⇒ `λ(H)=live ⟹ λ(ρ_p)=live` ✓）
> ⚠️ "⟹" 这一侧是**对这个纪律**（区域级、路径不敏感）成立：更精细的分析可以靠
> "指针被覆盖前对象已不可达"来放宽它，但那超出"区域纪律"能表达的范畴 ⇒ 本文不做 ✓

⇒ **推论 1a**：`H` 的下界由「**实际会流过 S 的那些值的来源**」决定，
而**不是**由"实参自己住哪"决定 ✓

### 2.3 今天的规则 ④ 是把两种流**合并**后的特例

实现的 `checkCallRefArgs` 对**每一个** `ref`/`mut ref` 实参 `i` 要求：

```
R_slot(arg_i) ⊒ H        (placeDepth(arg_i) ≤ h)
```

对照引理 1：这正是**只考虑地址流 (A)** 时的要求，而且**无条件地施加于所有实参** ——
不管被调者到底存的是 `&arg`（地址流）还是 `l.head`（内容流）✗

- 对 `fn bind(s: mut ref slot, target: mut ref i32) { s.r = target }` ⇒ **正确**（真的存地址）✓
- 对 `fn push(l: mut ref list, v: i64) { n.next = l.head  l.head = n }` ⇒ **过强**：
  `push` 从没存 `&l`；它存的是 `l.head`（内容流，区域 = 节点图所在区域）✗

⇒ **推论 1b**：今天的规则是"**反正不知道，就按最坏情况（地址流）要求**"——
这是**保守**，不是**不严谨**；真正不严谨的是**下一步**：

### 2.4 为什么"合并流"必然导致 #31，而甲′ 只是把后果记下来

`push` 的家 `H` 被**语法**选成 `R_slot(l)`（实参槽位区域）。但 §2.2 说 H 的下界应该由
**内容流的来源**决定：`l.head` 指向的节点是**早先 push 自己分配在 H 里的** ⇒ `ρ = H` ⇒
约束 `H ⊒ H` **恒成立** ✓ ⇒ 从内容流看，**H 选哪儿都行**！

⇒ H 其实**完全自由**，可以按**调用者的逃逸需求**去选（原丙的直觉是对的 ✓）
⇒ 但**地址流**这一条（`R_slot(l) ⊒ H`）把它钉死：`H` 不能比调用者的那个局部槽位活得更久 ✗
⇒ **两条约束打架**：逃逸要求 `H ⊒ 调用者的家`，地址流要求 `H ⊑ 调用者的帧`（帧 < 家）
⇒ 甲′ 的处置是：**承认打不赢，把结果写回容器深度 ⇒ 让逃逸不可能** ✗（这就是"不够优秀"的地方）

**但地址流那条约束只在"被调者真的存了 `&l`"时才需要** ✓（引理 1：约束是**按实际流**的）
⇒ 只要**把两种流分开、各自只对真正发生的流施加约束**，冲突就消失了 ✓（§3、§4.3）

---

## 3. 正确的东西：一个**约束系统**

### 3.1 变量

| 变量 | 含义 |
|---|---|
| `α_S` | **分配点 S 的区域**（我们要求的解）|
| `ρ(e)` | 表达式/地方 `e` 求值出的**指针值的区域**（上界，取"最坏的那只"）|
| `R(p)` | 地方 `p` 的**槽位区域** |
| `δ(v)` | 容器 `v` 的**对象图区域**（它里面的指针**允许**指向哪只区域；数据结构的"home"）|
| `H_f` | 函数 `f` 的**区域形参**（A3 的 `__extc_home`），调用点实例化 ✓ |

### 3.2 约束生成（每条都直接来自引理 1 或"拷贝只搬指针"）

```
(C1 存储)      p.f := e        ⟹  ρ(e) ⊒ R(p.f)
(C2 容器不变式) x : T 含引用    ⟹  δ(x) ⊒ R(x)            // 图里的对象要活过槽位
(C3 拷贝/返回)  x := y / return y ⟹ δ(y) ⊒ R(x) / δ(y) ⊒ R(dst)   // dst = 返回值落点
(C4 分配)      S = new …        ⟹  α_S ⊒ δ(所有 S 的对象可能流入的容器)
(C5 调用实例化)  g(a⃗)            ⟹  用 g 的**效果摘要**实例化 H_g
```

**函数效果摘要在 §3.4 定义**，它是这套东西的心脏：它回答"**g 往它的 `mut ref` 实参里
存了哪些区域的东西**"，而不是"实参住哪" ✓

### 3.3 定理 2（解的存在唯一 ⇒ **不需要用户注解**）

> 约束都是 `X ⊒ ⨆ Y_i` 的形状（单调、join 只出现在右侧）⇒ 在**有限**区域格上，
> 由 Knaster–Tarski，**最小解存在且唯一**，可从 `X := ⊥`（= 最深、最短命的区域）出发
> 迭到不动点算出 ✓
> 而且**格有顶**（`ρ_top` = 程序级区域）⇒ **解一定存在** ⇒
> **任何程序都不需要"用户显式指定 arena"** ✓✓（主人这句判断是对的）

**推论 3.1**：最小解 = **满足所有约束的最短命选择** ⇒ 循环里的临时容器仍然落在**块**区域
（PLAN #24 的紧致性**自动**保住 ✓，不用专门打补丁），而真正会逃出去的容器落在**家**区域 ✓

### 3.4 函数效果摘要（三个集合，按实参编号 `i`）

对函数 `f`，静态算（在它自己的区域形参 `H_f` 与参数槽位区域 `R_i` 抽象下）：

```
Fresh(f, i)   : f 分配在 H_f 并最终存进 i 所指容器的东西            → 约束: H_f ⊒ H_f  (恒真)
Addr(f, i)    : f 把「实参 j 的地址/字段地址」存进 i 所指容器       → 约束: R_j ⊒ H_f
Cont(f, i)    : f 把「从实参 j 读出/取到的指针」存进 i 所指容器       → 约束: ρ_j ⊒ H_f（若读的是对象图内部 ⇒ δ_j）
```

⇒ **`Addr` 只在真有地址流时非空** ⇒ `push` 的 `Addr(push, 0) = ∅`
⇒ 那条钉死 H 的约束**消失了** ✓（这正是今天规则 ④ 过强的地方）

### 3.5 定理 3（soundness）

> 若程序对某个解 `α` 满足 §3.2 的全部约束，且**实现按 `α` 分配**，则所有 trace 安全 ✓

*证明（归纳，不变量 I）*：
**I**：任意时刻，对内存里任意活着的槽位 `p`（区域 `R(p)`）与它持有的指针→对象 `o`（区域 `ρ_o`）：
`ρ_o ⊒ R(p)` 且 `λ(R(p)) = live ⟹ λ(ρ_o) = live`。

- *分配*：新对象在 `α_S ⊒ δ(…)` ⇒ C1/C2 保证它被存进任何容器时满足 I ✓
- *存储* `p.f := e`：由 C1 得 `ρ(e) ⊒ R(p.f)` ✓ 新边满足 I；其余边由归纳假设 ✓
- *拷贝* `x := y`：只搬指针值 ⇒ `x` 的槽位区域是 `R(x)`，指针区域不变 ⇒ 需 `ρ ⊒ R(x)`，
  由 C2/C3（`δ(y) ⊒ R(x)`）+ I 得 `ρ ⊒ δ(y) ⊒ R(x)` ✓
- *返回 / 跨帧传递*：同 C3（返回值落点 `dst`），由**调用点实例化** `H_g` 保证 ✓
- *回收*：区域按块 LIFO 回收；由 `⊒` 的传递性，`λ(R)=live ⟹ λ(ρ)=live` ✓
- *deref*：命中的地址区域 ρ 与某活槽位同链 ⇒ 由 I 得 ρ live ⇒ 安全 ✓ ∎

---

## 4. 推论（这就是主人要的"严谨推导"的产出）

### 4.1 推论：不需要注解，但**需要摘要**

- **不需要**注解：定理 2 说解总存在 ✓（"用户显式指定 arena"确实不是答案 ✓）
- **需要**摘要：`Addr`/`Cont` 是**跨过程**信息 ⇒ 要么做整程序不动点，
  要么让每个函数带一份**区域抽象的效果摘要**（今天已经有的 `needsHome` / `funcAllocates` /
  "推迟到实例化复查"就是这套机制的雏形 ✓）

### 4.2 推论：今天的规则 = "只算 `Addr` 且当成非空" ⇒ 严格更保守

- `push` 的真实 `Addr = ∅` ⇒ 今天却按非空处理 ⇒ **假阳性** ✓
  这就是 PLAN **#34**（"容量够的容器其视图也不能返回"）的**根因**：
  它根本不是"保守得没法改"，而是**规则用错了东西** ✓

### 4.3 推论：**原丙为什么破**，以及拆开哪一条就成立（"丙″"）

原丙把 `H` 一律换成"调用者的家"（比调用者帧更长寿）。此时：
`Addr ≠ ∅` 的那种 callee（`n.owner = ref l`）的约束 `R_slot(l) ⊒ H` **被违反** ⇒
按 §3.5 不变量 I，`&l` 被存进比 `l` 更长寿的区域 ⇒ **悬垂** ✗ 这正是我当时撤回它的理由 ✓

**拆开流之后**：
- `Addr = ∅` 的 callee（数据结构那类：`push`、`varArray` 的方法…）⇒ `H` **自由** ⇒
  按 C3/C4 取「**调用者的逃逸需求**」⇒ **丙″ sound** ✓✓
- `Addr ≠ ∅` 的 callee ⇒ 约束 `R_j ⊒ H` 仍在 ⇒ 调用点要么把 `H` 选在**调用者的帧**里
  （安全，但那只容器就不能逃出去 ✓ 可选），要么**报错**（想同时要两样 ⇒ 无解 ⇒ 拒绝）✓

### 4.4 推论：**"造完返回容器"现在可以证安全**（甲′ 拒绝它，丙″ 接受它）

```extc
fn mk() -> varArray<i32> {
    var v: varArray<i32> = varArray<i32>::withCap(0)
    v.push(42)
    return v
}
```
**约束集**（`mk` 的形式区域参数 = `H_mk`，`v` 的槽位区域 = `R_v` = `mk` 的函数体块）：

| 约束 | 来源 | 结论 |
|---|---|---|
| `Addr(push, 0) = ∅` | §3.4：`push` 只存 `l.head`（内容流）与 `new`（Fresh）| 没有把 `H_push` 钉在 `R_v` 上的约束 ✓ |
| `Cont(push, 0) = {δ(l)}` | `n.next = l.head` | 需 `δ(l) ⊒ H_push` |
| `δ(v) = H_push`（归纳）| C4：`push` 分配进 `H_push`，而 `l` 就是 `v` | 代入得 `H_push ⊒ H_push` 恒真 ✓ ⇒ **`H_push` 自由** |
| `H_push ⊒ R(caller 的落点)` | C3 + 调用 `mk()` 的返回值落点 | 调用点把 `H_push` 与 `H_mk` 一起实例化成**调用者的家** ✓（家 ⊒ 落点 ✓）|

⇒ **解存在**（取 `H_push = H_mk = 调用者的家`）⇒ 由定理 3 **安全** ✓
⇒ 甲′ 拒绝它，**不是因为它不安全**，而是因为今天的规则把 `Addr` 当成非空 ✗

**对照 `sneaky`**（真的 unsafe）：
```extc
fn sneaky(l: mut ref list) { var n: mut ref node = new node  n.owner = ref l }
```
`Addr(sneaky, 0) = {R_slot(l)}` ⇒ 约束 `R_slot(l) ⊒ H_sneaky` ⇒ 若调用点想让
结果逃逸（`H = 调用者的家 > 调用者帧 = R_slot(l)`）⇒ **无解 ⇒ 编译错误** ✓ 而
不逃逸时（`H = 调用者的帧`）⇒ **有解 ⇒ 放行** ✓ —— 这就是"**该拦的拦、能放的放**" ✓✓

### 4.5 推论：报错的位置也变了（更早、更准）

今天 #31 的报错落在**调用者**（`return v`）且文案靠"指路"；丙″ 下
`Addr/Cont` 的违反点在**调用点**（"这一刀的 callee 会把 `&实参` 存进比它更长寿的区域"）✓

---

## 5. 落地清单与已知近似（诚实）

### 5.1 实现里会保留的近似（"我们故意没做精确"的四处，逐条写清代价）

| # | 近似 | 理想是什么 | 我们的做法 | **代价**（诚实分类）|
|---|---|---|---|---|
| **1** | **深度线性化**：寿命是**树**（祖先/后代/兄弟），实现压成**一个数** `depth` | 用**偏序**判：`R ⊒ S` 当且仅当 `S` 在 `R` 的子树里 | 只比大小 `d ≤ h` ⇒"祖先"和"兄弟"**分不出来** | ① 假阳性：兄弟块被判"谁更深"（顺序无关时也不精确）② ⚠️ **未证**：`d(x) ≤ d(y)` **不蕴含** `x ⊒ y`（兄弟块深度相等但时间上不相交）⇒ 抽象本身要单独论证。我试过几个形状（把 `ref 局部` 存进外层 struct、经出参转手、跨分支）**都被现有规则挡住了、没构造出反例**，但**也没证过** ⇒ 诚实记为"欠一个证明" ✓ |
| **2** | **路径不敏感**：收窄/深度只认**绑定名**，不认 `p.next.field` 这种路径 | 路径敏感的证明（`if p.next != null` 就证明那个**路径**非空）| 只跟踪标识符；深度对 `EX_FIELD/EX_INDEX` 用**根绑定**的槽位深度 | **假阳性**（用户要写一行 `var n = p.next` 才能收窄；根的槽位深度往往比里面东西的实际寿命**更短命** ⇒ 误拒）✓ PLAN #14 就记着这条 |
| **3** | **值不敏感**：只看类型/区域，**不看值**（`cap` 到底够不够）| 值/计数分析：知道"这一路上 `len` 从没超过 `cap` ⇒ 不会分配" | 只要 callee **可能会**分配（`push → grow → new`）就按"会"算 | ⚠️ **跟 #34 的关系要说清**：**丙″ 之后它不再影响合法性**（`push` 的 `Addr = ∅` ⇒ `H` 自由 ⇒ 调用者按逃逸需求选，真会扩容就扩在更长寿的区域里 ✓ 合法且安全），残留的代价是**内存**：**可能逃逸就按逃逸算** ⇒ 用了比实际需要更长寿的区域 ⇒ 峰值偏高 ✓（不是误拒，是费内存）|
| **4** | **无 free**：区域只能**整块 LIFO 回收**，没有逐个释放 | 精确的活跃期（引用计数 / GC / 手动 free）⇒ 对象一死就能还 | 区域在**块退出**时整体回收；`clear()` 不还内存、`shrink_to_fit` 不存在（定案 56/§7.9）| **内存换精度**：最小解给的是"**最深的祖先**"（能活到块尾的最短命选择），而不是"精确到语句的活跃期" ⇒ ① 循环里创建又丢弃的东西要等到块尾才还 ② `varArray` 扩容的旧 buffer 一直占着（定案 59 因此改用"**累计分配**"当指标）③ 区域内无法复用碎片（#29 chunked / #30 原地扩就是在绕它）✓ |


### 5.2 实现步骤（按"先小而安全"排）

1. **算摘要**：给每个 `FuncDef` 加 `Addr`/`Cont` 两个集合（按参数编号），
   在检查函数体时收集（`p.f := e` 且 `p` 通向某个 `mut ref` 参数 ⇒ 按 `e` 的来源分类：
   `&参数` ⇒ Addr、`参数读出的指针` ⇒ Cont、`new`/字面量 ⇒ Fresh）✓
2. **调用点求解**：`H = ⨆(调用者侧落点, Cont 已知部分)`；`Addr` 非空 ⇒ 施加
   `R_slot(arg) ⊒ H`（今天那条规则④**只在这时**用）✓
3. **删掉甲′ 的 `raiseMutRefTargets`** ⇒ #34 的假阳性随之消失 ✓
4. **跨函数不动点**：`Cont` 依赖被调者的总结（如 `varArray::push` 的 `δ`）⇒ 按名字做
   **惰性**闭包（照抄 #33 的 `funcAllocates`：memo + 环保护 + 拿不准保守）✓
5. **回归**：`tests/errors/canary_return_{grown_varArray,built_list,asSlice_after_grow}` 要
   **变成正例**（它们其实安全 ✓），而 `sneaky` 系（`ref_launder_field` 那批攻击）**必须继续被挡** ✓

> ⚠️ 第 5 条是这次推导最硬的验收判据：**该放的要放（3 条 canary 转正例）、
> 该挡的要挡（攻击库基线不动）** ⇒ 两边同时满足才算"推导落地"✓

---

## 6. 补 §5.1 的第 1 条：**深度线性化到底 sound 吗？**（2026-09-21，主人点名要证）

主人的判断：「**2 3 4 都不会导致我的代码失去表达力，但是 1 这个真得去证明（证真或证伪）一下**」✓
⇒ 下面给证明 + 反例猎捕实验，结论是：**① 本身不是洞，洞在别处（§6.4）** ✓

### 6.1 要证的命题

程序点 π 处的一次检查「把值 `v` 存进地方 `p`」被接受 ⟺ `d(v) ≤ d(p)`。
要求它蕴含**运行时**：`reg(v) ⊒ reg(p)`（`v` 指向的对象活到 `p` 的槽位死之后）。

### 6.2 ⭐ 关键引理 L1：**名字只出现在祖先链上**（这条是线性化 sound 的全部理由）

> **L1**：若在程序点 π 能写出一个**命名地方**（标识符路径，如 `h.p`、`a[i]`），
> 那么它的**根绑定**所在的块一定是 π 当前块的**祖先或自身** ✓

*证明*：名字解析（`lookup`）只沿**当前作用域链**向上找（这是检查器唯一的解析路径，
`check_lookup.c`）⇒ 命名的绑定必在祖先链上 ✓ 而块深度沿作用域链**严格递增**
（参数 0、函数体 1、每进一层块 +1）⇒ **在同一条链上，深度比较 `d ≤ d` 就是真实的寿命序** ✓ ∎

**推论 6.1**：两个命名地方永远在**同一条祖先链**上
⇒「兄弟块不可比」这件事**根本不会出现在"命名地方 vs 命名地方"的比较里** ✓
⇒ 不可比只可能发生在**两条不同链**之间，而那意味着比较里至少有一侧**不是**命名地方（见 §6.3）✓

### 6.3 另一侧的三条来源，逐个交代

检查里参与比较的深度只有三种来源：

| 来源 | 例子 | 为什么正确 |
|---|---|---|
| **(S1) 命名地方** | `h.p = …`、`a[i] = …` | **L1**：在同一条链上，深度比较 = 真实寿命序 ✓（**精确，不是近似**）|
| **(S2) 参数 / 家**（深度 0）| `out.p = …`、`new` 出来的东西 | 调用期间调用者帧一定活着 ✓；"家"由**调用点按规则 ④** 实例化 ⇒ 深度 0 的含义是"活过这次调用" ✓ |
| **(S3) 记账的深度** | `?ref node` 游标、`slice<T>`、容器里的图 | 只要保证 **"记的数是真实深度的上界"**（记大 = 更保守 = 只会误拒 ✓）|

### 6.4 ⭐ 定理 6.2（线性化 sound）与**真正的洞在哪**

> **定理 6.2**：若 **L1** 成立、且 **(S3) 的上界性**成立，则深度比较 `d(v) ≤ d(p)` 蕴含
> `reg(v) ⊒ reg(p)` ⇒ 存储安全 ✓

*证明*：分情形。
- `v`、`p` 都来自 (S1) ⇒ 由 L1 同链、比较即寿命序 ✓
- `p` 来自 (S2)（深度 0）⇒ 需 `reg(v) ⊒ 调用者帧`：要么 `v` 也来自 (S2)（✓ 直接成立），
  要么 `v` 的记数必须是 0 —— 由 (S3) 上界性 + 逃逸检查/规则 ④ 给出 ✓
- 任一侧来自 (S3) ⇒ 由 (S3) 上界性 + 归纳假设 ✓ ∎

**⇒ 结论（出乎我意料，但推导很硬）：**
**深度线性化在"同一个根链"上是精确的，不会把"不可比当成可比"而放行** ✓
真正能破 soundness 的是 **(S3)：记的深度比真实情况小（欠估）** ✗
**#31 正是这一条**：`v.push(…)` 把更深的存储塞进容器，而 `v` 的记数**停在 0** ⇒ 逃逸检查看不见 ✗
⇒ 这也解释了为什么 §5.1 里我列"假阳性"时**构造不出不可比的反例**：
不可比的两个东西**根本不会出现在同一次比较里**（除了记账出错）✓

### 6.5 反例猎捕（实验，2026-09-21，`/tmp/hunt/`）

| 形状 | 意图 | 结果 |
|---|---|---|
| h1 兄弟块直接偷渡（`p = ref x` 存在外层）| 不可比放行？| **拒绝** ✓ |
| h2 经 struct 字段偷渡（`h = { p: ref x }`）| 同上 | **拒绝** ✓ |
| h3 经出参偷渡（`stash(ref h, ref x)`，x 在兄弟块）| 绕过规则 ④？| **拒绝** ✓（*cannot store a borrowed value…*）|
| h4 块 A 里 `push` 造链表、块 B 里用 | 兄弟块存活期 | **编过且正确**（`求和 = 3`、ASan 无 UAF ✓）—— 因为节点分配在**实参 `l` 所在区域**（函数体），**不是**块 A ✓ |
| h5 两个 `if` 分支（兄弟块）里各存一次 | 分支不相交 | **拒绝** ✓ |
| h6 块 A 里 `new`、指针带出 | 逃逸提升 | **编过且正确**（ASan 无 UAF ✓）—— `new` 在有家函数里进的是家 ✓ |

⇒ **没有找到反例** ✓ 与定理 6.2 一致（两种"编过"的形状都是因为分配**没有**落在兄弟块里）✓

### 6.6 ⭐ 留给将来的**记账不变式**（写进规矩，防的就是 #26/#31 那两次）

> **(S3) 不变式**：任何**能让某个值的内容发生变化**的规则，都必须把"写进去的东西的深度"
> **取上界记回那个值**（`max`，不是覆盖）✓
>
> 新加语言特性时逐个问一句：**这一步会不会让谁的记数变小？** 会 ⇒ 那就是 #31 ✓
> 已覆盖：赋值、元素/字段写、方法调用（甲′）、出参（甲′）；
> ⚠️ 将来要覆盖：`for` 循环变量、闭包捕获、`match` 载荷绑定、模块级可变状态、
> 任何"把容器交给别人改"的新语法 ✓

⇒ 也就是说：**① 不需要证伪，它是对的**；要守住的是 **(S3)** —— 而 §3 的约束系统（丙″）正是
把 (S3) 从"靠人记得"升级成"由效果摘要 `Addr`/`Cont` 自动算"✓

---

## 7. 误拒（假阳性）在这个粒度下**无解吗**？（2026-09-21 主人追问）

**结论：不是无解** ✓ 但要分清两件事：**两个旋钮**、**一处硬门槛**、和**一个真正的极限**。

### 7.1 判据：误拒 ⟺ 判断时用的**事实**比需要粗

能拧的旋钮只有两个：

| 旋钮 | 做法 | 代价 |
|---|---|---|
| **(A) 事实更细** | 路径 / 字段 / 别名 / 值 / 控制流敏感 | 分析复杂度（有的还要别名保证）|
| **(B) 分配区域更长寿** | **不证明它安全，直接让它安全** | 内存 |

**(B) 是被严重低估的那一半**：今天几乎所有的误拒，根子都在"分配区域是**语法猜**的"（`callHomeDepth`），
而不是"证明不出来" ✗

### 7.2 ⭐ 命题 7.1（单调放大是安全的）⇒ 误拒从来不是 soundness 逼出来的

> **命题 7.1**：把某次分配的区域换成任意**更长命**的区域，程序仍然安全（只是可能多占内存）✓
>
> *证明*：安全条件是 `H ⊒ 每一个会持有该对象的地方`（引理 1）；把 `H` 放大（`H' ⊒ H`）后，
> 该条件只会更容易满足；而"值所指区域 ⊒ 槽位区域"这类检查，在所指对象**更长寿**时同样更容易满足 ✓
> 区域回收只会**更晚** ⇒ 不会凭空产生悬垂 ✓ ∎

> **推论 7.2**：任何被拒的安全程序，都存在一个**重写**（把相关分配放到更长寿的区域）
> 使它**既被接受又安全** ✓（极端：全放程序级区域）

⇒ 所以：**误拒与费内存是同一枚硬币的两面** ✓
⇒ #34、"规则 ④ 过强"、"造完返回被拒"，本质是**同一条**：区域选得比需要短 ✗

### 7.3 当前已知的误拒，一条条归因

| 误拒 | 靠 (B) 能消吗 | 需要 (A) 的什么 |
|---|---|---|
| **#34**（容量够的容器其视图也返回不了）| **能**：家按逃逸需求选 ⇒ 真扩容就扩在长寿区 ✓ | — |
| **规则 ④ 过强**（`push` 那类）| **能**（`Addr = ∅` ⇒ `H` 自由）| 两流摘要 `Addr`/`Cont`（很便宜）|
| PLAN **#8 剩下**：字段里的参数引用按**槽位**深度算 | 能，但**费内存**（把外层也顶到长寿区）| 字段级/槽位级深度（便宜）|
| PLAN **#14** 路径收窄 | **不能**（跟区域无关）| 路径事实 + **别名稳定性**（"只读链"正好能给 ✓）|
| "循环最多 3 次 ⇒ 其实不会扩容" | **能**（直接放长寿区）| 只有想**省内存**时才需要值分析 |

### 7.4 ⚠️ 硬门槛：**没有别名信息 ⇒ 只能用弱更新**（这一小块是真无解，除非加别名分析）

**命题 7.3（不可区分）**：下面两个程序在**今天的粒度**里**同态**（同样的区域、同样的深度事实、
同一条控制流路径），但一个安全、一个不安全 ⇒ 任何 sound 的分析只能**同时拒** ⇒ 安全那个是**必然的误拒** ✗

```extc
struct holder { p: ?ref i32 }

fn safe() -> holder {              // 运行时安全：逃出去之前 h.p 被置空了 ✓
    var x: i32 = 5
    var h: holder = { p: ref x }
    h.p = null                     // ← 「强更新」（覆盖）本来能救它
    return h
}

fn unsafe() -> holder {            // 形状一模一样，区别只在 h 的地址被人拿走过 ✓
    var x: i32 = 5
    var h: holder = { p: ref x }
    var q: mut ref holder = ref h
    q.p = null                     // 写的是 *q；分析不知道 *q 是不是 h ⇒ 只能**弱更新**
    return h                       // 悬垂 ✗
}
```
两者的差别只是「**这个绑定有没有取过地址**」—— 而今天的检查器**不跟踪这一位** ✗
⇒ 硬门槛**不是"区域粒度"**，而是**别名信息**；它的**最小可用版本就是"取地址跟踪"**
（每个绑定一个 bool：取过地址 ⇒ 禁止强更新 ✓ 很便宜）

### 7.5 真正的极限（诚实版）

| 想要 | 要付的代价 |
|---|---|
| **不误拒 + sound** | **内存**（(B)：分配放长寿区；极端 = 全放程序级区域，退出才回收）|
| **不误拒 + 省内存** | **运行时信息**（把决定推迟到运行时：chunked #29 / 原地扩 #30 / 容器记住出生 arena + 运行时判断）|

⇒ **在这个静态粒度内，"误拒"和"费内存"可以互换，但不能同时省** ✓
⇒ 好消息：**表达力不丢**（总有重写），丢的只是内存 —— 这与主人对 2/3/4 的判断一致 ✓
⇒ 所以正确的问法不是"误拒能不能消掉"，而是"**哪些误拒值得用内存去换、哪些值得用 (A) 的分析去换**" ✓
