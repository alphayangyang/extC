# extC —— 设计与亮点总览

> 写这份的时间：2026-09-21（WSL 侧，`127.0.0.1:3090` 这台）
> 仓库：`/home/alphayang/extC_Compiler`（Windows 侧对应 WSL 里的 `~/extC_Compiler`）
> 想要"一句话"：**extC = Rust 的安全 + 不写生命周期 + Go 那么少的语法量**，编译到 C。

---

## 0. 动机（原话）

> 「我就是觉得 C、C++ 都太垃圾了（C 太原始、C++ 太混乱），Go 我觉得很好但是不够安全，Rust 太陡峭了」

现实目标：**给 C 语言课程设计写一个五子棋 AI，而且是安全地写**。
⇒ 于是有了 extC：一个**C 的超集式替代品**（自己写、自己用），编译产物是**无聊的 C**（无宏魔法、无优化花招），
`#line` 把 gcc 的报错映射回 `.extc` 源码行 ✓

---

## 1. 三条原则（全部设计都由它们推出来）

| 原则 | 含义 | 落地 |
|---|---|---|
| **P** | **能证明的，运行时不留痕** | 收窄后的 `?ref T` 解引用**零检查**；能证明的转换不插 trap；切片边界能静态证明就不查 ✓ |
| **P′** | **不能证明的，必须写在语法里、并且响亮报错** | 收窄要写 `if p != null`；有损转换要写 `T(x)`；逃逸危险在**调用点/返回点**报错并**指路** ✓ |
| **P″** | **没有手动分配，就没有手动释放** | 语言层**没有堆**：只有 栈 + arena + 静态 ⇒ 不用 `malloc/free`、不用生命周期标注 ✓ |

---

## 2. 语言长什么样（语法量刻意压到最小）

```extc
struct node { value: i64  next: ?ref node }
type color = | red | green | blue            // 枚举；可带载荷：| none | some(T)
type maybe<T> = | nothing | just(T)          // 泛型（单态化）

fn sum(head: ?ref node) -> i64 {             // 引用类型写在签名里，权限一眼看见
    var t: i64 = 0
    var cur: ?ref node = head
    while cur != null {                      // ← 收窄：证明过就零运行时检查
        t = t + cur.value
        cur = cur.next
    }
    return t
}
```

要点：
- **类型**：`i8..u64`、`f32/f64`、`bool`、`struct`（可带方法）、`type` 枚举、泛型、`[N]T`、`slice<T>` 视图、
  `ref`/`mut ref`/`?ref T` 可空引用、`option<T>`/`result<T,E>`（prelude 里就是普通枚举）✓
- **视图**：`slice<T>`（只读）/ `mut slice<T>` —— **只有一个视图类型，`mut` 是限定词**；
  可写性从源头继承，「链式只读」是承重墙（不是讲究）✓
- **`?` 家族**（读值只有两条路，**故意不给** `unwrap/valueOr/has/ok`）：
  `?T` 类型糖 · `e?` 传播 · `e ?? 兜底`（C 三元，只算一侧）· `e!` **签字**（错了就是 UB）✓
- **显式转换**：`T(x)`（不是 C 的 `(T)x`，因为 parser 不看符号表）；整数收窄/换符号/浮点转整数**
  装不下就 trap 并带源码位置**；能证明的零检查 ✓
- **运算符协议**：`fn ==`；数组的 `==` 由编译器派生（元素支持才派生）；带载荷枚举**没有** `==` ✓
- **换行敏感**：`;` 只在同一行内需要（少打字符，但语法依然显式）✓

---

## 3. 内存模型：**没有堆**（这是 extC 与所有对手最不一样的地方）

### 3.1 区域 = 作用域（不是数据类型）

- **arena 按块细化**：`extc_arena __extc_a[块嵌套深度]`，进入/离开块时整块回收；
  深度**编译期算好**，栈上定长，零分配 ✓
- `new T[n]` / `new T`：从**当前块**的 arena 拿，**永远清零**（所以"新对象的 `next` 自动是 `null`"）✓
- 循环里 `new` **不会涨内存**（出块即回收）—— 压测（150 MB 上限）盯着这一条 ✓
- `break`/`continue` 回收循环体那一层；`return`/`?` 回收全部层 ✓

### 3.2 逃逸提升（A3）—— 跨函数怎么活

> 一个"会分配 + 返回带引用的东西（或往形参里写）"的函数，多收一个**隐藏参数** `__extc_home`
> （"家" arena）。调用点按**逃逸上下文**决定传哪只：不逃 ⇒ 当前块（回收得早、省内存）；
> 逃 ⇒ 调用者的家 ✓

### 3.3 规则 ④（写进形参的东西必须活得够久）

被调者可以把它分配的东西写进 `mut ref` 实参指的地方 ⇒ **实参指的对象必须活得 ≥ 这一刀的家 arena** ✓
（报错在**调用点**："argument 2 of `stash` points into a deeper scope (depth 2) than the arena this call may store it in (depth 1)"）
—— 这条正是"链表出参"能安全写出来的原因 ✓

---

## 4. ⭐ 形式化那一块（本机最硬的成果）

文档：`ARENA-FORMAL.md`（推导）· `ARENA.md`（设计 + 证明）· `PLAN-REGION.md`（执行计划 + 总账）

### 4.1 把"分配放哪只区域"从**语法猜测**改成**约束求解**

- **引理 1**：分配区域 `H` sound ⟺ 对**每一个会被存进 H** 的指针（其区域 `ρ`）都有 `H ⊒ ρ`
  ⇒ 下界由「**实际流过那里的值来自哪**」决定，**不是**"实参自己住哪" ✗
- **两种流**（这是关键区分）：
  - **地址流**：存 `&实参`（调用者的指针进来了）⇒ 需要**实参活得够久** ✓
  - **内容流**：存 `l.head` 这种从容器里读出来的指针 ⇒ 需要**容器内容活得够久**（通常自动满足）✓
  旧规则把两者**合并**、并按最坏情况一律施加 ⇒ 大量**误拒** ✗
- **定理 2**：约束形如 `X ⊒ ⨆Yᵢ` ⇒ 有限格上**最小解存在唯一**；且格有顶 ⇒ **永远不需要用户注解** ✓
- **定理 3**：按最小解分配 ⇒ 所有执行都安全（不变量：槽位活着 ⇒ 它存的指针所指区域也活着）✓
- **定理 6.2**：**深度线性化是 sound 的** —— 命名地方必在当前作用域的**祖先链**上 ⇒
  同链上"深度比较 = 真实寿命序"（精确，不是近似），"兄弟块不可比"**不会同框比较** ✓
- **误拒分析（§7）**：两个旋钮 —— (A) 事实更细 / (B) 把分配放到**更长寿**的区域；
  **命题 7.1**：放大分配区域**永远安全** ⇒ 误拒**从来不是 soundness 逼出来的**，
  而是"不想为不逃逸的东西付长寿区代价" ⇒ **误拒与费内存是一枚硬币** ✓
  唯一**硬门槛**：**别名信息**（没有它就只能弱更新）⇒ 最小可用版本 = **取地址跟踪** ✓

### 4.2 让 checker **真的用**定理（2026-09-21 落地）

| 件 | 作用 |
|---|---|
| **效果摘要** `Addr`/`Cont`/`Home` | 每个函数回答"我往 `mut ref` 实参里存了什么"（地址流/内容流/Fresh）⇒ 机器证据：链表 `push` 的 **`Addr = ∅`** ⇒ 那条钉死家 arena 的约束**不存在** ✓ |
| **惰性传递闭包** `funcAllocates`/`computeEffectsTransitive` | memo + 环保护 + "是否完整"位 ⇒ **只有摘要被证明完整**才允许跳过保守规则 ✓ |
| **E 分析** | 哪些局部会被**搬出本函数** ⇒ 家 arena 按逃逸需求选（内容落在**调用者的家**）✓ |
| **字段级深度 + 取地址跟踪** | 每个绑定按**字段**记深度（根 = 各字段最大值）；**没取过地址 ⇒ 强更新**（覆盖），取过 ⇒ 弱更新 ✓ |
| **路径收窄（sound 子集）** | 根是**没取过地址的局部**时，`if m.next != null { m.next.value }` 直接可用 ✓ |

**结果**（双向判据都过）：
- **该放的放**：`varArray-return`（`[42, 43]`）· `list-return`（`求和 = 30`）· `varArray-asSlice-return`（`[7, 8]`）·
  `field-strong-update` · `path-narrowing` —— 全部**进 examples 且 ASan 干净** ✓
- **该挡的挡**：`tests/attacks/BASELINE` **一字不动**（27 个攻击形状，7 条已知安全，其余全挡）✓
- **golden**：老例子的生成 C **逐字节不变**（检查器改动不碰 codegen）✓

---

## 5. 工程与验收纪律（"同一条命令跑全套"）

| 工具 | 作用 |
|---|---|
| **`./check.sh`**（`quick` 跳基准）| 一把梭：构建 → 220 个测试（例子/反例/trap）→ arena 150 MB 上限 → **攻击库对基线** → 三段基准 ✓ **通过 4，失败 0** |
| **`tools/golden.sh`** | **重构安全网**：把生成 C 存成 md5 清单（可提交）⇒ 改编译器后**逐字节比对**。
  "纯移动代码的拆分不许改一个字节"就是被它背书的 ✓ |
| **双向判据** | 每次改检查器：**该放的放**（canary 转正例）+ **该挡的挡**（攻击库基线不动）✓ |
| **攻击库 + BASELINE** | 27 个"像 UB 的形状"；`BASELINE` 记 7 条**已知安全**，"通过集合变化 ⇒ FAIL" ✓ |
| **canary 策略** | 成对用例（必须拒绝 / 必须接受），新特性都配一对 ✓ |
| **文档三件套** | `DECISIONS.md`（编号定案）· `PLAN.md`（缺陷/路线图）· `DEVLOG.md`（**倒序叙事，含我犯的错**）✓ |

> 这套纪律的实测价值：本轮三次"看着对其实错"都是**判据**抓的，不是我的直觉 ——
> ① 摘要没做传递闭包 ⇒ `ref_arg_too_deep` 被放行 ✗ ② 分类器把"指针形参本身"当内容流 ✗
> ③ E 过度近似 ⇒ `out-param` 被误拒 ✗ 全部留痕在 `DEVLOG.md` / `PLAN-REGION.md` §6 ✓

---

## 6. 实测性能（2026-09-21，本机，本仓库脚本，**同算法同参数 + 输出逐位对拍**）

### 6.1 重负载（`bench/heavy/run.sh`）

| 负载 | extC | C |
|---|---|---|
| **binary-trees**（分配 + 递归）| **0.23 s · 9.9 MB** | 0.45 s · **18.0 MB** |
| **radix sort**（1e7 × u32 全 32 位）| 0.29 s · 142 MB | 0.27 s · 142 MB |
| **mandelbrot** | 0.14 s | 0.14 s |

⇒ **在"分配 + 递归"型负载上 extC 快约 2×、内存约一半**（arena bump 赢过 per-node malloc/free）✓
其余打平（内存带宽/浮点型负载）✓

### 6.2 小负载（`bench/run.sh`，ms）

| 负载 | extC | C:O1 | C:O2 |
|---|---|---|---|
| sieve | 9 | 8 | 9 |
| matmul | 6 | 5 | 3 |
| fib(32) | 8 | 8 | 4 |

### 6.3 多语言（`bench/multi/run.sh`，本机新增的 bash 驱动器）

| 负载 | extC | C | JS | Python |
|---|---|---|---|---|
| mod 3e7 | 271 ms | 270 ms | 422 ms | （3e6，不可比）|
| sieve 1e7 | 30 ms | 29 ms | 102 ms | （1e6，不可比）|
| matmul 512 | 35 ms | 33 ms | 229 ms | （numpy，不可比）|

### 6.4 压力 + 内存（`bench/stress/run.sh`，同种子）

四项结果**逐位相同**；RSS **extC 5944 KB vs C 5880 KB**（差 1% 量级）✓

### 6.5 编译速度 / 产物

`extc → C` 生成 ~3 ms；含 gcc 的完整构建 ~0.17–0.25 s（跟 `rustc -O` 同量级）；
可执行文件 ~17 KB（C + 8%）—— 生成的是**无聊的 C**，没有运行时库拖累 ✓

---

## 7. 规模现状

| 东西 | 数字 |
|---|---|
| 编译器 | **10 540 行 C**（14 个 `.c` + 内部头；按功能拆过：`check_escape.c` / `check_expr.c` / …）|
| prelude（用 **extC 自己**写的标准库）| 295 行：`option` / `result` / `slice` / `pcg32` 随机数 / `varArray<T>` |
| 例子 | 70 个 · 反例（必须报错）143 个 · 运行时 trap 6 个 · 攻击 27 个 |
| 文档 | 18 份 md · git 提交 202 个 |
| 测试 | **220 通过，失败 0** |

---

## 8. 已知限制 / 明确不做（诚实清单）

1. **别名信息缺失是唯一"信息性"硬门槛**：`if p.next != null` 里 `p` 是**参数**时仍会拒绝
   （调用者可能持有别名）⇒ 合法替代：`var x = p.next` 先拿本地 ✓
2. **值/计数不敏感**：不知道 `cap` 够不够 ⇒ 代价只是**可能多占内存**（不再造成误拒）✓
3. **无 free**：区域只能整块 LIFO 回收 ⇒ `clear()` 不还内存、没有 `shrink_to_fit`；
   扩容易留旧 buffer（所以扩容策略的指标是"累计分配"，倍增在**不能 free**的模型下被证明最优）✓
4. **明确不做**：迭代器 / 范围 for（等 `for` 与函数值）· `at()` 双入口（extC 的 `[]` 已经 trap 带位置）·
   分配器模板参数 · `vector<bool>` 位压缩 ✓
5. **账上还开着的**（见 `PLAN.md`）：IO（`rawRead`/`rawWrite` + `reader`，做完就能写五子棋）、
   `for` 循环、函数值/闭包、模块与 `@main`、`match` 当表达式；几条小缺陷（字面量越界不在编译期报、
   无限递归无声崩溃、arena 耗尽没位置…）✓

---

## 9. 怎么继续（主人自己动手时）

```bash
cd ~/extC_Compiler
./check.sh quick        # 一把梭：构建 + 全部测试 + arena + 攻击库（不跑基准）
./check.sh              # 连三段基准一起跑
./build/extc --run examples/varArray-return.extc      # 跑单个例子
./build/extc --dump-effects file.extc                 # 看效果摘要 + E 分析（调检查器用）
tools/golden.sh check   # 生成 C 跟基线逐字节比（改编译器/重构时用）
```

**文档地图**（都在仓库根目录）：

| 想了解 | 看哪份 |
|---|---|
| 语言用法（面向使用者）| `MANUAL.md` |
| 为什么这么设计（编号定案）| `DECISIONS.md` |
| arena / 内存模型 + 证明 | `ARENA.md` |
| **形式化推导（定理 + 误拒分析 + 让 checker 用定理）** | **`ARENA-FORMAL.md`** |
| 分档落地计划 + 三档总账 | `PLAN-REGION.md` |
| 还剩什么 / 缺陷清单 | `PLAN.md` |
| 开发过程（含踩的坑）| `DEVLOG.md` |
| 语法速查 / 数组 / 引用 / IO 设计 | `SYNTAX.md` / `ARRAYS.md` / `REFS.md` / `IO.md` |

---

## 10. 一句话总结

> **extC 把"安全"放在编译期（能证明的零痕迹、不能证明的响亮报错），把"内存管理"交给作用域（arena、无堆、无手动 free），
> 并把这套区域纪律**形式化**了 —— 于是"造完返回容器"这种写法由推导决定合法性，而不是靠特批或注解 ✓
> 代价是**可能多占一点内存**，换来的是：不用写生命周期、不用 `malloc/free`、跑起来 ≈ C（分配密集时更快）✓**
