# extC：一条原则

> v1（原则手术版）· 2026-09-18
>
> 本文的作用：把 v0 的 10 条原则收敛成 **1 条**，凡是从它推不出来的，当面承认是品味，不要伪装成推导。
>
> **推翻的**：v0 §2（编译策略「不建语法树、token 流模式替换」）—— 被判出局，理由见 §5。
> **其余**：不是推翻，是收敛。v0 大部分结论是对的，只是没被推出来，所以长得像拼盘。

---

## 0. 两个前提（不是原则，是选择）

原则是从前提推出来的；前提本身是选择，不证自明。

- **G1 · 内存自动回收** —— 用户永不写 `malloc` / `free` / `close`。
- **G2 · 第一个真实用户是五子棋** —— 不是自举。第一版目标 = **能写出主人的程设大作业（五子棋人机对战）**。自举是第二个目标。
- **G3 · 能自举** —— 编译器最终能用 extC 写出来。

前提要少、要硬、要能拿出去说。就这三条。

> **G2 是本次问诊最大的修正。** v0 把「四个月自举」当第一目标 —— 那是个虚荣的里程碑，而且时间上根本不可能。真正该问的第一个问题是：**extC 能不能编出一个能跑的五子棋？** 这个问题答案近、可测、而且正好是主人本来就要做的事。
>
> 副作用（好消息）：五子棋需要的东西（多维数组、深递归、搜索上下文、region、文件 IO、少量字符串）**比自举少得多**，所以第一版能砍掉一大半标准库。

---

## 1. 唯一原则

# P：凡是编译期能证明的，运行时不留痕迹。

三个词都是承重的：

| 词 | 含义 |
|---|---|
| **编译期** | 证明发生在按下编译时，不是运行时 |
| **能证明的** | 证明不了的不强求 —— 但见 P′ |
| **不留痕迹** | 不许有隐藏的元数据、隐藏的分配、隐藏的控制流、隐藏的类型标签 |

### 推论 P′（P 的逆否命题，与 P 等价）

# P′：不能证明的，语法上必须看得见。

P 负责「删」，P′ 负责「标」。**v0 的 §11.1（安全是默认，逃生舱要显式）就是 P′** —— 它不是第十一条原则，它是第一条原则的另一面。两条原则，一条。

最好的例子：`a[i]` 越界。边界检查是运行时痕迹，所以要么证明它（见 §4）、要么把它写在语法上（`a.at(i)?`）。没有第三种写法。

### P′ 的机械形式：`!`

P′ 不能只是一句话，它得有个符号。就是这个：

> **`!` = 「这里我签字，接受运行时的后果」。**

| 写法 | 说的是 |
|---|---|
| `a[i]!` | 越界会 trap，我知道 |
| `@recursive!` | 用真调用栈，可能栈溢出，我知道 |

两个好处：

1. **危险可以在源码里数出来** —— `grep -c '!'` 就是一次逃生舱审计。
2. **它是稀缺的** —— 语言里只有很少几个地方需要签字，所以每个 `!` 都值得看一眼。逃生舱要显式，但更要**少**（一旦 `!` 长出几百个，它就跟 `interface{}` 一样失去意义了）。

### 推论 P″（P + G1）

**自动回收的释放时机必须是编译期已知的 ⇒ 释放只能由词法作用域触发。**

因为释放时机若不是编译期已知，就必须由运行时决定 —— 那就是 GC 或引用计数 —— 那是运行时痕迹 —— P 禁。

于是有了 §2 的 arena 规则。

> **这是本文最重要的发现：v0 §3 把「arena / 作用域释放」和「逃逸检查」当成两个设计块，其实它们是同一条推论的两半。arena 不是第二条原则，是 P + G1 的机械形式。**

---

## 2. arena：P″ 的机械形式

**一只 arena = 一个词法作用域，或一个被显式命名的 region。** arena 构成嵌套树，越内层深度越大。

### 唯一分配规则

> 所有资源属于某个 arena；arena 结束时，它的资源一起死。

### 唯一引用规则

> 若引用 `r` 指向值 `v`，则 `arena(r)` 的深度 **≥** `arena(v)` 的深度。
> 人话：**引用所在的 arena 不能比被引用者更浅 —— 引用不能活得更久。**

### 为什么这条规则好

- **可判定**：arena 深度是纯词法属性，比两个整数。不需要生命周期变量、不需要解约束、不需要类型系统的帮助。
- **方向不能反**：`v` 在内层(深度大)、`r` 想存到外层(深度小) → 拒绝，因为 `r` 活得比 `v` 久。反过来允许，因为内层先死，`r` 早死无害。
- **正确性由词法嵌套直接担保**，不依赖任何流分析。

### 一条规则吃掉 v0 §3 的三套机制

| v0 里的东西 | 现在是什么 | 说明 |
|---|---|---|
| 局部变量 / 栈 | arena = 函数帧 | 同一个东西 |
| `Box<T>` | arena = 所在作用域，**只有值**的 arena | 不是独立机制 |
| `VarArray<T>` | arena = 所在 region | — |
| `String` | arena = 所在作用域 | 它只是个缓冲区 |
| `File` | arena = 所在作用域 | OS 句柄也在 arena 里 |
| `region` | **显式命名的 arena** | 全语言唯一为 arena 新增的语法：给 arena 起个名，用来推迟释放 |

**没有析构函数、没有析构顺序、没有所有权转移、没有 move 检查的必然性。** 「谁拥有它」这个问题消失了 —— **arena 拥有它**。

（v0 §3 说「分配绑定作用域」，方向完全正确，只是没意识到这句话已经把三套机制吃掉了。）

### 顺手解决一：全局变量

> **全局 = 深度 0 的 arena，永不结束。**

于是不需要为全局写任何特殊规则，而且 `static` 关键字直接消失：

1. 在最外层写 `var board: Board` 就是全局，没有关键字要记。
2. **引用规则自动兜住危险用法** —— 全局（深度 0）存一个指向局部（深度 ≥ 1）的 `ref`：`0 ≥ 1` 为假 → 编译错误。
3. **真正危险的那个 `static`（局部变量活得比函数调用久）在语法上不可达** —— 局部的作用域就是它的 arena，作用域结束它就死。「局部但活得比局部久」这句话说不出来。

### 顺手解决二：返回值

把引用从被调用者返回给调用者，是 `arena(返回值)` = 调用者(浅) < `arena(v)` = 被调用者帧(深) —— **直接违反引用规则**。

所以不需要额外规定什么，规则自己给出了唯一答案：

> **返回引用 ⇒ 被引用者必须在调用者提供的 arena 里。**

两种合法形态：

| 形态 | 例 | 何时用 |
|---|---|---|
| **传 out-arena** | `fn make(into: ref Arena) -> ref T` | 变长堆值（String、VarArray） |
| **按值返回** | `fn make() -> T` | 定长值免费；变长堆值仍需 arena 来源 |

**「结果自动落到调用者帧」被判出局**（这是最容易顺手做的一个语法糖）：语法上看不见那块内存在哪儿 —— 违反 P′；而且在循环里会静默堆积内存 —— 这正是 Zig 逼你传 allocator 的原因。

**结论：这不是我们挑了 Zig 的风格，是规则不允许别的写法。**

---

## 3. 从 P 推出的设计决策（一个不剩）

| 决策 | 推导 | 对应 v0 |
|---|---|---|
| 无 GC | P″ | §6 ✅ |
| 无引用计数 | P：refcount 是运行时字段 | （v0 没写，补上） |
| 无异常 | 异常是运行时展开的控制流；`Result<T,E>` 是**值** | §6 ✅ |
| `?` 合法 | 可见的语法、静态解析、无栈展开的转发 | §7 ✅ |
| 无 vtable / RTTI | 运行时分派；标签联合 + `match` + 穷尽检查 = 写出来的分派 | §6 ✅ |
| **无 `any` / `interface` / 反射** | 需要运行时类型标签 | **v0 没写，补上** |
| 泛型单态化 | 没有运行时分派，泛型只能是编译期展开 | §6 ✅ |
| 拓宽自动 | 可证明无损失 | §5 ✅ |
| 收窄必须显式、且必须命名失败方式 | 收窄是运行时动作（回绕/舍入/检查），不可证明 ⇒ P′ 要求可见 | §5 ✅ |
| 无 `as` 链 | 同上 | §5 ✅ |
| 无格式串 | **格式串是运行时解析**：`println("{}", v)` 在运行时干着编译期能干的活 | §8，但**理由要换**（见下） |
| 无变参 `...T` | `va_list` 是运行时参数元数据 | §8，**理由要换** |
| 索引/越界 | P 要求能证明的零痕迹 ⇒ 逼出范围类型 | §6，但**要重写**（见 §4） |

### 两处理由升级（v0 的理由太弱）

- **格式串**：v0 说「编译器可以用 `concat` 拼」。真理由：**格式串违反 P** —— 它在运行时做编译期能做的解析。输出 = 值与视图的拼接，没有格式语言。
- **变参**：v0 说「编译器不需要」。真理由：**`va_list` 是运行时痕迹，P 判出局**。不是「往后放」，是不做。

理由升级很重要：**「编译器不需要」是暂时的，「违反 P」是永久的。** 用暂时理由删掉的东西会被重新加回来。

### 一处主动收回（v0 的「6 种转换写法」不是冗余）

`.truncate()` / `.round()` / `.checked_to_int()?` 看着像三种多余的 API，其实是一条规则的三张脸：

> **可能失败的转换，必须在语法上命名它的失败方式。**

而"失败方式"真的只有三种：**回绕** / **就近** / **报错**。名字必须不同，因为行为不同。这三张脸留着。

**但 `From` / `Into` 砍掉**：那是用户扩展点，不是失败方式命名，v0 的编译器不需要它 —— 违反 §11.4「不做没人用的特性」。

### `@recursive`：把调用栈变成编译期数据（P 的直接推论）

`@recursive` 标记的函数，由编译器展开成**显式栈 + 循环**，不使用调用栈。

> **奶昔上一版把它当「品味」丢进了 §7 —— 错了，它是 P 的直接推论。**
>
> 调用栈是**隐藏的运行时机制**：深度由 OS 决定、溢出是运行时崩溃、每帧的内存不受 arena 管辖。
> 把它变成显式的数据，就是 P 那句话本身：**能显式化的，别留在运行时。**

这是 arena 之后的第二次同款出手：

| | 隐藏的 → 显式的 |
|---|---|
| arena | 「什么时候 free」从运行时 → 词法作用域 |
| `@recursive` | 「调用栈」从 OS → 编译期定长数组 |

#### 定案：`@recursive(maxDepth: N)`，留空走默认

主人问得对：「调用方传 region」那不就没法静态检查了吗 —— **对，而且这正是它被否掉的理由。** 静态上限买到的是一整条链：

> **深度上限是编译期常数 ⇒ 帧数组是编译期定长的局部数组 ⇒ 递归的全部内存在编译期确定 ⇒ 零堆、零运行时分配、不可能 OOM。**

帧数组甚至不用进堆，就是一个**定长局部数组**，跟着 `@recursive` 函数所在的 arena 走。
**写起来是递归，编出来是一个循环 + 一个数组。**

留空的默认值：注解可省（比如默认 64），或跟编译器参数。

#### 深度溢出：trap + 只读 `depth` —— 不是二选一，是一套

主人两个都想选 —— **其实不用选，它们是一件事的两半**：

| | 角色 |
|---|---|
| 越界 **trap** | **机制本身**：帧数组 `frame[depth]` 越界就是 trap，零新概念 |
| 只读的 **`depth`** | 让人**永远不必撞上**它 |

> **`depth` 之于递归，就是 `len` 之于数组。** `a[i]` 会 trap，但 `a.len` 让你能提前避开 —— 一个道理。

于是可以自己写优雅的降级：

```extc
@recursive(maxDepth: 64)
fn vcf(b: ref Board, x: i32, y: i32) -> i32 {
    if depth >= 60 { return 0 }        // depth 是隐式的只读绑定，像 self
    ...
}
```

`depth` **只读、不可写**，所以它不是新状态。而且它其实不是附加功能 —— **它是这个特性的最后一块**：

> 把隐藏的调用栈深度显式化，跟 `@recursive` 本身是同一个动作。

#### `!` 版的真递归

主人提的 `@recursive!` 很对，而且它不只是个方便的开关 —— 它跟 §1 的 `!` 是同一个符号的同一个意思：

| 写法 | 意思是 |
|---|---|
| `@recursive(maxDepth: 64)` | 编译器展开成显式栈；深度定死；**零运行时后果** |
| `@recursive!` | 用真调用栈；深度不可证明；**我签字** |

`!` 一旦有了「我签字，接受运行时后果」这个统一含义，`@recursive!` 就不是特例了 —— **它是那条规则的一个实例。**

#### 为什么它对五子棋有具体用处

VCF / VCT 是深递归搜索，工程上唯一的护身符就是深度上限（引擎协议里已经写了 VCF 20 / VCT 16）。`@recursive` 把那个上限从「希望别溢出」变成**编译器保证**；而「整个搜索的临时内存一次释放」正是主人设计里 `region` 的形态，两件事天然咬合。

#### 诚实说明难度（这才是它该进 v2 的真理由）

v0 §8 给的理由是「编译器递归深度可控」—— 又是弱理由。真理由是**这个变换本身很难**：

| 形态 | 难度 | 需要什么 |
|---|---|---|
| 直接递归，调用点不在循环里 | 易 | 显式栈 + 循环，帧 = 局部变量结构体 |
| **递归调用在循环里**（VCF 就是这种） | 中 | 帧里带循环变量 + 恢复点标签（状态机） |
| 互相递归成组 | 难 | 共享状态机，帧类型变联合 |
| 递归嵌在表达式中间 / 早返回 | 难 | 每个调用点一个状态，接近 CPS 变换 |

**v2 只做「易 + 中」两档（主人定案）。** 而「中」正好覆盖 VCF / VCT 那种「递归调用在循环里」的形态 —— **也就是足够覆盖五子棋。**

| 形态 | v2 |
|---|---|
| 直接递归，调用点不在循环里 | ✅ |
| 递归调用在循环里（VCF） | ✅ |
| 互相递归成组 | ❌ **编译报错** |
| 递归嵌在表达式中间 / 早返回 | ❌ 报错 |

处理不了就**报错，不静默退化**（见上面的实现纪律）；用户要么改写成单函数 + 自己的显式栈，要么写 `@recursive!`。CPS 那一档不做。

**所以它是 v2 的理由**：不是不重要，是它需要 AST / 类型信息 / arena 全部先稳下来 —— 而且现在可以确定，**它的范围是可控的**。

#### 两个必须处理的坑（奶昔发现的）

**坑一：帧在堆上会打破 §2 的深度规则。** 第 3 层和第 5 层递归的帧**词法深度相同**，但第 5 层先死。按 §2 的规则（`d(r) ≥ d(v)` 即放行），把第 5 层的 `ref` 存进第 3 层的帧会被判合法 —— 可它是悬垂的。

> **解法（不改规则）：帧对用户不可见。** 用户拿不到 `ref Frame`，就没法把某个深度的帧引用交给另一个帧 —— 这个不安全写法在**语法上不可达**。跟「局部但活得比局部久」是同一个手法：**用不可表达代替禁止。**

**坑二：编译器变换不出来时必须报错，不许偷偷退化成真递归。** 否则「安全」会静默降级 —— 那比没有这个特性更坏（P′：看不见的东西最危险）。

#### 它没有引入任何新语义（这才是它值得做的理由）

展开后就是「一个定长帧数组 + 一个循环 + 一个下标」。于是：

- **递归深度 = 数组下标** ⇒ **深度溢出 = 数组越界** —— 完全复用 §4 既有的机制，**语言里不多一个概念**。
- 想优雅处理溢出（而不是 trap），就自己传一个深度参数判。

---

## 4. 边界检查：P 逼出来的一个新设计

v0 说「索引有边界检查」。但**边界检查本身就是运行时痕迹** —— P 要求能证明的不留痕迹。

所以真问题不是「检不检查」，而是：**什么时候能证明？**

答案是**范围类型**：

```
for i in 0 .. a.len      // i 不是 int
                         // i 的类型是「已证明落在 a 范围内」的索引
```

| 情况 | 结果 |
|---|---|
| 可证明在范围内 | **不生成任何检查，运行时零痕迹** —— P 完美达成 |
| 不可证明 | `a[i]` **带检查**（越界 trap）；不想 trap 就写 `a.at(i)?`（返回 `Option<T>`） |

> **⚠️ 本条已被真实用例证伪并修正（2026-09-18）。**
> 初版写的是「不可证明 → 编译错误」。可五子棋里满屏都是 `b.cell[py][px]`，`py` / `px` 是运行时算出来的、证不出来 —— 初版规则会让**五子棋根本写不出来**（见 `SYNTAX.md` 里那段代码）。
> 修正：**P 说的是「能证明的不留痕迹」，不是「不能证明的不许写」。** P′ 说的是「不能证明的要看得见」—— `a[i]` 这个写法本身就承当了「这里可能 trap」的语义。**范围类型从门槛降级成优化**（让检查消失）。

配套约束（复用 arena 的机器）：索引证明绑定在某个数组绑定的**长度**上；长度一变，旧索引作废。所以 `a.len` 的稳定性也要静态追踪 —— `let a` 长度不变，证明免费；`var a` 且可能 `append`，证明作废，要重新取。

> **诚实标注**：范围类型是本次手术里**唯一的新想法**（v0 没有）。它现在的地位是**优化**：让可证明的索引零开销，而不是给不可证明的索引判死刑。代价是它要真能推出「`px >= 0 && px < 15` 之后 `cell[py][px]` 安全」—— 这种区间传播在 C 编译器里是常规手段，但叠上别名和函数边界就未必了（见 §8 问题 3）。

---

## 5. 编译策略：v0 §2 判出局

v0 §2 主张：不建语法树，在 token 流上做模式替换，不解析整个 C。

> **这句话里揉着两件事，必须拆开**（主人的原意就是在这儿被搅浑的）：
>
> - **「不解析整个 C」** → ✅ **保留**。透传过去的那部分 C 永远不需要解析，这是 §2 里完全正确的一半。
> - **「不建语法树」** → ❌ **出局**。要被检查的是 **extC**，而 extC 必须有 AST。
>
> 一句话：**不解析 C ≠ 不解析 extC。** 主人说的「不用建」是对 C 说的，不是对 extC 说的 —— 这条直觉本身没错，是总结者把它扩大化了。

**P 判「不建 AST」出局**，三个独立理由：

1. arena / 引用检查要**作用域树** —— token 流里没有树。
2. 单态化要**符号表**和实例化图 —— 有符号表就等于已经解析了。
3. P′ 要让错误「看得见」，就要精确 span 和类型信息 —— 透传 token 给不出好诊断。而 v0 §11.6 自己写着「错误信息是语言的一部分」。

**§2 省下的解析成本，会在检查器上加倍还回去。** 「不建语法树」和「用编译期检查替代运行时机制」是互斥的两条路，必须二选一 —— 既然 P 是这门语言的命，§2 出局。

### 新策略

```
tokenize → parse（完整 AST） → arena/类型检查 → 单态化 → C 代码生成 → gcc
```

保留 v0 §2 里对的两个直觉：

- **生成的 C 是「无聊的 C」** —— 不玩花活，不做聪明优化。
- **用 `#line` 把 C 的报错映射回 extC 的行号** —— 这才是 v0 想要的「轻量」的正确实现方式，靠映射而不是靠不解析。

### 泛型：主人的直觉是对的，而且比真单态化便宜

主人说「模板类我直接大手一挥宏定义，或者我编译器生成对应的 C 代码即可」—— **两条都对，而且是同一个办法**：

> **泛型不靠类型系统实现，靠 C 的宏 / 代码生成实现。** `Array<int>` → 生成一份 `array_int` 的 C 代码，或展开一个 `ARRAY_DEFINE(int)` 宏。

这比「真单态化」便宜得多（编译器里不需要维护实例化图），代价是 C 输出膨胀要量（§8 问题 5）。术语上这叫**语法层泛型**，对自举和五子棋都够用。

---

## 6. 判出局清单（直接拿去砍 v0 文档）

| v0 里的 | 处置 | 理由 |
|---|---|---|
| §2 不建语法树 / token 重写 | **删** | §5 |
| §3 的三套生命周期机制 | **合为一套** | P″ |
| §4 五种「指针替代品」 | **合为两件**：`ref` + arena | `Box`/`region` 都只是 arena |
| §5 `From` / `Into` | **删** | 用户扩展点，编译器不需要 |
| §6 / §8 / §10 的 C 互操作 | **统一为一句话**（见下） | 三处说法矛盾 |
| §6 多线程 | 保留为 v2 | 数据竞争不是编译期可证明的，要么禁止共享要么可证明，见 §8 |
| §7 `String<T>` 泛型 | **删泛型**，`String` = UTF-8 bytes | 泛型 String 不服务于 P；`WString` 让 `find`/`substr` 同名不同义，违反 v0 §11.7 |
| §7 `WString` | **删** | 自举不需要（v0 §11.4 自己的标准） |
| §7 `concat` | **改带 `into:`，或改成 `append`** | 变长堆值返回需要 arena 来源（§2） |
| §7 `substr`（返回新串） | **删，改 `view`（零分配）** | 词法分析器要的是源码视图，不是拷贝。顺带让 tokenizer 零分配 |
| §7 `to_bytes` | **删**（被 `view` 取代） | — |
| §8 格式串 / 变参 | **从「往后放」升级为「不做」** | 违反 P，理由见 §3 |
| §8 `inline_c` / `extern` | **从「往后放」升级为「唯一的逃生舱」** | 见下 |
| §10 定位写「Zig 的 C 互操作」 | **删这一条** | 它是边界声明，不是语言特性 |

### C 互操作：三处矛盾的一句话解法

v0 §6 说「不做」、§8 说「往后放」、§10 说是定位卖点。真正的答案是：

> **`extern` 不是语言特性，是唯一的逃生舱。** 它接的是一个不受 arena 管辖的外部世界 —— 所以它天然在 P 的管辖之外，也就必须**显式、可见、默认关闭**（正是 P′）。v0 里不做，v1 作为边界声明引入。

它不该是定位卖点（从 §10 删掉），也不该被永久禁止（从 §6 删掉）。

### 但有一条必须放开：标准库内部的 libc

主人的性能直觉（「C 的 mem/str 系列很快」）把这件事判死了：**要 `memchr` 级别的速度，就必须能碰 libc。**

> **定案：标准库实现可以调用 libc（由编译器生成），用户代码不能。**
> 用户写不出 `extern`，但 `String<u8>.find` 内部就是 `memchr`。

**P 没被违反**：用户手上的逃生舱依然只有一个；编译器自己生成 `memchr` 不是逃生舱，是后端细节。

三处矛盾（§6 不做 / §8 往后放 / §10 当卖点）到此画句号：**编译期悄悄用，用户用不到。**

### v0 §11 十条原则的去向

| v0 | 去向 |
|---|---|
| 1 安全默认、逃生显式 | = **P′**（P 的另一面） |
| 2 编译期能做的别留到运行时 | = **P**（本文唯一原则） |
| 3 没有手动分配就没有手动释放 | = **P″** |
| 4 不做没人用的特性 | 保留（**是 I/O 预算，不是原则**） |
| 5 用已知技术拼装 | 保留（**是策略，不是原则**） |
| 6 错误信息是语言的一部分 | 保留（**是 P′ 的实现要求**：看不见就谈不上可见） |
| 7 语法一致无特例 | 保留（**是审美**，但可用来判 §7 的 `WString` 出局） |
| 8 默认值语义、引用显式 | = **P′** 的一个实例 |
| 9 编译到 C 不是汇编 | 保留（**是手段**：C 是我们的后端，不是我们的目标） |
| 10 语言是给人用的 | 保留（**是 P′ 的另一面**：规则要能被人读懂） |

**十条里三条是 P 的化身，两条是 P′ 的化身。剩下五条不是原则，是预算、策略、审美和手段。** 原则只剩一条。

---

## 7. 推导不出来的（诚实清单）

这些**留着**，但必须标注为品味选择，不许伪装成推导 —— 伪装才是丑的根源：

- `let` 默认不可变 / `var` 显式可变（弱推导：不可变更可证明；但这是倾向，不是定理）
- 语法像 Go、可读性像 Python（这是**招募口号的品味**，可以要，别说它是推导）
- 管道 `|>`、契约（v2 候选）
- **命名规范**（camelCase / PascalCase / `name: Type`；整数写 `i32` 不写 `int`）—— **已确认的主人口味**，不是推导，但也不用再争
- **`let` 不可变 / `var` 可变** —— 同上，主人的 `var thisIsAGoodName: i32` 已经把答案写在脸上了
- ~~`@recursive`~~ —— **已移出本表**：它是 P 的直接推论，见 §3
- 契约的 P 视角备注：**运行时的契约断言违反 P**，所以契约只能是「编译期可证明的义务」，证明不了就不许写。这一点值得想清楚再决定要不要契约。

---

## 8. 悬而未决的真问题（五个，按硬度排序）

1. **arena 粒度**：函数帧就是一个 arena，还是每条语句/每个循环体一个？循环体里分配会不会静默堆积成 OOM？→ 决定 `for` 体是不是独立 arena，以及最常见的踩坑形态。
2. **`into:` 的语法形态**：位置参数？命名参数？后缀 `.into(a)`？→ 影响所有 API 的手感，越早定越好。
3. **范围类型 × 别名**（最硬）：`ref VarArray` 传进函数后，调用者那边的长度不变性还证得出来吗？可能逼出「传出去就作废，回来重新取」。**这条决定 §4 是不是真能落地。**
4. **`Box<T>` 的 move 语义**：既然「arena 拥有它」，那按值传 `Box` 之后源还能用吗？—— 可能真的可以完全不要 move 检查。**如果成立，这是相对 Rust 的一次真减法，值得专门验证。**
5. **单态化代码膨胀**：`VarArray<T>` 到处用，生成的 C 会不会爆？可接受，但要量过。

---

## 9. 标准库最小自举集（按新规则重写）

### String：泛型只做存储，单位语义进类型名

主人要 Unicode（byte 和 u32 两套都能用）—— **泛型保留**，但要划一条界线：

> `String<T>` 只是「变长的 T 缓冲区」。**单位语义属于类型名，不属于泛型参数。**

因为 `len` 在 `String<byte>` 上是字节数、在 `String<u32>` 上是码点数 —— 同名不同义正是 v0 §11.7 要禁的特例。

```
struct String<T> {
    data: ref T
    len:  size_t
    cap:  size_t          // ← v0 漏了，没有 cap 的 push 无法扩容
}

fn view(self: ref String, start: size_t, len: size_t) -> StringView  // 零分配
fn find(self: ref String, needle: StringView) -> Option<size_t>
fn eq(self: ref String, other: StringView) -> bool
fn append(self: ref String, other: StringView)      // 原地增长，需要 cap
fn push(self: ref String, c: byte)
fn len(self: ref String) -> size_t
fn is_empty(self: ref String) -> bool
fn at(self: ref String, i: size_t) -> Option<byte>
```

**关键收获：tokenizer 不需要任何堆分配 —— token 是源代码的 `view`。** 只有符号表才分配。这让自举里最热的那条路径零堆操作，也顺手让 arena 的压力小得多。

`view` 取代 v0 的 `substr`，是为了满足 P 而不是为了省事：**能证明的拷贝，就不该发生。**

### 其余

| 组件 | 备注 |
|---|---|
| `VarArray<T>` | arena 里，单态化 |
| `HashMap<K,V>` | 符号表、关键字表；v0 可用「字符串 → int」的小实现起步 |
| `File` | 读源文件、写生成代码；arena 里 |
| `Result<T,E>` + `?` | 解析错误、IO 错误 |
| `Option<T>` | 可空 |
| `match` + 标签联合 | token 类型、AST 节点、错误类型 |
| `struct` + `fn` + `self` | 编译器对象 |
| 递归 | 递归下降解析 |
| region | 每个编译阶段的临时内存（现在是「给 arena 起名」，唯一新语法） |

---

## 10. 一句话（修订版）

v0 的版本是四个「替代」，那是对其他语言的投诉清单。新版：

> **凡是编译期能证明的，运行时不留痕迹。**

推出：内存归 **arena**、错误归 **Result**、多态归 **标签联合 + match**、分派归 **单态化**、越界归 **范围类型**。
推不出来的，当面承认是品味。

---

## 11. 下一步

- [ ] 主人对着 §6 砍 v0 文档（这是一张可直接执行的清单）
- [ ] 定 §8 的问题 2（`into:` 语法）和问题 3（范围类型 × 别名）—— 这两个决定语言手感，越早越好
- [ ] 写 week-0 骨架：Python tokenizer + parser + C 代码生成，最小闭环（`int` / `let` / `if` / `while` / `fn` / 输出），跑通 `extC → C → gcc → 可执行`
- [ ] 拿问题 4（`Box` 要不要 move 检查）当第一个真实验：它是相对 Rust 最大的潜在减法
